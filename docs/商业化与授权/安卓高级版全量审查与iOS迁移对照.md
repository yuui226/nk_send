# 安卓高级版全量审查与 iOS 苹果内购迁移对照

日期：2026-09-19。状态：代码侧审查完成，支付功能未实施、未运行验证。

配套设计：[iOS 高级版与苹果内购接入方案](iOS高级版与苹果内购接入方案.md)。本文记录现有代码事实和迁移差异，不把建议作为已确认产品决定。

**本文同时作为实施参考与跟进台账。** 第 2、3 节按固定编号记录安卓行为、替代方式与差异；第 8 节使用同一编号记录具体 iOS 接口／接入点、验收要求和实施状态。后续完成某项时必须补实现位置和验证证据，不能只修改总进度。

## 1. 审查范围与结论

全局检索 `app/src/main` 中 `LicenseManager`、`isPro`、免费常量、高级提示、临期提醒、购买与激活相关引用，直接命中 14 个 Kotlin 文件；继续追踪设备身份、相机网络、录制、批处理、资源文案、备份配置、相关测试，以及服务端授权／订单／管理接口。并对照当前 iOS 的传输、监看和录制实现。

结论：**本次找到的全部高级功能权限均能由 StoreKit 2 的有效权益驱动，没有发现必须因为苹果内购而删除的高级功能。** 但安卓整套商业化流程不能照搬：授权身份、换机、续费、发码、退款吊销与人工改有效期有明确平台差异。当前无自建后台方案能完成主要购买和解锁需求，不能同时承诺保留安卓后台所有控制能力。

“全量”指本次仓库当前代码中找到的完整高级版调用链，不代表动态行为已全部测试，亦未查询线上价格、订单数据库或 App Store Connect 实际配置。

### 1.1 已确认：取消自有激活码，以苹果购买权益为准

- **iOS 不生成、输入、复制或保管我们自己的激活码，也不展示用于绑定授权的设备码。** 安卓的激活、续签、解绑、换机顶替流程不迁移。
- 权益关联购买时使用的 **Apple 账户，即“媒体与购买项目”账户**，不是手机硬件、设备 UUID，也不一定是登录 iCloud 的账户。同一购买账户在支持的设备上恢复权益，不需要重新付费或转移激活码；两种商品均已确认不开家庭共享。
- App 不管理 Apple 账户登录、不要求用户提供苹果邮箱／密码、不另外加入“通过 Apple 登录”作为购买前提。StoreKit 返回的已验证权益才是业务依据，不以设备指纹、iCloud 标识或自建账户替代。
- “恢复购买”通过 `AppStore.sync()` 后重新读取有效权益完成；正常启动先读 `Transaction.currentEntitlements`，无需用户每次手动恢复。恢复入口不等于续费、退款或取消订阅。
- `appAccountToken` 是开发者用于关联**自有服务账户**的可选 UUID，不是苹果账户 ID 或新的激活码；本次无自有账户体系，不为使用 StoreKit 而强行引入它。交易 ID／原始交易 ID 用于识别交易与订阅关系，也不能当成苹果邮箱或全局用户 ID。
- 账户切换后以 StoreKit 刷新结果重新评估，不长期沿用旧账户的本地 `isPro`；App 无法自行读取苹果登录凭据来比对账户，离线时也不能承诺立刻知道账户、续费或退款的最新变化。
- **用户已确认支持苹果优惠码兑换解锁。** 用户兑换的是苹果管理的优惠，兑换后由 StoreKit 交易交付对应权益；不另建发证系统，不接受安卓激活码直接解锁。赠送期限／折扣／优惠后续费规则尚待具体配置。

依据：[购买账户与 iCloud 账户可不同](https://support.apple.com/en-us/117294)、[StoreKit 购买与跨设备恢复](https://developer.apple.com/storekit/)、[主动恢复购买](https://developer.apple.com/documentation/storekit/appstore/sync())、[appAccountToken 的实际用途](https://developer.apple.com/documentation/storekit/transaction/appaccounttoken)。

## 2. 用户功能及界面对照

“可迁移”表示没有苹果支付方面的障碍，仍需要实现权限检查并验证业务行为。

| 编号 | 安卓实际功能／行为 | iOS 判断与迁移要求 | 主要代码依据 |
|---|---|---|---|
| A01 | 每天成功传输 25 个原文件，照片／视频／RAW 共用 | 可迁移。本地独立记账，失败、取消、跳过不扣；原图与效果图不重复计数 | `LicenseManager.kt:65,352`；`TransferViewModel.kt:2396` |
| A02 | 免费单文件最大 400 × 1024 × 1024 字节 | 可迁移。在队列处理处拦截；保留大文件未知大小哨兵的判断，不把未知大小随意当作 0 放行 | `LicenseManager.kt:348`；`TransferViewModel.kt:2117` |
| A03 | 免费允许批量选择／入队，超限任务失败、队列继续 | 可迁移。先判断本地已存在跳过，再检查额度，不把整批入队当成扣额度 | `FileListScreen.kt`；`TransferViewModel.kt:1988,2103` |
| A04 | 剩余额度流变化到 1～5 时提示 | 安卓 `quotaLeft.drop(1)` 跳过首次值，授权变更也更新该流。iOS 用户已确认改为成功传输后剩余 5／1 次提醒，当天去重，不因纯授权变化弹提示 | `FileListScreen.kt:771`；`LicenseManager.kt:277,293,355,425` |
| A05 | 免费监看每天累计 3 分钟，首帧准备完成后起算 | 可迁移。加载不扣时长、退出停止、归零退出并在列表提示；第 5 节的动态状态缺口需要处理 | `RemoteScreen.kt:446`；`FileListScreen.kt:765,1630` |
| A06 | 端侧录制：开始／暂停／继续／停止／保存 | 可迁移。免费时按钮可见且压暗，点击提示；实际开始函数也要门控。停止／保存已有录制必须始终可操作 | `RecControlBar.kt:64,158`；`RemoteScreen.kt:2337` |
| A07 | 效果图生成、滤镜、边框对免费版开放 | 可迁移。不要因接高级版而新增滤镜数、边框数或本地导出数量限制 | `LicenseDialogs.kt:320`；`LocalPhotoEffectsOverlay.kt` |
| A08 | 高级版自定义水印全部控制项 | 可迁移。含开关、文字／图片类型、文字内容、图片导入、字体、大小、透明度、位置、文字颜色与效果；图片水印按其支持项开放 | `SettingsScreen.kt:2150–2463` |
| A09 | 免费水印固定品牌默认值，用户偏好另存 | 可迁移。默认水印并非所有输出强制附加，按装饰开关使用；权限变化不覆盖高级版原偏好 | `TransferViewModel.kt:563,579`；`TransferStateTest.kt` |
| A10 | 设置、工作台、相机传输后处理均使用水印权限 | 用户已确认 iOS 入队时计算生效水印，后续查重／渲染使用固定快照。界面及新任务门控仍保留，不把退款后重新门控错误地用于修改已入队水印 | `SettingsScreen.kt:396,708,835`；`LocalPhotoEffectsOverlay.kt:197,403`；`TransferViewModel.kt:1299,1478,2014,2959`；本轮用户覆盖规则 |
| A11 | 免费显示“解锁高级版”，付费显示高级徽标；点击付费徽标放烟花 | 可迁移。连接页／照片列表的设置共用入口；购买成功、手动激活成功也触发庆祝，苹果恢复和自动续费不宜被当成新购反复庆祝 | `SettingsScreen.kt:632`；`HomeScreen.kt:876`；`FileListScreen.kt:620`；`Fireworks.kt` |
| A12 | 免费连接双脉冲、高级版金色连接成功效果；Debug 可单独预览高级动画 | 可迁移。特效读取权益，Debug 动画预览不得变成真实高级功能授权 | `HomeScreen.kt:188,902,1722,1906`；`HomeConnectionPresentationTest.kt` |
| A13 | 高级版比较表、年费／永久切换、暂未开放套餐、购买成功页 | 可迁移外观和功能说明，购买操作与文案按苹果重写；永久商品未返回不可用时不能展示虚构价格 | `LicenseDialogs.kt:266,963`；`PurchaseDialog.kt` |
| A14 | 年费常驻续费入口；续费弹窗剩余 30 天转橙，连接页 7 天内提醒；日期及剩余天数 | 能实现，但自动续费正常时应展示下次续费日期，不直接复制催手动续费文案；永久无到期日 | `LicenseDialogs.kt:85,90,638`；`HomeScreen.kt:159,458`；`SettingsScreen.kt:635` |
| A15 | 客服／反馈复制 QQ，付费流程的错误提示 | 反馈能力可保留；支付错误改用苹果状态，不能再提示“激活服务器／微信到账未激活”。联系客服不应成为苹果付款的前置步骤 | `LicenseDialogs.kt`；`SettingsScreen.kt:1298`；多语言 `strings.xml` |

补充范围：本次未发现 GPS 写入、普通连接、文件浏览、缩略图、照片预览、筛选、基础主题／语言／目录设置的独立高级版权限检查，不新增这些付费门槛。**端侧录制指将监看画面录制到手机；不是把相机自身录像按钮一概改为高级版专属。** 相机录像／调参仍受所属监看页面的免费时长约束。

## 3. 购买、授权与运营能力对照

| 编号 | 安卓实际能力 | 苹果方案与边界 | 主要代码依据 |
|---|---|---|---|
| B01 | 年费和永久同一 `isPro`，期限决定到期；永久另发新码、原年费码保留 | 两种商品都授予同一高级功能集合；有效永久优先，年度续订信息独立，不能丢弃仍在续订的状态 | `LicenseManager.kt:466,704` |
| B02 | 年费主动购买／提前续费，按 `max(现在,原到期日)+天数` 延长 | 改为年度自动续期，不能用重复购买同一有效订阅来“再囤一年”；重新开续订与买永久也不是同一动作 | `LicenseManager.kt:704`；`LicenseDialogs.kt:613` |
| B03 | 服务器下发价格、划线原价、约合每月价格、可售状态；缓存及年费兜底价 | 价格改由 App Store Connect 管理、StoreKit 读取。月均价只能辅助展示，必须明确实际每年扣费；不搬虚构划线价和 365 天假设 | `LicenseManager.kt:445–597`；`LicenseDialogs.kt:1054` |
| B04 | 切换套餐可以新建不同商品订单，旧付款码独立有效 | 苹果按真实 `productID` 处理每笔到账。年费待批准后用户改买永久，原年费将来获批仍可能出现；不能只按当前选中的按钮发权益 | `PurchaseSafety.kt`；`PurchaseSafetyTest.kt` |
| B05 | 微信／支付宝链接、二维码、下载／生成支付码、保存相册、分享／截图、回 App 查单 | 替换为苹果系统付款单。二维码及其图片下载／保存／权限／轮询／过期重建无需迁移 | `PurchaseDialog.kt`；`LicenseManager.kt:602–831` |
| B06 | 付款页失败分类、退避重试、付款后自动激活；重开／网络恢复补单 | 保留“付款不丢权益”的目标，机制改为购买结果、未完成交易、权益恢复与交易监听；不照搬二维码查单轮询和 `pending_order` | `PurchaseSafety.kt`；`LicenseManager.kt:125,133,654,673` |
| B07 | 六位激活码输入、设备码展示、复制保管、手动激活错误映射 | 普通苹果购买不生成安卓激活码。用“恢复购买”完成找回；已确认支持的苹果优惠码兑换是另一个操作，不把任意安卓码当成苹果授权 | `LicenseDialogs.kt:533,560,767`；`DeviceIdentity.kt` |
| B08 | 换机顶替旧机、单码一台、设备指纹绑定、不备份授权 | 不按原规则搬。苹果权益跟随购买账户，可在该用户支持的设备恢复；无需用户转移激活码。本方案不提供任意踢掉某台已购设备的后台控制 | `server/license-server.js:506–594`；备份 XML；`LicenseManager.kt:145` |
| B09 | 滚动 30 天计数激活达到 6 次可吊销；同型号小米指纹变化有豁免 | 苹果无自建后台方案不复刻这套设备风控；不能把正常多设备恢复视为换机滥用。已有家庭共享选项另行决定 | `server/license-server.js:487–577`；`payment-regression.test.js` |
| B10 | 自有 ECDSA 通行证、指纹校验、每 24 小时软续签、约 7 天硬到期、见过的服务器时间防回拨 | 替换为 Apple 验证结果及有效权益；不继续联系安卓授权服务器，不给永久购买强加每 7 天联网续签 | `LicenseManager.kt:69,212–300,409` |
| B11 | 年费到期清 token、激活码；通行证到期则提示联网重开续签 | 苹果到期后使用免费规则，但保留合法的苹果历史和恢复能力；不清历史交易，不把扣款宽限期误当作安卓 token 过期 | `LicenseManager.kt:281,300,409`；`HomeScreen.kt:465` |
| B12 | 购买页主动释放相机 AP 会话和 Wi-Fi 占用，关闭时恢复；USB／STA 不断开 | 必须平台适配。iOS 不机械执行 Android `requestNetwork` 解绑逻辑，不擅自断开正在传输的相机；按实际联网能力请求苹果，必要时提示用户处理网络 | `PurchaseDialog.kt:148`；`CameraViewModel.kt:1596` |
| B13 | 已解锁时连点版本号 7 次清本地授权测试免费版 | 不放入正式 iOS 授权逻辑。清本地标志不是取消／退款，StoreKit 会恢复有效权益；测试用沙盒或明确隔离的 Debug 注入 | `SettingsScreen.kt:1291`；`LicenseManager.kt:297` |
| B14 | 管理端生成赠送码、指定天数、改永久／追加天数 | 用苹果优惠码提供兑换解锁已确认；任意改既有 Apple 交易到期日、把年费交易直接变永久仍不能由普通 App 实施 | `server/license-server.js:597,715` |
| B15 | 后台解绑、吊销、解除吊销、退款后停用码 | 普通苹果方案不能任意删除用户已购权益；退款由苹果处理，App 收到实际撤销后重算权益。退款申请成功提交不等于退款已批准 | `server/license-server.js:684,696,741` |
| B16 | 自有订单／激活／绑定台账、营收及客服记录 | 不保留自有授权数据库仍可购买和解锁；运营改用苹果报表及交易信息，但不等于保留安卓设备绑定台账或任意按人搜索的 CRM。若要此类运营后台需额外设计 | `server/license-server.js:616`；管理脚本 |
| B17 | `LicenseManager` 兼管检查更新、APK 地址、安装统计等非付费功能 | 与购买管理解耦；iOS 正式版更新走 App Store，不把 APK 下载、安装或自有更新服务器调用移进 StoreKit 管理器 | `LicenseManager.kt:832`；`AppUpdateManager.kt`／`AppUpdateDialog.kt` |

### 不可混淆的四个区别

1. **两种购买都能解锁功能，不代表能自动互相替换。** 年费改买永久不自动关闭年费，亦不自动退剩余年费；用户已接受通过苹果管理页自行取消的替代流程。
2. **不建数据库，不等于没有授权逻辑。** App 仍需验证、恢复、到期／宽限期处理和各功能门控；苹果不会统计每天传了几张或监看了几分钟。
3. **同一 Apple 账户多设备使用，与家庭共享是两件事。** 即便不开放家庭共享，也不能沿用“换到新手机就使旧手机失效”的安卓承诺。
4. **技术上能退款申请，与开发者能随意吊销不是一回事。** 普通 StoreKit 提供用户退款申请 UI；Advanced Commerce 的取消／撤销接口需要特定资格与服务端调用，不在本次方案中。

苹果依据：[购买与跨设备恢复](https://developer.apple.com/storekit/)、[订阅管理与套餐模型](https://developer.apple.com/app-store/subscriptions/)、[退款申请](https://developer.apple.com/documentation/storekit/transaction/beginrefundrequest(for:in:))、[Advanced Commerce 资格](https://developer.apple.com/in-app-purchase/advanced-commerce-api/)。

## 4. 优惠码兑换及其他运营替代

- **赠送高级版：**用户已确认支持苹果优惠码兑换解锁。苹果优惠码支持非消耗型等内购，也支持年度订阅免费／优惠期。在 App Store Connect 创建并由用户通过苹果兑换，不需要搭建自己的激活库。它不是安卓六位码：有商品审批、兑换资格、次数、有效期和系统版本要求。具体活动和入口布局待定；订阅免费优惠可配置优惠结束不自动续费，但并非所有优惠都默认如此，也不代表兑换新码会自动取消其他既有订阅。
- **补偿延期：**苹果 App Store Server API 有订阅延期能力，但适用于符合条件的服务中断补偿，具有期限和次数限制，需安全的服务端或受控管理工具调用。它不是给任意用户随意加天数的客户端接口；无后台首版不把它列为必做，也不把签名私钥放 App 内。
- **年费送永久：**可考虑让用户兑换永久商品优惠码，之后仍需处理旧年费续订；不能只把本地 `expirationDate` 改成空值假装苹果永久购买。
- **调整售价：**App Store Connect 能改价／配置可售地区，现有订阅者可能保留旧价格；新购展示价不能直接当成所有老用户下一次的实际续费价。涨价同意、保留旧价和优惠是苹果各自的规则，不照搬服务器全员即时替换价格。
- **售后入口：**用户确认退款交给苹果，App 只响应实际权益变化并复用免费规则，不新增自有退款流程或独立申请按钮。保留苹果官方退款渠道说明／反馈；申请处理中不提前降级，明确撤销后重算，仍有其他有效高级权益则继续高级。

苹果依据：[内购优惠码](https://developer.apple.com/help/app-store-connect/manage-in-app-purchases/create-offer-codes-for-in-app-purchases)、[订阅优惠码](https://developer.apple.com/help/app-store-connect/manage-subscriptions/set-up-subscription-offer-codes)、[订阅延期适用范围](https://developer.apple.com/documentation/appstoreserverapi/extending-the-renewal-date-for-auto-renewable-subscriptions)、[定价和老用户价格](https://developer.apple.com/help/app-store-connect/reference/pricing-and-availability/in-app-purchase-and-subscriptions-pricing-and-availability)。

### 4.1 用户提出的“优惠一部分，剩余仍走苹果付款”

**年度订阅与永久非消耗商品均有苹果官方付费优惠码机制，不限于免费赠送。** 非订阅内购在后台选择 `Paid Offer` 并指定优惠后售价；订阅选择支持的付费优惠形式、持续时间及优惠价格。用户兑换后仍由苹果展示实际优惠付款条件并收款，最终凭验证后的交易交付；不是输入码后直接设置高级标记。

- **按优惠后价格配置，不是任意面额代金券。** 例如以暂定人民币目标价举例：年费 ¥39.99 减 ¥10 对应配置优惠价 ¥29.99，永久 ¥99.99 减 ¥20 对应 ¥79.99。以上仅解释机制，金额是否可选须核对苹果价格档位，不能记为活动已确定；App 不自行在 `purchase()` 中传减免金额，也不新建低价“永久版”商品来模拟每张券。
- **年费明确优惠覆盖几期。** 若活动是首年优惠，应配置一年的付费优惠，之后按苹果向用户说明的标准年费续订；不是以后每年都减同样金额。现有订阅用户兑换可能作用于后续续订期，不能承诺对已经支付的订单追溯立减或退款。
- **免费赠送与付费折扣分别配置。** “免费优惠结束不自动续费”的可选规则只适用于苹果支持的免费优惠，不照搬到付费折扣。付费年费仍有自动续订，用户可取消；永久折扣购买仍是一次付清。
- **兑换就是购买流程的一条入口。** 使用苹果兑换界面／链接完成优惠校验和付款。无需自制验码服务器，不把普通 `Product.displayPrice` 当成用户已兑换的最终优惠价格；以苹果确认单为准。兑换成功产生交易后直接交付，不再让用户回到原价按钮重新买一次。
- **统一交易接收。** App 外兑换由 `Transaction.updates`／后续权益刷新接收；如果所选系统 API 直接返回验证后的交易结果，也送入同一处理器。按最低 iOS 16 做 API 可用性分支，永久优惠码的 iOS 16.3 支持要求仍保留；不把新 SDK 示例直接当成最低系统可调用接口。
- **活动参数按需配置。** 指定商品、地区、资格、优惠价／期限、兑换次数及截止时间；定向发放优先考虑一次性码。停用未兑换码不等于撤回已兑换权益，仍以苹果交易状态为准。

依据：[非订阅内购付费／免费优惠及优惠价格配置](https://developer.apple.com/help/app-store-connect/manage-in-app-purchases/create-offer-codes-for-in-app-purchases)、[订阅付费优惠及自动续期规则](https://developer.apple.com/help/app-store-connect/manage-subscriptions/set-up-subscription-offer-codes)、[兑换界面与交易交付](https://developer.apple.com/documentation/storekit/supporting-offer-codes-in-your-app)。本轮未登录 App Store Connect、未生成优惠活动／代码或验证实际优惠付款。

## 5. 不能照搬的动态状态缺口

以下为源码推导出的风险，未在安卓或 iOS 实机复现，不写成已确认的运行结果；本轮不改安卓代码。

### 5.1 页内到期和跨日可能沿用旧监看额度

`RemoteScreen.kt:451` 用不带日期或授权 key 的 `remember` 初始化 `trialLeftMs`。高级版进入时初值来自 `Long.MAX_VALUE`；若留在页面期间降级，`if (!isPro)` 会开启计时，但没有重新读取当天免费余额。免费状态跨自然日也没有重新装载页面剩余时间，账本换日不等于页面倒计时自动重置。

iOS 实施要求：在授权变化、日期变化和前后台恢复时协调当前页面计时与账本，避免无限试用或误扣；先定义并测试状态切换，不能复制“只在进页读一次”。

### 5.2 录制权限必须在异步返回后复查

`startRecorder()` 有 `isPro` 检查，但麦克风授权返回直接调用 `startRecorderResolved()`；后者仅检查是否已录制，没有再次验证高级权限。等待系统授权期间若到期／退款，存在绕过起录前门控的窗口。

`RecControlBar` 注释假定“免费用户不会正在录制”，实际停止分支仍允许执行，这是应保留的保护。iOS 需在真正开录前复查，并确定已经录制时降级的结束／保存策略；不能让用户因权限失效无法停止或保存已录内容。

### 5.3 两类效果图任务的权限快照时机不同

- 相机传输后的派生图：`TransferViewModel.kt:2959` 在真正轮到渲染时重新读取 `isPro`；查重与导出共用同一生效水印。
- 本地工作台：`LocalPhotoEffectsOverlay.kt:327` 点击生成时传入效果快照，`LocalPhotoBatchViewModel.kt:50` 后续批次直接使用该快照，没有逐张重新读取授权。

以上是安卓代码事实。**用户随后明确“已加入队列的肯定不改变水印”，iOS 实施以此为准：所有任务在入队时按当前权限计算并固定生效水印，相机派生图也不在执行前重新读取权限换水印。** 本地批次在点击生成建立任务时固定快照；仅选择照片尚未生成不算已入队。

该差异由用户明确授权。已入队快照用于查重、渲染和原任务重试，升级／到期／退款／设置编辑都不回写旧任务；相机下载后创建派生子任务要继承原入队快照。新建任务用当时权限。水印图片素材也保持任务期间可用且不可被覆盖，不能只有结构体不变而其引用图片已经改变。此规则不冻结全局高级权益，传输额度和大小限制仍按各自业务节点执行。

### 5.4 待批准的旧年费稍后到账

安卓支持同时存在不同商品旧订单；苹果可能出现年度购买待批准、用户后来买了永久、旧年度随后获批。即使本机禁用了重复点击，也不能保证账户层面只发生当前选中的一笔交易。

接收任何已验证交易时按商品与实际权益更新，并重新判断是否出现“永久＋年费仍自动续订”。提醒不能只放在刚买永久成功的那一刻。已处理交易 ID 后续撤销也不能被幂等去重丢弃。

### 5.5 默认状态和临期文案不能沿用安卓假设

安卓年费到期清码，用户回到新购流程；苹果仍有可恢复历史及可能的续订、宽限期。加载中、无权益、暂时离线、价格获取失败、待批准必须分开；正常自动续订不能每到 30／7 天就被提示“授权即将失效，请再买一年”。

## 6. 测试与资源覆盖

已阅读的相关测试依据包括：

- `license/DeviceIdentityTest.kt`、`ProductIdTest.kt`、`PurchaseSafetyTest.kt`：设备身份、旧商品兼容、切套餐、支付码来源、锁定价、错误分类和退避。
- `ui/screen/LicenseDialogPolicyTest.kt`：USB／连接页／AP／STA 的激活入口网络分支。
- `viewmodel/TransferStateTest.kt`：免费品牌水印与高级偏好隔离、图片水印和位置约束。
- `ui/screen/WatermarkPlacementPreferenceTest.kt`：临时位置回退不覆盖真实偏好。
- `ui/screen/HomeConnectionPresentationTest.kt`：免费连接效果和临期提醒门槛。
- `server/tests/payment-regression.test.js` 相关用例：换机及豁免、年费／永久商品、原码续费、订单幂等；本轮没有执行这些测试。

文案覆盖 `values/strings.xml`、`values-zh/strings.xml` 和 `values-b+zh+Hant/strings.xml` 的购买／激活／套餐／续费／换机／额度相关内容。iOS 已有部分安卓翻译资源不代表功能已接入；需要消除可达 UI 中微信扫码、六位码、设备码、复制激活码、解绑顶替、授权服务器等失效描述，同时保留其余功能文案含义。

当前 iOS 有监看、端侧录制和效果处理实现，例如 `RemoteMovieRecording.swift` 中的 `RemoteViewfinderRecorder`；尚无 StoreKit 权益接线，不能只加购买页就认为迁移完成。

## 7. 产品决定与当前状态

年度自动续期、永久非消耗商品、保留月均价、无自有激活系统、支持苹果优惠码已确认，不重复询问。用户随后确认人民币目标价、年转永久替代流程、不开放家庭共享、16 天已付费续订宽限期，暂不考虑安卓迁移。D07–D09 的三组体验选择也已确认；余下具体配置不是重新讨论已定原则，不要求用户决定接口或内部结构。

| 编号 | 待定事项 | 推荐方案／需要的输入 | 何时需要 |
|---|---|---|---|
| D01 | 年费／永久售价与销售地区 | 人民币目标价暂定 ¥39.99／年、¥99.99 永久；实际档位待苹果后台核对。若不能精确采用，不擅自改成其他正式价；海外销售地区及对应价格尚未指定 | 目标价已确认；上线前落实档位／地区 |
| D02 | 年费转永久的取消流程 | P03 已获接受：购买前说明，永久立即交付，之后由用户在苹果管理页取消旧年费；仍续订时保留状态入口，不反复强弹 | 已确认；待实现、验证 |
| D03 | 家庭共享 | 两种商品均不开；同一购买账户自己的多台设备恢复仍支持 | 已确认；后台配置待落实 |
| D04 | 扣款失败宽限期 | 启用 16 天，仅适用已付费订阅续订；期间继续高级功能，以苹果状态和截止时间为准，不自己加天数 | 已确认；后台配置／沙盒验证待完成 |
| D05 | 优惠码用途与活动规则 | 支持免费赠送及真正的付费折扣码，后者按预设优惠价通过苹果付款，详见第 4.1 节。具体优惠金额／持续时间／资格／数量按活动另定；免费赠送结束是否续费仍需在该活动配置时明确 | 能力方向已确认；具体活动在发码前确定 |
| D06 | 安卓老付费用户待遇 | 用户明确当前不考虑，不实现迁移／补偿逻辑或跨平台授权承诺 | 已确认暂不纳入范围 |
| D07 | 运行中失权的任务规则 | 已确认录制安全停止／保存及所有已入队水印不变，覆盖相机派生图与本地批次；新任务用当前权限。监看剩余时间、暂停／继续与保存失败的技术收尾需实现验证，不再改旧任务水印 | 原则已确认；待实现、验证 |
| D08 | 提醒频率与文案 | 已确认剩余 5／1 次提醒，正常自动续费不催买。取消续订显示有效期、扣款问题提供管理入口；不额外添加尚未要求的临期弹窗 | 已确认；待实现、验证 |
| D09 | 退款后的处理 | 已确认由苹果退款，App 响应权益撤销；无其他有效高级权益时 isPro=false，复用免费逻辑，不新增独立申请按钮／自有退款流程。手动清本地标记不等于退款或取消续费 | 已确认；待实现、验证 |

商品 ID／订阅组的命名由开发拟定并与既有 App Store Connect 配置核对，不需要用户手工设计 API 标识。离线遵守“已验证有效可用、未知最新续费需确认、不自行额外赠送时间”的现有原则；客户端计时、跨日修正、恢复监听和错误映射是实现职责。任意改期限的后台、设备解绑／吊销和自建跨平台账户仍不在已确认范围。

配置依据：[宽限期时长与适用续订类型](https://developer.apple.com/help/app-store-connect/manage-subscriptions/enable-billing-grace-period-for-auto-renewable-subscriptions)、[免费优惠结束是否续订](https://developer.apple.com/help/app-store-connect/manage-subscriptions/set-up-subscription-offer-codes)。

本次审查任务 1/1 完成；主接入方案实施状态仍为文档调研 3/8。未改业务代码，未构建／安装／运行测试，未提交或推送。没有宣称真实购买、沙盒或所有运行中授权变更已验证。

## 8. 逐项实现参考与跟进台账

### 8.1 统一接入约定

以下模块名称是**拟采用的职责划分，不是仓库已有实现，也不是已完成的代码**。实施时可依现有结构调整名称，但必须保留各项职责和独立业务生命周期。

| 拟建职责 | 实现方式与边界 | 现有接入位置 |
|---|---|---|
| `StoreKitPurchaseStore`：商品、购买、恢复、交易处理 | 应用级唯一实例；加载商品，接收购买返回、交易更新与未完成交易，验证后统一处理。异步读取不进入渲染／传输热路径 | `ios/ZTransfer/App/ZTransferApp.swift` 创建并持有；购买／状态界面共用 |
| `ProEntitlement`：可观察的权益快照 | `isPro` 为有效权益的派生值；权益扫描保持一致，阻止旧异步结果覆盖新状态。价格与年度管理信息独立加载，不能阻塞已确认权益；各 UI 只观察需要的值 | 设置、连接、照片列表、监看和效果处理读取同一来源，避免全局对象变化使整页重绘 |
| `FreeUsageStore`：免费额度 | 本地持久化当天成功传输次数、监看累计时长；两类账本独立。串行更新，传输与计时不查询苹果。系统日历换日，监看使用单调经过时间，前后台恢复协调 | `ios/ZTransfer/Domain/TransferQueue.swift`、`ios/ZTransfer/Domain/RemoteViewModel.swift`；列表只观察额度与提示 |
| `ProFeaturePolicy`：实际功能门控 | 本地纯判断与生效水印计算；保留高级偏好、任务配置和批次快照的原作用域，不用一份可变配置覆盖所有任务 | `ios/ZTransfer/Domain/PhotoEffects.swift`、`PhotoEffectsBatch.swift`、`LocalPhotoBatchViewModel.swift` 及实际录制入口 |
| 高级版介绍／权益状态 UI | 保留安卓说明、布局和已确定的效果；付款交给苹果。使用现有弹窗组件，购买、恢复、管理订阅分别对应真实操作 | `ios/ZTransfer/UI/SettingsView.swift` 及连接页／列表已有设置入口；具体新视图待实施 |

### 8.2 功能接入：A01–A15

“待实现”专指高级版权限接线，不表示对应的传输／录制／编辑基础功能不存在。差异和安卓源码依据见第 2 节同编号条目。

| 编号 | iOS 用什么实现／替代 | 验收条件 | 当前状态 |
|---|---|---|---|
| A01 | `FreeUsageStore`＋`TransferQueue` 单 worker 任务开始门控＋正式原文件成功落盘的唯一记账点；高级状态完成时不增加免费计数，不预建额度预占数据库 | 第 24／25／26 个边界正确；失败、取消、跳过、派生效果图不多扣；重复完成幂等；异步重入不启动第二个下载 worker | 待实现、未验证 |
| A02 | `ProFeaturePolicy` 在实际传输入口比较文件字节数；复刻安卓未知大文件哨兵处理 | 恰好 400 × 1024 × 1024 可传，超 1 字节被限制；未知大小不误放行；高级版不受此限制 | 待实现、未验证 |
| A03 | `TransferQueue` 逐任务读取权益／余额；先执行跳过判断，再决定能否开始；`TransferQueueViewModel` 保留批量入队入口 | 免费可多选；混合跳过／失败／成功队列持续处理；购买成功不擅自重启所有失败任务 | 待实现、未验证 |
| A04 | 余额仍共用 A01 账本；仅成功传输后的剩余 5／1 次触发提示，当天阈值去重，不在 SwiftUI `body` 中发提醒 | 初次观察、重绘、纯权益变化不弹；不改 25 次限制；到零沿用正常超限拦截 | 用户已确认精简；待实现、未验证 |
| A05 | `RemoteViewModel` 首帧 ready 后驱动免费监看计时，每秒累计到独立账本，退出／停止收尾；不能只在退出结算；观察权益／日期／前台变化 | 等待首帧不扣；反复进入累计 3 分钟；中断进程验证已用量保留；耗尽退出到列表提示；降级、跨日不沿用无限额度 | 待实现；运行中边界待定、未验证 |
| A06 | 录制 UI 与实际开录函数同时读取 `ProEntitlement`；麦克风授权返回后复查；已有录制停止／保存独立于新增录制权限 | 免费起录被拦，付费可起录；异步授权期间到期不能绕过；确认的安全停止／保存与监看退出顺序要验证 | 安全收尾已确认；待实现、未验证 |
| A07 | 保留现有效果图、滤镜、边框处理路径，不加高级版 gate；仅水印自定义部分读取 A08–A10 策略 | 免费版仍能选择滤镜／边框并生成；不出现新增数量或导出限制 | 待接线回归、未验证 |
| A08 | `SettingsView`／效果编辑器控件及保存入口使用 `ProFeaturePolicy`；图片导入回调也检查，避免仅依靠按钮置灰 | 免费不能设置任何高级水印项；付费文字／图片各项可用；系统选图返回时权限变化有正确处理 | 待实现、未验证 |
| A09 | 在 `PhotoEffects` 侧由原始偏好＋权益计算“本次生效水印”，免费默认值与高级用户偏好分别处理 | 降级遵守装饰开关与安卓默认值；重新付费可读回原偏好，不因免费计算覆盖用户配置 | 待实现、未验证 |
| A10 | 入队时按当前权限计算不可变生效水印；相机派生子任务继承原任务快照，本地批次生成时固定。查重、渲染与原任务重试均使用快照，不再按实时 isPro 更换 | 入队后升级／到期／退款／编辑均不改水印及素材；同一快照下查重／输出一致；新建任务按新权限；不豁免其他免费限制 | 用户已确认覆盖安卓采样时机；待实现、未验证 |
| A11 | 设置入口读取权益快照；新购庆祝绑定明确用户购买流程的已验证成功事件，不能仅以“此交易首次被处理”为依据；徽标点击可单独放烟花 | 免费／年度／永久入口正确；重装后的历史交易、恢复、自动续费不反复庆祝；交付与动画去重独立，现有动画时序保持 | 待实现、未验证 |
| A12 | 连接成功效果读取统一权益；Debug 预览只覆盖动画选择，不写购买管理器或额度账本 | 免费双脉冲与高级金色效果正确；Debug 预览不能解锁录制或水印；不在动画帧内查苹果 | 待实现、未验证 |
| A13 | 自定义套餐页＋`Product.products(for:)`＋`Product.purchase()`；读取 `displayPrice`／订阅周期，沿用现有弹窗组件 | 两商品独立加载／可售判断；缺商品禁用对应购买；介绍表与 A01–A10 一致；价格和系统付款单一致 | 待配置、待实现、未验证 |
| A14 | `Product.SubscriptionInfo.status`，验证 `status.transaction` 与 `status.renewalInfo`；使用 `expirationDate`、`willAutoRenew`、`gracePeriodExpirationDate`；管理入口用 `AppStore.showManageSubscriptions(in:)` | 已取消但未到期仍高级；正常续订不催手动续买；宽限期不误锁；永久不虚构到期日；系统页返回须刷新 | 待实现；平台文案待核对、未验证 |
| A15 | 反馈复制能力继续用现有平台实现；购买 UI 根据 `PurchaseResult`／StoreKit 错误分类显示取消、待批准、不可用、失败与重试 | 简体／繁体／英文可达页面不出现安卓扫码和激活服务器错误；用户取消不误报付款失败或降级 | 待实现、未验证 |

### 8.3 购买及运营替代：B01–B17

本表中的“旧机制不迁移”是平台方案的处置，不等于对应替代入口已经实现。“可选”项不自动纳入首版，不计作已完成开发。

| 编号 | iOS 用什么实现／替代 | 验收条件／明确差异 | 当前状态 |
|---|---|---|---|
| B01 | App Store Connect 年度自动续期商品＋永久非消耗商品；由 `Transaction.currentEntitlements` 聚合有效权益，年度续订状态另读 `subscription.status` | 两商品同一功能集合；永久优先，但不丢年费续订信息；永久撤销后仍检查有效年费 | 待配置、待实现、未验证 |
| B02 | 年费续期由苹果扣费／生成新交易；`AppStore.showManageSubscriptions(in:)` 代替安卓手动续费流程；永久仍独立调用购买 | 不用本地“到期日加 365 天”；取消不提前剥夺已付期限；旧年费按用户已确认的管理页流程取消 | 交互已确认；待实现、未验证 |
| B03 | App Store Connect 配价／可售地区；`Product.products(for:)` 读取 `price`、`priceFormatStyle`、`displayPrice` 与 `subscriptionPeriod`；商品状态与权益状态分开缓存 | 不读取安卓兜底价、划线原价；正确本地化；月均仅辅助，明确每年付款；新售价不冒充老客实际续费价 | 待配置、待实现、未验证 |
| B04 | 用苹果交易的 `productID`、交易 ID 和原始交易 ID 关联处理；`Transaction.updates` 接收晚到结果；不自建安卓 pending order | 待批准年费在永久后到账仍按真实商品处理；出现双权益时更新提醒；不按当前选中套餐错误发放 | 待实现、未验证 |
| B05 | 调用 `Product.purchase()` 展示苹果系统确认单；移除自有微信／支付宝付款链接、二维码以及保存／分享支付图片专用流程 | 付款入口只进入对应苹果商品；不申请付款码相册权限、不启动二维码下载或查单；普通照片保存不受影响 | 旧机制不迁移；替代待实现 |
| B06 | `purchase()` 返回值和唯一 `Transaction.updates` 共用处理；启动时一次 `unfinished` 恢复扫描也复用它，不轮询。有效权益及时交付后 `finish()`，不等待价格、庆祝或取消旧年费；前台合并刷新 | 付款后杀进程不丢权益；取消／待批准有区别；重复处理收敛到相同权益；弹窗关闭不停止监听；旧刷新不覆盖新状态；不建全量订单去重表 | 待实现、未验证 |
| B07 | 移除激活码／设备码／手动激活 UI；设置中“恢复购买”调用 `AppStore.sync()` 后刷新权益；普通启动直接读取当前权益 | 重装／新设备同一购买账户可恢复；无购买、恢复失败、有效恢复分别反馈；全程不要求安卓码 | 已确认无自有激活码；替代待实现 |
| B08 | 交由 StoreKit 提供购买账户当前可用权益；使用 B07 恢复；不采集设备指纹用于发证或互踢 | 同账户多设备可用；换机无需手动解绑；换购买账户重新评估；不承诺离线实时识别账户变化 | 旧单设备机制不迁移；恢复待实现 |
| B09 | 不实现安卓激活频次／小米型号豁免；交易有效性由 StoreKit 验证，业务门控读取权益；两商品不开家庭共享 | 正常多设备恢复不能触发“激活过多”；不误开家庭共享，不承诺自有设备封禁 | 旧风控不迁移；家庭共享已确认关闭 |
| B10 | 使用 `VerificationResult.verified`、商品白名单、当前有效状态及交易签名依据；替换自有 token、ECDSA 公钥／指纹续签与定时授权 HTTP 请求 | 无效验证不新增权益；离线仍有效不降级；永久不被强加 7 天联网；未知／过期不能伪造延长 | 旧签发机制不迁移；权益管理待实现 |
| B11 | `subscription.status`＋已验证交易的期限／撤销字段重新聚合；已知期限边界和回前台触发刷新；保留苹果历史及本地配置 | 到期后按免费账本；宽限期与纯扣款重试区分；一项到期不抹另一项；不清用户水印偏好或误判历史交易仍有效 | 待实现、未验证 |
| B12 | 使用 StoreKit 实际请求结果处理联网失败；相机连接层保持原有任务，不移植 Android `requestNetwork`／`releaseNetwork` | 相机 AP 无外网可重试；有蜂窝可付款；打开／关闭付款页不无故中断传输；恢复后正确更新 | 待平台接入、未验证 |
| B13 | Xcode `.storekit`／StoreKit Testing、苹果沙盒；若需要权限注入，只在 Debug 依赖入口提供，不改正式交易 | Release 无隐藏本地激活／清授权入口；模拟免费不声称已取消年费；本地、沙盒、正式环境不混用 | 旧入口不迁移；测试配置待实现 |
| B14 | App Store Connect 配置苹果 offer codes；兑换走苹果链接或适用系统版本的 `AppStore.presentOfferCodeRedeemSheet(in:)`，权益由交易监听交付 | 安卓码无效；不自行改天数／改永久；非消耗商品优惠码需考虑 iOS 16.3 起的支持，最低 iOS 16 不盲用；延期服务端 API 不纳入客户端 | 优惠码能力已确认、待实现；活动配置待定 |
| B15 | 处理 `Transaction.updates`／刷新结果中的真实权益撤销，重算派生 `isPro`；无其他有效高级权益时回免费，复用已有门控；退款由苹果处理 | 申请不提前降级；永久退款不抹有效年费；不清免费用量、素材或偏好；已入队水印不变；不以手动清标记假装退款 | 用户已确认不新增独立申请按钮／自有退款流程；待实现、未验证 |
| B16 | 运营使用 App Store Connect 的 Sales and Trends／Payments and Financial Reports；App 仅保留必要交易诊断及本地有效依据 | 无自有授权数据库仍能恢复权益；报表不伪装成设备绑定台账或具名客服 CRM；不在客户端保存管理 API 私钥 | 运营替代已记录；实际配置／核对未做 |
| B17 | 正式版分发／更新使用 App Store；如需 App 内更新提示，单独设计 App Store 跳转及版本查询，不放进购买管理器 | 不调用 APK 下载／安装流程；高级版购买不依赖安卓安装统计；不因拆解 LicenseManager 丢失另有要求的更新功能 | 非内购范围；旧 APK 机制不迁移 |

接口依据：[商品购买结果](https://developer.apple.com/documentation/storekit/product/purchaseresult)、[交易验证](https://developer.apple.com/documentation/storekit/verificationresult)、[未完成交易](https://developer.apple.com/documentation/storekit/transaction/unfinished)、[完成交付](https://developer.apple.com/documentation/storekit/transaction/finish())、[苹果优惠码的系统要求](https://developer.apple.com/documentation/storekit/supporting-offer-codes-in-your-app)、[苹果销售与订阅报表](https://developer.apple.com/help/app-store-connect/measure-app-performance/download-and-view-reports)、[收款及财务报表](https://developer.apple.com/help/app-store-connect/getting-paid/view-payments-and-proceeds)。其余依据见第 1–4 节及配套接入方案。

### 8.4 后续更新规则与本轮记录

- 本台账共 32 项：A01–A15、B01–B17；每项均已记录实现／替代方式和差异。**32/32 表示审查文档覆盖，不表示功能开发完成。** 其中包含移除旧机制、可选项及非内购项，不能直接把 32 当成首版必做开发任务数。
- 主接入方案仍按 8 个里程碑追踪，目前 3/8 为文档调研完成。上述各项均无新增苹果支付实现或运行验证证据。
- 开发时逐项补充：实际文件／关键函数、与安卓差异的确认结果、验证日期、环境（本地 StoreKit／苹果沙盒／TestFlight／正式）、步骤与结果、遗留问题。静态复核、编译通过和真实支付验证分开填写；未跑的场景保留“未验证”。
- 一项同时包含必需处理和可选入口时分别记录，例如 B15 的撤销处理必须接入，退款申请按钮可以另行决定。可选项未决定不得阻止无关功能实施，也不得擅自添加。
- 新发现的遗漏追加稳定编号；已编号项不因拆模块或改文件而重排，方便后续讨论、实现与回归引用。

| 日期 | 跟进内容 | 证据／结果 | 未完成事项 |
|---|---|---|---|
| 2026-09-19 | 安卓高级版完整调用链、后台及相关测试静态审查；核对苹果官方接口 | 第 2–6 节记录 32 项对应关系、替代边界及源码推导风险；测试文件仅阅读 | 未跑安卓／iOS 测试，未查线上配置 |
| 2026-09-19 | 按用户要求明确取消自有激活码、购买账户归属，补逐项实施台账 | 第 1.1、8 节；32/32 项已有方案与验收条件；主里程碑仍为 3/8 | 第 7 节决定待定；商品配置、代码接入、沙盒支付及所有业务回归待实施 |
| 2026-09-19 | 对 32 项做合理性、复杂度与稳妥性复核 | 第 9 节记录逐项判断；修正文档中退出才记监看用量、刷新等待范围和新购事件判定等歧义 | 产品简化建议待讨论；仅文档校验，未实现／运行支付；主里程碑仍为 3/8 |
| 2026-09-19 | 用户明确月均价有必要保留 | P06 改为已确认保留；真实年度总价＋辅助月均价，按年扣费说明不省略；同步主方案 | 价格展示实现与多币种验证待完成；其余 P01–P05、P07 仍待确认 |
| 2026-09-19 | 用户接受苹果优惠码替代自有激活码的兑换用途，询问剩余决定 | B14 改为能力已确认；第 7 节收敛为 D01–D09 产品决定，技术选型由开发处理 | 具体活动规则和其他产品取舍未确认；无代码实施、无支付验证；主里程碑仍 3/8 |
| 2026-09-19 | 用户确认三组体验选择，强调所有已入队任务水印不变，退款后恢复免费 | D07–D09 更新为已确认；相机派生图也固定入队快照，明确覆盖安卓渲染前换水印；退款以苹果撤销后聚合权益为准 | D01–D06 商业决定未确认；未实现、未测试；主里程碑仍 3/8 |
| 2026-09-19 | 用户确认暂定售价、年转永久流程、关闭家庭共享、宽限期，暂不考虑安卓用户迁移；询问付费折扣码 | D01–D06 更新；第 4.1 节核实年度／永久都可配置付费优惠码，仍通过苹果付款；示例折扣不是已定活动 | 价格档位／销售地区／具体活动待落实，未配置后台或验证付款；主里程碑仍 3/8 |

## 9. 合理性复核与精简建议

本节是对第 8 节的进一步约束，不能把提出问题等同于功能存在实测故障。**技术表述修正已同步到主方案与台账；涉及体验或权益变化的建议未擅自定案。**

### 9.1 已收紧的技术方案

1. **权益交付不等待价格和无关详情。** 原“全量刷新完成再发布”只适用于一次权益枚举的一致性，不等于把价格、续订详情、当前权益绑成一个全成功事务。永久已确认就开放永久；新购买已验证且有效就及时交付。年度续订详情失败独立显示未知，不能锁住另一份确定有效权益。宽限期仍按苹果有效状态处理，不能先按旧 `expirationDate` 过滤掉再查询宽限期。
2. **不复制苹果的交易存储和发证系统。** StoreKit 负责提供设备端交易资料及验证结果；本方案用统一内存状态、必要显示缓存和本地免费用量。没有必要额外做自签 token、自己的苹果账户表、手写证书链验签、每周联网续证、全量订单数据库。跨启动缓存若确有实测需要再专门设计，不能把“缓存用于离线”理解为永久信任旧 `isPro`。
3. **一个交易处理器，事件驱动刷新。** 保留本机购买返回、应用级更新监听、主动恢复。启动一次未完成交易扫描是合理的恢复措施，苹果示例也采用这一组合；多余的是每个页面、每次前台切换都新增监听或扫描所有历史。合并同时发生的刷新请求、忽略旧结果；`AppStore.sync()` 只在主动恢复时调用，不自动弹认证。
4. **权益幂等和庆祝去重分开。** 年费／永久不是要累计发放的金币，不需要记录所有订单来保证“只加一次”。权益由当前有效交易归并；同一交易后续撤销仍处理。首次见到某个交易可能是历史恢复或自动续费，不能因此放新购烟花；庆祝必须关联实际用户购买流程。`finish()` 在业务交付后完成，不等待动画或用户取消另一项订阅。
5. **监看每秒记账，不能只等退出。** 第 8 节原描述不够稳妥，已修正为沿用安卓每秒累计＋停止收尾。计时按实际经过时间，离开监看／会话停止后不累计；不能把“端侧录制暂停”当成“停止监看”，也不能因单次视频帧间隔就重置计时。降级后不补扣之前的高级时段；跨日重读新日账本，不保留“无限”倒计时。系统中断后的持久化误差要验证，前后台生命周期仍须按主方案待定项验收，不能承诺退出回调总会执行。
6. **不为串行队列预建复杂额度锁。** 当前 `TransferQueue.startWorkerIfAllowed()` 只启动一个 `run()` worker，按任务等待下载；因此原文件额度采用检查→下载→成功记账即可。保留异步重入保护及唯一完成点；效果图后台并行不多占传输次数。以后若真正引入并行原文件下载，再评估额度预占／释放；现在不建通用事务账本。
7. **共用来源不扩大 UI 刷新范围。** 免费余额、监看秒数、商品加载、购买进度与高级权限分别发布。列表和连接动画只观察其需要的权限值；状态未变不重复发布。业务入口的本地布尔／配置判断值得保留，逐帧、逐像素查权益或联网没有必要。模块名是职责提示，不要求为了表格拆成多个相互转发的管理器。

依据：[启动交易监听及未完成交易](https://developer.apple.com/documentation/storekit/transaction/updates)、[苹果包含一次启动恢复扫描的示例](https://developer.apple.com/documentation/storekit/getting-started-with-in-app-purchases-using-storekit-views)、[主动恢复的使用限制](https://developer.apple.com/documentation/storekit/appstore/sync())、[使用设备端资料主动恢复](https://developer.apple.com/videos/play/wwdc2022/110404/)、[StoreKit 验证结果](https://developer.apple.com/documentation/storekit/verificationresult)。本地依据：`RemoteScreen.kt:446–468`、`LicenseManager.consumeRemoteTime()`、`ios/ZTransfer/Domain/TransferQueue.swift` 的 `startWorkerIfAllowed()`／`run()`。

### 9.2 每项复核结论

| 编号 | 判断 | 保留理由／应避免的多余逻辑 |
|---|---|---|
| A01 | 保留，简化实现 | 25 次是既定商品差异；沿用串行队列与本地成功计数，不建服务器／预占数据库。完成时是否记免费账沿用安卓，不擅自改成入队时冻结身份 |
| A02 | 保留 | 单次整数比较成本很低；不要为门控额外下载文件或读取整张照片，已有文件大小和哨兵足够 |
| A03 | 保留 | 允许多选、逐任务拦截合理；不额外限制免费批量，也不新增解锁后全队列自动重试 |
| A04 | 已确认减打扰 | 剩余 5／1 次提醒，当天去重；纯权益变化不触发。沿用额度限制，见 P01 |
| A05 | 必须补足 | 每秒持久化、实际使用累计、跨日和降级处理缺一不可；无需服务器计时，不按视频帧扣费。退出才保存的表述已修正 |
| A06 | 两层门控合理 | UI 提示与实际起录校验有不同用途；系统权限异步返回后复查不能删。无需录制每帧验权，运行中收尾见 P04 |
| A07 | 保持免费 | 无需购买依赖；不要把全部效果功能统一包进高级权限开关 |
| A08 | 保留，控制检查位置 | 在交互入口、异步导入提交点和生效配置处检查即可；不在每个滑杆像素变化时查 StoreKit，不因到期删已有素材 |
| A09 | 保留 | 生效配置与用户偏好分离能避免降级毁配置，复杂度很小；不要维护一套“降级时覆写、升级时恢复”的备份机制 |
| A10 | 已按用户要求统一水印快照 | 两类任务保留独立生命周期，但都在入队时固定生效水印；渲染前不能因权限变化改旧任务。查重／渲染／重试共用入队快照，见 P05 |
| A11 | 修正事件判定 | 首次处理交易不等于新购；历史恢复不庆祝，明确的新购才庆祝。不要为烟花建长期订单库，也不延迟交付 |
| A12 | 保留 | 只是读取已有权限决定效果，不应产生网络或历史查询；权益异步变化不得在动画中途无条件重启动画 |
| A13 | 保留，简化状态耦合 | 自定义页用于复刻现有介绍即可，不再加自制付款确认层；苹果付款单负责最终确认，介绍不等价格才能打开 |
| A14 | 改为订阅管理语义 | 正常自动续费不需要 30／7 天“快到期”催买；取消续订与扣款问题才有需处理状态，见 P02 |
| A15 | 保留必要售后 | 普通反馈和支付帮助分开，不把 QQ 联系／上传截图作为恢复购买前提；错误分类以用户可采取的行动为准 |
| B01 | 保留 | 两个商品映射同一权限，附加独立年费管理状态即可；不用给两个套餐复制两套业务功能 |
| B02 | 必须平台替换 | 苹果自动续期不使用本地加天数；永久不自动取消年费。不要引入不适用的 Advanced Commerce 只为实现自动取消，见 P03 |
| B03 | 保留真实售价及月均价 | 用户已确认月均价保留；商品价与权益独立，年度总价及按年扣费说明明确，划线价不能虚构；换算规则见 P06 |
| B04 | 保留晚到交易处理 | 真实交易可以来自待批准或其他设备；不应删这个边界，也无需复刻安卓多订单管理 UI。待批准不是持续转圈或整 App 锁定 |
| B05 | 删除旧支付机制 | 苹果付款完整替代二维码，不留生成图片、保存支付码、查单轮询等死分支 |
| B06 | 保留恢复保障，减少重复 | 一个应用级监听、一个处理器、一次启动恢复扫描足够作为基础；不是每页一套补单，不扫描全量历史当日常授权 |
| B07 | 保留恢复入口 | 自动恢复与用户主动“恢复购买”各有用途；删除激活码后也不能把主动恢复一起删掉 |
| B08 | 删除设备绑定 | 同购买账户恢复即可；不要增加手机号／邮箱登录、设备数限制或假装能读取 Apple 账户身份 |
| B09 | 删除安卓风控 | 激活次数／小米豁免无对应需求；免费额度跨设备防绕过也不在本次范围，不为它引入设备指纹 |
| B10 | 替换，不做双份授权 | 使用 StoreKit 已验证交易；不同时保留安卓续签服务器和苹果权益作为两个可能互相否定的来源 |
| B11 | 保留状态重算 | 到期和撤销确实需要处理，但不删除苹果历史或用户偏好，也不把每次联网失败视为撤销 |
| B12 | 保留相机任务 | 直接尝试苹果请求并处理结果，不先探测安卓授权服务器、不仅凭 Wi-Fi 类型阻止付款、不自动断相机 |
| B13 | 测试隔离合理 | 使用 StoreKit 测试和 Debug 注入；不为测试绕过正式授权，不将“清缓存”包装成取消订阅 |
| B14 | 优惠码已确认支持 | 不再建议把优惠码能力整体延后；活动配置可后定。补偿延期服务端和后台任意改期限仍不纳入本轮 |
| B15 | 复用免费规则 | 苹果退款后的真实权益变化驱动 isPro，不建自有退款流程或独立申请按钮；仍需正确处理撤销、多份权益及任务收尾 |
| B16 | 使用现成运营后台 | App Store Connect 报表足够作为首版运营起点；没有具名客服 CRM 需求就不新建系统 |
| B17 | 与内购解耦 | App Store 更新与安装统计不属于授权逻辑；不要为搬完整个安卓管理器而扩充本轮范围 |

### 9.3 产品取舍与讨论状态

P01、P02、P03、P06 已确认；P04 安全停止／保存原则已确认；P05 已按用户要求改成所有已入队水印不变。P07 中优惠码支持免费与付费折扣，退款按 B15 处理，家庭共享不开；具体活动及价格配置见 D01–D06。

| 提案 | 当前问题／取舍 | 推荐方案 | 代价或边界 |
|---|---|---|---|
| P01：减少额度提醒（已确认） | A04 的 5 次连续提示可能打扰；降级后的余额变化也会触发 | 只在成功传输后首次降到 5、1 时提醒，用尽时按原有拦截提示；当天阈值提示去重 | 与安卓当前 1～5 每次变化提醒不同；减少的是提醒，不改变 25 次额度 |
| P02：去掉正常续费的临期催买（已确认） | A14 的 30／7 天提示针对安卓手动续费，套用会让苹果用户误以为要重复购买 | 正常订阅只在权益页显示下次续费日期；关闭续订显示有效至，扣款异常显示管理入口；不为正常订阅弹临期催买 | 不额外添加用户尚未要求的已取消用户临期弹窗 |
| P03：年费转永久用一处长期管理提醒（已确认） | 既要防重复扣费，又不能替用户取消；每次打开 App 弹管理页会妨碍使用 | 购买前说明一次，永久交付后提供管理入口；仍续订时在权益页保留状态，不反复强弹。晚到年费同样更新；仅引导本人可管理的订阅 | 不能保证用户最终取消，不能承诺自动退款；已付款永久权益不以取消年费为条件 |
| P04：运行中录制失去权益（安全收尾原则已确认） | 立即强停可能损失当前拍摄，任由无限延续又偏离免费监看限制 | 已开始的本次录制允许安全停止／保存，不逐帧判权；确认失权后不新起录。若仍在监看则转入当天免费余额，归零退出前先完成录制收尾 | 不是“录制到任意时刻都豁免”；暂停／继续与保存失败的技术收尾按不丢已有内容的原则实现和验证 |
| P05：所有已入队任务水印不变（用户明确确认） | 安卓相机派生图在渲染前重新取权限可能让旧队列水印中途变化，用户明确不接受 | 两类任务均在入队时计算并固定生效水印；相机后续派生子任务继承，不重新门控。查重／渲染／原任务重试使用同一快照，后续权限或设置变化只影响新任务 | 明确覆盖安卓相机派生图旧规则；冻结生效水印及素材，不冻结全局 isPro，不改变传输额度／大小检查 |
| P06：保留月均价（用户已确认） | 月均价对套餐比较有用，用户明确要求保留；需要避免误解为每月扣款 | 年度总价＋“约合每月”同时展示，明确每年扣费一次。使用年度 `Product.price` 的 Decimal 值除以 12，再用该商品 `priceFormatStyle` 按币种本地化／舍入；不解析 `displayPrice` 字符串计算 | 月均显示为约数，舍入后乘 12 不必严格等于年价，实际付款以年度总价为准；商品未返回不造月均价；永久版不虚构月均；老客实际续费金额不按新客价推断 |
| P07：控制其他运营扩展范围 | 用户已接受免费／付费苹果优惠码；退款由苹果处理，App 复用免费规则；家庭共享不开、暂不考虑安卓迁移 | 支持优惠码，不做自有验码页；活动后定。不新增独立退款申请按钮或自有退款系统，但必须响应真实撤销；保留官方售后渠道 | 不把不做申请按钮误解为不允许用户向苹果申请退款；不引入补偿延期服务端 |

本轮逐项复核 32/32，文档中的技术修正已落地；P06 已确认保留月均价，P07 中优惠码能力已确认，其余建议以第 7 节决策表继续跟进，不能擅改安卓基线。主实施里程碑仍为 3/8。仅核对源码、苹果官方资料与文档一致性，未构建、未运行测试，未修改业务代码。
