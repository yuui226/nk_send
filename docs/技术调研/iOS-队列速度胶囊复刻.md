# iOS 队列速度胶囊复刻

## 安卓行为依据

- 队列入口图标、计数/速度、生成中、Done 使用同一个右侧承载控件，右缘固定，只向左伸缩。
- 宽度只有一条 `Motion.bouncy()` 弹簧；内容状态切换不再参与尺寸动画，只做淡入、淡出和右缘缩放。
- 速度使用 12 pt 粗体，数量、生成中和 Done 使用 14 pt 粗体；数字启用等宽数字特性。
- 速度与数量间距 8 pt，生成中与数量间距 6 pt，展开态左右各留 16 pt；入口态宽 40 pt、高 36 pt。
- 数量减少时旧数字向上离开、新数字从下方进入；增加时反向。完成态保留 1.8 秒，再弹性收回入口图标。
- 入队残影抵达后，整个右上控件先在 110 ms 内放大到 1.18，再弹簧回落。

## iOS 实现

- 移除外层 `ZTransferGlassButtonStyle` 与内部胶囊的双重背景，入口和展开态改为一个持续存在的胶囊表面。
- 取消 `fixedSize` 和顶栏整组隐式布局动画，按当前文字度量得到唯一目标宽度，并用右缘锚定弹簧插值。
- 计数实现独立的双层里程表动画；相同位数的速度使用等宽数字，不因实时采样产生宽度抖动。
- 完成瞬间直接从当前内容过渡到 Done，不经过一帧入口图标；进入 Done 后立即移除进度层，只保留完成文字与胶囊材质，避免满格进度在 Done 下方再次播放。
- 列表和预览入队抵达都触发同一套右上控件接住回弹。
- 接住回弹使用安卓 `Motion.bouncy()` 的等效物理参数：stiffness 200、damping ratio 0.5；
  先用 110 ms 放大至 1.18 倍再回落。
- 开始/暂停图案分别按安卓的 21 pt / 18 pt 占比；队列自绘图标使用 20 pt 画布，
  其可见轮廓与安卓 22 dp Material Checklist 图标一致。
- 任务身份切换时，进度填充先用 220 ms 平滑回落到 0，再继续追踪下一任务进度，
  避免继承上一任务满格，也避免瞬间清空。
- iOS 26+ 选择“液态玻璃”主题时，入口、速度/数量、生成中、Done 与开始/暂停整组统一改用系统 `glassEffect(.regular.interactive())`。原有右缘锚定、内容裁切和进度填充保持不变；原生按压反馈启用时关闭队列自定义按压缩放，避免双重形变。

## 验证记录

- 2026-09-18 Done 进度层隔离与连拍合集纯缩略图修正一起通过 Debug 构建，并覆盖安装、启动到 iPhone 17 Pro 模拟器。
- 2026-09-18 Liquid Glass 整组适配通过 Debug 模拟器构建；其他主题分支仍编译保留固定状态材质。
- 2026-09-18 Debug 模拟器构建 `BUILD SUCCEEDED`。
- 已覆盖安装并启动到 iPhone 17 Pro 模拟器 `CA046456-B859-45F4-9CB3-2C6E2F8E03B0`。
- 已截图确认静止入口态右缘、40×36 pt 尺寸、单层玻璃表面和图标居中正常。
- 已在调试相册整组入队并启动连续任务：等待态显示开始按钮，传输且后方仍有等待任务时
  显示暂停按钮，全部完成后暂停按钮消失并进入 Done。
- 调试传输已覆盖多任务切换；最终主观手感仍以用户在设备上的连续观察为准。
