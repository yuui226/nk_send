# Z传 iOS 原生复刻计划与任务清单

## 目标与边界

本计划对应 `ios` 分支。iOS 使用独立的 Swift/Xcode 工程，代码放在仓库根目录的 `ios/`，不引入 Kotlin、KMP 或 `shared`。安卓 `app/` 作为行为对照，不参与 iOS 编译；不为 iOS 复刻改动安卓结构，也不自动删除安卓参考代码。

安卓端已经迭代验证过的连接判断、协议顺序、状态流转、错误处理、取消、重试、持久化和文案含义，是 iOS 的功能基准。iOS 可以使用 SwiftUI、UIKit、Core Image、Metal 等原生实现来获得自然的页面和动画体验，但不能因为换了 UI 技术而改变业务结果。

## 强制复刻原则

本节与 [项目记忆](../项目记忆-iOS复刻规则.md) 同为后续开发、审查和验收的长期约束，由仓库 `AGENTS.md` 引用。每次开始或恢复任务时先阅读，子 agent 执行同一规则。

1. **安卓行为是事实来源。** 所有 UI、动画、点击/长按/拖动操作、判断条件、状态流转、协议时序、错误、取消、重试、持久化和文案含义，都必须先从安卓代码和测试中确认，再在 iOS 中实现。不得凭经验补功能或修改语义。
2. **不添加用户没有要求的内容。** 不增加多余文本、页面、按钮、入口、跳转、提示或源码/项目地址展示；安卓没有的能力，iOS 不自行创造。
3. **平台实现可以不同，结果必须一致。** SwiftUI、UIKit、Core Image、Metal 和 Swift Concurrency 可以按 iOS 习惯选用，但只能改变实现方式，不能改变用户可观察的结果。确实无法保持一致时，先讨论并在任务记录中写明差异。
4. **公共组件优先且保持可维护。** 字体、颜色、间距、按钮、拨轮、弹窗、状态模型、动画曲线和错误映射应集中封装；组件有清晰职责，避免复制粘贴、临时占位和无法测试的巨型文件。
5. **每项功能必须闭环。** 入口、正常路径、边界、错误、取消、重试、恢复、持久化、动画和测试全部完成后，才能在清单中标记完成。
6. **文本与可见行为逐项对照。** 所有文本逐字一致，包含标点、空格、大小写和版本号格式；页面元素位置、显隐条件、按钮功能、手势、动画触发/中断/恢复均须对照安卓。截图辅助核对，不能代替阅读实现。不把原生系统默认交互直接当作复刻完成。
7. **验证记录如实填写。** 编译成功或接口存在不代表功能完成；未执行的测试不写通过。发现过去的完成状态不准确时立即更正，保留问题和后续工作记录。
8. **动画参数与时序也是复刻内容。** 时长、延迟、曲线/弹簧参数、位移、缩放、透明度、交错顺序和手势联动逐项依据安卓实现；连续触发、中断、取消、恢复和退出清理一并核对。不能用系统默认效果替代，优化性能也不能擅改行为。
9. **复用不改变状态归属。** 复用组件、模型类型、渲染器和公共逻辑；安卓独立的配置、草稿、持久化作用域及任务生命周期继续独立。工作台与相机传输的照片效果配置分别保存，不能因使用相同控件而合并。不为 iOS 复用改动安卓结构。

第一条实际连接链路选择 USB 有线连接。先把“插入相机—授权—建立 PTP 会话—读取文件—预览—下载”跑通，再扩展 Wi‑Fi STA、AP 和 BLE/GPS。USB 能力必须以真实 iPhone、转接设备和 Nikon 机身验证结果为准，不能只按模拟器结果判断。

## 进度规则

- 总任务数：62
- 当前进度：**2/62**（完成工程基础 2 项；其余 60 项尚未完成验收）
- 每完成一个可验收任务，将勾选对应项目、补充实现提交和验证证据，并更新本节进度。
- “完成”必须包含入口、正常路径、错误/取消路径、状态恢复和对应测试；只有写出界面不算完成。
- 未经确认不进行真机打包；代码阶段优先使用单元测试、模拟数据和 Xcode 预览，最后集中真机回归。

## 待处理问题（2026-09-13 用户真机反馈）

以下问题由用户在当前工作台版本中直接操作发现，全部保持未完成状态；在逐项修复并完成真机回归前，不得把相关任务标记为完成，也不得以“已能编译”代替验收。

1. **滤镜预览未生效**：工作台选择滤镜后，预览图没有展示对应滤镜效果；需要沿安卓的滤镜目录、参数和预览渲染路径逐项核对，不能只显示选项名称。
2. **边框效果不是安卓实现**：当前 13 个边框样式为臆想实现，未按安卓端完整版式、颜色、间距、元数据布局和水印锚点复刻；必须以安卓 `PhotoFrameExporter.kt` 及相关预览代码为唯一依据重做。
3. **滤镜收藏误触拨轮**：点击滤镜收藏按钮会改变当前选项，并可能把错误的滤镜加入收藏；需要隔离按钮与拨轮的命中区域，并验证收藏前后当前滤镜和强度保持不变。
4. **边框收藏状态错误**：边框无法正确加入收藏，取消收藏也不能稳定生效；需要按安卓完整收藏对象（边框预设 + 水印配置）进行单次原子更新，并验证重新选择、取消、重启后的持久化。
5. **水印控制全部失效**：水印无法开启或关闭，水印设置入口及展开后的各项按钮点击后没有效果；需要核对开关、展开状态、内容类型、文本/图片和各拨轮的绑定及状态写回。
6. **批量生成速度过慢**：工作台生成效果图的速度明显低于安卓；需要对照安卓批处理管线检查图片解码、滤镜/边框/水印渲染、并发限制、缓存和写入流程，不能只通过降低质量掩盖性能问题。

### 2026-09-13 本轮修复记录

- 已修复上述 1、3、4、5、6 的代码路径：预览设置变化会清空旧图并重新渲染；收藏按钮与拨轮隔离且采用原子快照；水印控件统一通过原子绑定写回；滤镜像素处理改为受控并行，避免批量任务 CPU 争抢；无边框水印沿安卓 watermark-only 路径保留原照片尺寸。
- 已重做上述第 2 项的渲染管线：13 个边框均有独立布局/装饰分支，品牌与型号按安卓规则规范化，颜色档案从照片采样调色板，水印尺寸和锚点按安卓分段比例计算。
- 验证：Swift 全量语法解析通过；Xcode iOS Debug generic build 成功；`dist-debug-ios/build.command` 已签名、安装并启动真机（iPhone 14，iOS 16.0.3）。

## 安卓对照资料

实现每项功能前先阅读对应安卓入口和状态来源，记录到提交说明中：

- 连接与 PTP：`app/src/main/java/com/ztransfer/protocol/`、`CameraViewModel.kt`、`TransferViewModel.kt`
- 页面与交互：`HomeScreen.kt`、`FileListScreen.kt`、`PhotoPreview.kt`、`TransferScreen.kt`、`RemoteScreen.kt`、`SettingsScreen.kt`
- 照片效果：`PhotoEffectsBatch.kt`、`LocalPhotoEffectsOverlay.kt`、`LocalPhotoPreviewPager.kt`
- GPS：`gps/` 下的状态、BLE 客户端和协议文件
- 验收清单：`docs/测试与验证/`、`docs/技术调研/安卓非shared特性回放手册.md`

视觉参考来自 `/Users/orys/code/z传截图/`。当前文件包括 11 张 1272×2800 的界面截图和 1 张 Nikon Z 30 TIFF 样本。复刻时逐张登记页面、状态和对应任务，不把微信文件名当作页面语义。

## 任务清单

### A. 工程与基础设施

- [x] **1. Xcode 工程骨架**：建立 `ios/ZTransfer.xcodeproj`、App target、Tests target、资源目录和 scheme。
  - 实现：`ios/project.yml`、`ios/ZTransfer.xcodeproj`、`ios/ZTransfer/App/`、`ios/ZTransferTests/`
  - 行为依据：安卓 `MainActivity.kt` 的单入口页面结构；连接状态先独立于 UI 建模。
  - 动画/交互：暂未进入页面交互实现；后续按对应任务补齐。
  - 验证：`xcodebuild -project ios/ZTransfer.xcodeproj -list` 已确认两个 target 和 `ZTransfer` scheme 存在；模拟器 Debug build 成功。
- [x] **2. Swift 并发与最低系统版本**：部署 iOS 16，启用 Swift 6；工程支持 Swift Concurrency。各链路的取消、超时和会话清理由任务 6–13 单独验收。
  - 实现：`ios/project.yml`、`ImageCaptureUSBTransport.swift`
  - 行为依据：安卓连接任务的可取消生命周期；未引入后台线程阻塞 UI 的同步 I/O。
  - 验证：模拟器 Debug build 成功。
- [ ] **3. iOS DesignSystem**：统一颜色、字号、间距、圆角、阴影、触控热区、图标和动画曲线，建立浅色/深色主题。
  - 已修：`ZTransferDesign` 的全局字号改用 Android Material 对应的 system sans 语义；连接强调色改用 Android `Color.kt` 的浅色/深色 token。
  - 待完成：所有页面迁移到同一套 token、皮肤/主题持久化、图标与动画曲线逐项对照；当前只算基础 token 修正，不能标记完成。
- [ ] **4. 行为状态图**：把连接、文件扫描、预览、下载、队列、监看、GPS 的状态、事件、错误和取消写成可测试模型。

### B. USB 有线连接与 PTP

> 2026-09-13 审计：以下项目原先只有部分接口/编译记录，却被标记完成；已全部退回进行中。纯协议单测成功也不代表系统传输适配和生命周期已通过验收。

- [ ] **5. USB 设备发现**：通过 ImageCaptureCore 仅浏览本地相机，处理新增、移除和重复 UUID。
  - 已有：`ImageCaptureUSBTransport` 的浏览器和事件模型；发现流程移至 `RootView` 持有，避免连接页消失时清空设备。连接页卡片使用与安卓相同几何的自绘三叉 USB 图标。
  - 依据：安卓 `CameraViewModel` 的设备枚举/拔出流程。
  - 已修：设备映射、事件流和系统回调访问统一经过锁；保存已打开的 `ICCameraDevice` 对象，避免相同 UUID 重连时旧关闭回调关掉新会话；停止/拔出会结束挂起的缩略图请求。
  - 待完成：重复事件的去重策略、事件流生命周期的完整测试；真实插拔待任务 60。
- [ ] **6. 授权与会话生命周期**：未决定/拒绝/受限/已授权，打开/关闭会话、取消连接、设备替换和断线事件。
  - 已有：ImageCaptureCore 授权、会话接口。
  - 已修：`CameraConnectionService` 改为单一连接任务，使用不会等待不合作子任务的 deadline；调用方取消会取消共享握手，失败/取消后在独立收尾任务中限时关闭旧会话。
  - 已修：关闭会话按已打开对象身份匹配，不会因相同 UUID 的新设备替换而误关新会话；拔出/停止会清理等待者。
  - 已修：打开、读取和下载回调统一使用一次性 continuation box；调用者取消立即结束等待，迟到的 ImageCaptureCore 回调不会二次恢复或污染新会话；下载取消同时取消 NSProgress。
  - 待完成：关闭回调错误的安卓文案映射、系统请求的物理取消能力和真实系统回调验证。
- [ ] **7. PTP 数据层**：命令、响应、事务 ID、端序、超时、错误映射和分包读取。
  - 已修：`PTPCodec` 的非对齐读取、带非零起始索引的 Data、32 位容器长度、最多五个命令参数；`AsyncDeadline` 在系统回调不返回时也能结束调用方等待。
  - 依据：`UsbPtpConnection.sendCommand/receiveResponseWithPayload/checkTransaction`，PTP 完整事务被消费后才能放行后续操作。
  - 验证：独立协议测试已运行；正常响应、损坏长度/类型、事务不匹配、超时、取消和迟到响应已覆盖。
  - 待完成：系统适配层回调响应/载荷的真实设备样本、权限/断线/超时错误映射、关闭旧通道后再连接；不能把结束等待当作物理 I/O 已取消。
- [ ] **8. PTP 事件串行队列**：每会话的事务号、命令排队和事件分发；缩略图/下载/预览不能争用命令通道。
  - 已修：`PTPSession` 使用显式等待队列，跨 `await` 保留事务所有权；排队取消不发命令、不消耗事务号；活动请求超时/取消使本会话失效，拒绝后续请求；完整的 busy/unsupported 响应不使通道失效。
  - 验证：40 个并发调用时最大活动 I/O 数为 1；另外覆盖排队取消、活动取消、超时、迟到响应、busy、不支持、错误事务号/容器类型。
  - 待完成：ImageCaptureCore 媒体请求与 PTP 请求的统一仲裁，以及 PTP 事件接入、断线清理。此前两个顺序调用的测试不能证明串行，已替换。
- [ ] **9. 相机信息与能力读取**：读取 DeviceInfo 字段与安卓能力集合。
  - 已修：补回 FunctionalMode 和五组数组，保留 vendor description、operations、events、properties、capture/image formats，之后读取制造商、型号、版本和序列号。
  - 依据：`RemoteLab.parseDeviceInfo`；旧解析缺 FunctionalMode 和 ImageFormats，旧测试样本也缺 FunctionalMode，均已修正。
  - 验证：非空五组数组样本、所有截断前缀、非对齐和非零起始索引 Data 测试通过。
  - 待完成：连接层能力初始化、真实响应样本和异常重试，不只验收解析函数。
- [ ] **10. 文件对象与元数据解析**：ObjectHandles、ObjectInfo、文件名、格式、大小、拍摄时间、保护状态和目录过滤。
  - 已修：ObjectInfo 文件名从固定偏移 52 读取；32 位无符号大小保留 `0xFFFFFFFF`；目录成功但不形成照片；扩展名从实际文件名获取，缺文件名时按安卓格式表和 handle 低 16 位生成兜底名称。
  - 已修：`PTPObjectInfoResult` 区分完整身份与不完整载荷；缺失/截断/无终止符日期保留 UI 名称但不能授权缓存清理；显式零长度日期合法。
  - 依据：`NikonCamera.getObjectInfoInternal/parseObjectCacheIdentity/FileInfo.extension`、`PtpConstants.FORMAT_EXT`、`ObjectInfoCacheIdentityTest`。
  - 验证：已移植安卓缓存身份测试场景，补齐固定偏移、未知大小、目录、扩展名和数组边界测试。
  - 已修：`CameraRepository.loadCatalog()` 对多卡逐卡请求 ObjectHandles 并去重，保留每个对象的实际 StorageID，单卡/无卡列表仍分别走对应查询。
  - 待完成：扫描层传播完整性/失败状态，取消不能被 `try?` 吞掉，缓存身份/增量更新待对照实现。
- [ ] **11. 缩略图和原图读取**：请求合并、取消、回退、缓存及预览首帧。
  - 已有：ImageCaptureCore 缩略图、文件前缀读取接口，`ThumbnailCache` 有容量限制。
  - 已修：缓存键包含设备会话身份；取消与等待者登记在同一锁内，取消先发生时不发起请求；下载 Progress 和进度轮询会随任务取消。
  - 已修：打开/读取/下载的迟到回调不会二次恢复；停止或拔出会结束挂起缩略图等待者。
  - 已修：网络会话可通过同一 PTP 仓库请求缩略图和前缀读取，避免把无线会话误接到 USB 回调；预览按 FHD→LargeThumb→标准缩略图顺序回退。
  - 待修：按设备维度结束等待者、系统错误码映射、FHD/RAW/视频缩略图的安卓回退路径和真实样本。
- [ ] **12. USB 下载保存**：授权目录、命名、已存在文件判断、进度、取消、部分文件清理和真实大文件。
  - 已有：ImageCaptureCore 下载接口和进度回调。
  - 已修：取消先于 Progress 登记时会记录取消并在 Progress 出现后立即取消；独立进度任务可被回收。
  - 已修：下载 continuation 支持立即取消，迟到完成回调被忽略；Progress 登记与取消竞态已处理。
  - 已修：PTP/IP 会话使用分段读取写入临时文件，完成后原子移动，取消/断线会清理半成品；未知大小先按安卓专有 GetObjectSize 取 64 位长度。
  - 待修：返回文件名/路径处理、断线错误映射和真实大文件；需对照安卓按名称及大小判断已有文件，不能见同名就跳过。
- [ ] **13. 断线/重连恢复**：下载取消、队列保留、会话重建、列表刷新和用户可见错误保持一致。
  - 待完成：旧连接结果不能覆盖新设备/新会话；按安卓保留断线列表和队列；关闭失败、回调迟到和重试限制的完整状态测试。

### C. 连接页

- [ ] **14. 连接页结构**：复刻 Z 标、USB/Wi‑Fi 卡片、GPS 入口、设置入口和底部工作台入口。
  - 2026-09-13 真机现状：iPhone 14 / iOS 16.0.3 已成功安装；开发者信任及 USB 重新连接后，脱离调试器启动成功，`ios-deploy --get_pid` 返回 1343。截图显示上下黑边，内容区呈旧 320×480 比例，Wi‑Fi 标题溢出、切换条与图标重叠、底部入口过宽。安装包 Info.plist 缺少启动屏与场景配置。
  - 本轮修正：按本机 Xcode SwiftUI App 模板补齐生成启动屏/场景清单；抽出 `ConnectionPage`/`ConnectionMethodCard`，按 `HomeScreen.kt` 使用 56 顶栏、真机测得 310/250 双卡、12 卡间距、10 GPS 间距及 `(可用高度−56−310−62)×0.28/1.28×0.94` 的顶部比例留白；工作台使用自然宽度与底部 18 留白。卡内改为 42 图标底座、21 步骤圆点、30 模式条、34 工具入口、42 连接按钮，使用 `Type.kt` 的字体/行高常量；双 Z 按 `ZMark.kt` 重绘全尺寸路径与分隔缝。
  - 验证边界：新的布局仅通过 iOS 16 / Swift 6 类型检查，尚未重新构建或真机截图；底部工具入口的动作、AP/STA 完整反馈、设置锚点浮层和其它皮肤仍未完成。GPS 仅搬出原连接页内嵌视图并调整入口字号/圆角，没有改动 GPS 协议及状态流转。
  - 已修：根页面持有连接与 GPS 状态；GPS 入口改为连接页内可直接切换的胶囊按钮，不再额外弹出一套独立入口。
  - 已修：GPS 启用期间连接卡片按安卓语义降级显示并禁止发起 USB/Wi‑Fi 连接，避免无线和定位同时争用系统链路。
  - 待完成：Z 标/按钮几何、工作台位置和安卓截图逐项测量验收。
- [ ] **15. USB 卡片状态动画**：不可用、等待插线、发现设备、连接中、已连接、失败和断开状态。
  - 本轮修正：呼吸保留安卓 2.4 秒、0.38/0.82 分段五次平滑曲线和 Wi‑Fi 半周期错位；将逐帧更新移到捕获卡片内容的变换修饰器，后台或禁用时暂停时钟。完整连接成功图标转场及真机帧率仍待验证。
- [ ] **16. Wi‑Fi/AP/STA 状态占位与后续接入**：先保证文案和不可用态正确，再接入对应链路，不提前臆造成功状态。
  - 已修：Wi‑Fi 卡片保留安卓布局和文案，但未接入链路时连接按钮置为不可用，避免出现点击无响应的假成功入口。
  - 已修：新增 `PTPIPCodec` 和 `PTPIPDiscoveryPolicy`，复刻安卓 PTP/IP 包类型、命令封装、局域网 /24 扫描范围和本机地址排除策略。
  - 已修：新增 `PTPIPSocketTransport`，建立命令/事件双 TCP 通道、初始化握手、分包读取、数据阶段和 PING/PONG；`PTPIPDiscoveryService` 按当前 Wi‑Fi 接口做可取消的 PTP 端口探测，AP/STA 连接入口已接到状态机。
  - 已修：`ConnectionViewModel.startWiFiDiscovery()` 使用 `NWPathMonitor` 监听 Wi‑Fi，AP 模式在 iOS 可识别的 192.168.1.0/24 相机热点候选网上按安卓 1 秒看护、失败后 3 秒退避持续握手；离开 Wi‑Fi 时仅取消发现中的连接并回到空闲，已建立会话交由传输/心跳处理。`PTPIPDiscoveryPolicy.isCameraHotspotAddress` 有单测覆盖，实际身份仍以 PTP/IP 握手为准。
  - 待完成：STA/AP 的安卓配对顺序、按钮动作、网络变化恢复和真机错误样本；iOS 没有公开 DHCP gateway API，当前子网门控是平台差异记录，不能勾选。
- [ ] **17. 连接结果流转**：成功进入照片列表，失败停留连接页，重复点击、拔线和返回行为与安卓一致。
- [ ] **18. 连接页弹窗与提示**：权限、失败、帮助和设备信息弹窗的样式、锚点、遮罩、外部点击和关闭动画。
  - 已修：工程补充本地网络权限用途文案，PTP/IP 连接前走系统 Wi‑Fi 网络接口。
- [ ] **19. 连接页真机验收**：冷启动、插入、拔出、拒绝授权、重新授权和多次进入退出。

### D. 照片列表与大图预览

- [ ] **20. 日期分组和网格**：分组标题、折叠展开、空态、加载态、保护/连拍标记和布局自适应。
  - 已修：照片筛选保留原始文件集；新增安卓对应的连拍筛选条件；连拍折叠开关与合集展开状态已接入列表草稿；移除安卓网格没有的文件名额外文本。
  - 已修：日期分组增加可折叠标题与数字计数，网格列数读取设置；缩略图补回扩展名与保护角标，避免显示安卓没有的文件名文本。
  - 待完成：传输状态标记、空态/加载态和安卓网格测量与动画对照。
- [ ] **21. 列表滚动体验**：缩略图复用、下拉刷新、回到顶部、滚动中加载策略和动画性能。
  - 已有：列表扫描暴露可取消 `reload()`，接入 SwiftUI 下拉刷新，取消不会替换现有目录。
  - 已修：增加单次 `ScrollViewReader.scrollTo` 回到顶部按钮，按钮显隐使用滚动偏移淡入/缩放过渡，避免分段跳顶。
  - 待完成：缩略图复用/预取、滚动中加载策略和安卓动画性能对照。
- [ ] **22. 筛选浮层**：类型、保护、连拍、未传输、卡槽、日期和清除筛选，保留选择状态和关闭行为。
  - 已修：筛选数据源改用未过滤的完整目录；补齐“连拍”筛选状态和选择保留；筛选表单改为安卓同结构的类型/状态/卡槽/日期分组按钮，不再使用原生 Form 的额外行和默认控件。
  - 已修：队列完成状态会回流到列表筛选的“未传”判定，筛选变化即时重算。
  - 待完成：浮层锚点/动画、磁盘已有文件索引、日期拨轮与安卓文案逐项对照。
  - 已验证：新增连拍筛选后，纯领域规则测试覆盖“3 张连续连拍”和存储卡过滤，语法检查通过；协议、PTP/IP、GPS、监看状态/帧解析、曝光参数/显示参数与对焦状态回归共 39 tests、0 failures。
- [ ] **23. 连拍合集**：合集封面、展开/收起、成员关系、批量操作和对应过渡动画。
  - 已修：按安卓的连续编号/时间规则生成合集；列表支持点击封面展开，关闭“连拍成组”会清理展开状态。
  - 待完成：合集角标、成员批量传输、预览快照、展开级联动画和回收位置对照。
- [ ] **24. 单选与长按入口**：点击预览、长按预览、长按分类帮助、触控冲突和无障碍操作。
  - 已有：点击/长按根据“照片列表操作”设置切换预览与传输；连拍封面点击优先展开。
  - 待完成：安卓长按阈值、触感、预览起点、无障碍标签和快速重复点击边界。
- [ ] **25. 大图预览容器**：从缩略图位置展开、沉浸式黑底、缩放、双击、拖动、旋转和返回原位置。
  - 已修：iOS 预览使用黑底分页容器，支持缩放、双击、放大后拖动、旋转和异步高清预览回退。
  - 待完成：从缩略图位置展开/收起的锚点动画、旋转持久化和安卓手势冲突规则。
- [ ] **26. 预览信息与翻页**：文件名、曝光参数、保护/连拍标记、传输状态、加载占位和翻页淡入淡出。
  - 已修：预览移除无意义的“第几张/总数”文本；文件名翻页时淡入更新，曝光信息沿用同一字号常量；新增基于当前缩略图的直方图数据加载入口。
  - 待完成：高清原图/FHD 回退、保护/连拍/传输标记和安卓翻页时序对照。
- [ ] **27. 预览入队飞行动画**：单张/合集入队、队列胶囊回弹、取消、快速重复点击和页面退出清理。

### E. 传输队列

- [ ] **28. 队列模型**：等待、传输、暂停、完成、失败、取消、跳过和重复任务去重。
  - 已修：加入等待任务撤下、终态清理、批量重试；活动任务不被清理/取消，重试创建新的任务 ID。
  - 已修：队列项目写入 UserDefaults；应用重启时将未完成的 transferring 项恢复为 waiting，保留完成/失败/取消记录。
  - 待完成：暂停/断线恢复、效果配置快照和真实下载恢复测试。
- [ ] **29. 顶部队列胶囊**：图标态、数量态、速度态、完成态、宽度变化、安装/回弹动画和点击入口。
- [ ] **30. 队列页面**：任务分组、缩略图、文件名、进度、错误信息、重试和清理。
- [ ] **31. 暂停与继续**：暂停当前文件/暂停后续、继续、断线恢复和按钮状态动画。
- [ ] **32. 取消与异常**：单任务/批量取消、失败重试、已存在文件、目录不可写和清理未完成输出。
  - 已修：队列页加入安卓对应文案的清空/重试确认入口；清空先撤下等待项，短动画后移除终态项；不打断正在传输项。
  - 已修：队列页改为复用照片页同一个 `DirectoryAccessStore`，设置中的目录选择会立即影响队列开始传输；根页面再统一注入设置页与照片页，跨页面目录状态不会分裂。
  - 待完成：目录不可写、断线部分文件清理和真实下载回调验证。

### F. 设置页

- [ ] **33. 设置主页面**：按钮顺序、版本号、会员状态、反馈、帮助和返回行为。
  - 已修：照片效果摘要按安卓结构显示“照片滤镜”及名称/强度、以及“边框和水印”下的边框/水印两行；连接页继续通过 `showPhotoEffectsEntry=false` 隐藏该模块。
  - 待完成：高级版徽标/续费、检查更新、换机、主设置说明气泡和目录关注动画。
- [ ] **34. 通用拨轮组件**：`DetentWheel` 集中处理数据源、上下拖动、边界、选中态、统一字号/行高、松手提交、回弹动画和档位触感回调。
  - 实现：`ios/ZTransfer/UI/Components/DetentWheel.swift`
  - 行为依据：安卓 `ReleaseCommitWheel`，拖动预览、松手提交，选项不越界。
  - 已修：组件改为安卓同语义的 50pt 三行拨轮；超过 3 档才接管纵向拖动，拖动只预览位置、松手吸附并提交；短选项点击换档；拖动中标签淡出、档位变化触感回调、外部值变化同步。
  - 本轮重构：统一补齐 `readOnly`、圆角/强调边框、激活与长按回调、收藏星标、中心图标、多行文本、拖动提示和环境高亮参数，所有页面继续使用同一 `DetentWheel`，待真机逐项对照安卓手势细节。
  - 已修：修正 SwiftUI `ZStack` 重复居中偏移导致拨轮文字落到底部的问题；相邻档位只按行高差移动，中心值保持真实居中。
  - 已修：选项列表重建或恢复值失效时回退到第一档，避免沿用旧位置造成默认显示末档。
  - 已修：工作台边框信息拨轮补齐安卓的日期/时间格式选项，水印设置补齐“文字/图片”类型，展开内容按类型显示对应编辑控件。
  - 已修：工作台顶部下滑返回手势增加起始区域限制，不再与边框/水印/滤镜拨轮的纵向拖动冲突。
  - 待完成：长按分类选择弹窗、收藏图标/拖动提示、无障碍描述、边界与真机动画对照。
- [ ] **35. 照片效果设置**：滤镜、强度、边框、水印、曝光信息和保存/取消草稿。
  - 已修：设置页接收根页面持有的相机传输 `PhotoEffectsStore`，设置入口复用同一份传输配置；工作台保持独立配置作用域。设置保存仍通过草稿回调提交。
  - 已修：编辑器拨轮标题使用安卓文案“边框”；补齐内置滤镜名称目录，支持选择滤镜及强度，效果摘要文本与安卓一致。
  - 已修：编辑器改为可控的安卓式卡片、开关和拨轮布局，避免 iOS `Form` 产生安卓没有的系统行和控件。
  - 待完成：安卓完整分类选择、边框水印位置约束、曝光信息细节和设置弹窗动画/渲染真机对照；当前仍不能勾选。
- [ ] **36. 设置弹窗动画**：锚点展开、收起、遮罩、页面切换、外部点击、系统返回和连续开关。
  - 本轮已将连接页设置从系统底部 sheet 改为同层级全屏锚点覆盖层：保留背景、遮罩点击关闭，并按设置按钮位置计算面板顶边；当前仅完成外壳与连接页“隐藏照片效果”入口，尚未完成安卓全部弹窗内容、返回处理和真机动画验收。
- [ ] **37. 传输目录选择**：使用 iOS 目录授权能力，处理授权、重新选择、失效目录和无权限错误。
  - 已修：目录书签保存/恢复改用 iOS 可用的无选项 bookmark API，并在使用期间调用 `startAccessingSecurityScopedResource()`；旧书签失效时刷新书签。
  - 已修：根页面统一持有目录授权对象，设置选择、更改和队列/下载读取使用同一份书签状态。
  - 待完成：选择器失败/目录不可写的用户可见安卓文案、失效授权清理和下载链路联动。
- [ ] **38. 更新与授权**：检查更新、版本展示、购买/激活状态和错误提示，不暴露项目地址或源码入口。

### G. 工作台与照片效果

- [ ] **39. 本地照片入口**：照片权限、目录/相册读取、空态、拒绝权限和重新授权。
  - 已修：工作台持有 `PhotoEffectsStore(scope: .localPhotos)`，与相机传输设置分别持久化，复用编辑控件和渲染实现；空态/多选入口使用安卓文案“选择图片”“（可多选）”，替换入口使用“更换图片”。
  - 本轮结构修正：工作台改为连接页下方的纵向分页，可通过上下滑动切换；连接页底部入口与工作台返回按钮使用同一分页状态，不再以 sheet 弹出。仅完成结构落地，尚未真机验证分页手势、边界和释放生命周期，任务仍未完成。
  - 已修：生成前请求 `PHPhotoLibrary` addOnly 权限，拒绝时进入失败态；工程声明读取与写入相册用途文案。
  - 已修：非空态才显示“更换图片”；工作台说明灯泡包含安卓中文文案；预览长按临时显示未处理原图，松手恢复效果图。
  - 待完成：照片权限拒绝与重新授权的安卓样式提示、原图来源持久化和系统相册读取失败状态。
- [ ] **40. 多选模式**：进入/退出、单选、取消、全选、跨滚动选择、选择数量和底部操作栏。
  - 已修：工作台支持多张 PhotosPicker 选择、缩略图横向选择、选中角标、单项切换和“更换图片”替换整组选项；生成按钮按安卓批处理阶段显示数量文本。
  - 待完成：全选/取消入口、跨滚动选择保持、底部栏的精确动画和真实输出结果。
- [ ] **41. 本地筛选与分类**：分类顺序、收藏、强度独立性、长按帮助和筛选动画。
- [ ] **42. 滤镜/边框/水印编辑**：预览源、渲染顺序、位置约束、旋转、文本编辑和配置快照。
  - 已修：新增独立 `PhotoEffectsRenderer`，预览按“原图→滤镜→边框→水印”顺序渲染；工作台与设置复用渲染实现，各自保留独立配置。
  - 已修：无边框时将边框专属水印位置归一到照片内位置；水印支持上/中/下与左右锚点，避免隐藏边框预设改变实际输出。
  - 待完成：安卓完整滤镜参数/目录、边框版式、旋转/长按前后对比、配置快照与原生字体资源。
- [ ] **43. 批量处理管线**：逐项输出、并发上限、原片只读、命名、重复输出隔离和缓存释放。
  - 已有：工作台按所选照片逐项进入生成状态并复用同一效果配置。
  - 已修：生成阶段逐项调用 `PHPhotoLibrary` 写入效果图，不再用仅渲染内存图冒充“已保存”。
  - 待完成：与安卓“原目录旁保存”的命名/重复隔离语义、并发上限、取消和缓存释放；当前不能视为批处理闭环。
- [ ] **44. 批量进度与取消**：进度、单项失败、重试失败项、取消清理、完成结果和返回工作台动画。
  - 已有：工作台状态模型包含 ready/generating/complete/partial/failed，并按安卓文案展示进度和结果。
  - 待完成：单项错误来源、取消清理、失败重试、真实保存结果和返回动画。
- [ ] **45. 本地大图预览**：分页、缩放、旋转、效果预览、加载占位、手势冲突和退出过渡。

### H. 遥控监看

- [ ] **46. 监看会话**：进入/退出、取帧、帧丢弃、前后台、断线和相机通道独占。
  - 已修：`PTPConstants` 补齐安卓 `RemoteLab` 的 Start/End/GetLiveView/DeviceReady/Capture 操作码；`CameraRepository` 增加忙重试、DeviceReady 轮询和增强帧→标准帧回退。`RemoteState` 先建立进入、预热、首帧、丢帧恢复、拍摄/录像、取消/断线的纯 reducer；`RemoteFrameParser` 按安卓规则从 Nikon 前后缀载荷提取 JPEG，并以状态/帧解析测试锁定边界。
  - 已修：`RemoteExposureParameters` 集中定义照片/录像属性组、兼容属性优先级和安卓读数格式化规则；测试覆盖值域选择和 EV/ISO/光圈/快门格式。
  - 已修：`RemoteViewModel` 与 `RemoteView` 已接入照片列表入口，执行可取消的连续取帧、JPEG 解码、首帧状态、丢帧重试、缩放、拍摄和退出清理；USB/Wi‑Fi 共用同一 `RemoteCameraControlling` 会话接口。
  - 已修：按安卓 `StartTracking(x,y) → AfDrive → DeviceReady` 时序接入点按对焦；仅在明确返回“不支持”时回退 `ChangeAfArea → AfDrive`，再次点按结束追踪，MF 模式忽略点按自动对焦；iOS 对焦框覆盖聚焦/成功/失败动画并延时收起。
  - 待完成：安卓完整曝光拨轮、反挤压、直方图/斑马线/水平仪、录像、横竖屏/全屏工具和真实页面前后台生命周期；当前代码可类型检查但协议/页面仍未真机闭环，不能勾选。
- [ ] **47. 曝光参数控制**：四格参数、读取/写入、乐观更新、拨轮、完整值表和错误恢复。
  - 已修：`RemoteExposureParameters` 集中维护照片/录像四格属性、跨 Nikon 属性兼容选择与读数格式；`RemoteViewModel` 已按能力读取描述、支持写入后回读确认并在失败时恢复旧值；`RemoteExposureTile` 接入安卓同向逐档上下拖动、松手吸附和只读压暗，数值点击进入相机值表。
  - 已修：监看会话页面内轮询 Nikon `LiveViewSelector(0xD1A6)`，照片/录像属性组会随机身拨杆切换重新读取；录像快门在录像命令未接入前保持禁用，避免把照片命令误发到录像模式。
  - 已修：Auto ISO 按安卓候选顺序（录像 D0AD→D16A→D054，照片 D054→D16A）探测，ISO 卡片只在相机返回可写描述时显示 AUTO 角标，写入失败回滚。
  - 待完成：曝光模式切换、设备属性事件刷新和错误文案/动画；当前 UI 还没有覆盖安卓完整的模式切换和事件轮询。
- [ ] **48. 反挤压功能**：档位、循环顺序、真实画面比例变换、持久化、选择器动画和换相机处理。
  - 已修：新增安卓同序列的 1x/1.33x/1.5x/1.8x/2x 循环与 UserDefaults 持久化，并接入监看画面比例按钮。
  - 待完成：与增强帧元数据和横竖屏布局联动的精确画面比例、选择器外观和换相机重置。
- [ ] **49. 拍照控制**：快门、倒计时、拍摄中状态、结果回传、失败和按钮反馈。
- [ ] **50. 视频录制控制**：开始/停止、录制状态、时长、异常结束和恢复。
  - 已修：接入安卓 `0x920A/0x920B` 开始/停止命令、忙重试、录像态切换和 `REC` 计时徽标；失败回到当前监看页并清理本地计时。
  - 待完成：安卓应用模式前置序列、完成/中断事件确认、异常结束恢复和录像文件回流。
- [ ] **51. 直方图与辅助显示**：直方图数据、显示开关、帧同步、性能和退出清理。
  - 已修：大图预览按钮可按安卓语义切换直方图，并从当前缩略图计算 24 档亮度数据；翻页会清理旧图数据。
  - 已修：监看页加入 24 档亮度直方图叠加、D067 水平仪读取/轮询与显示开关；开关状态按安卓监看偏好持久化。
  - 待完成：监看帧直方图、绘制样式、性能和退出清理逐项对照。
- [ ] **52. 全屏与横竖屏**：沉浸式切换、方向锁定、安全区域、控件动画和返回逻辑。

### I. GPS

- [ ] **53. BLE 设备发现与配对**：权限、扫描、连接、断线、重连和设备选择。
  - 已有：`NikonGPSBluetoothClient` 按安卓 UUID 扫描 Nikon 服务，串行发现特征、开启 pair/not1 通知、执行四消息配对并处理重连/断线；控制器 ID 写入保持 32 字节 ASCII 规则。
  - 已修：设备号和 nonce 按安卓键名语义写入 UserDefaults；后续启动会复用身份，提供清除配对入口并停止当前 BLE 会话。
  - 待完成：Classic 配对替代路径、CoreBluetooth 权限文案/系统状态和真实设备回归。
- [ ] **54. Nikon GPS 数据协议**：坐标、时间、海拔、精度、端序、校验和错误映射。
  - 已修：新增 `GPSProtocol.swift`，逐字节实现 41 字节 GEO payload、坐标方向/度分拆分、海拔符号与 UTC 时间；Nikon 四消息配对的 Blowfish/盐值计算通过安卓捕获向量校验。
  - 待完成：CoreBluetooth 设备连接、特征读写、校验/错误状态和真实 Nikon 设备验证。
- [ ] **55. 更新频率与节流**：频率拨轮、后台限制、电量策略、失败退避和持久化。
  - 已修：新增与安卓相同的 30 秒/1 分钟/2 分钟/5 分钟档位，以及位置和网络采样间隔策略。
  - 已修：`GPSCoordinator` 接入频率拨轮、UserDefaults 持久化和 GEO 写入节流；位置采样与安卓状态模型分离。
  - 待完成：后台限制、电量策略、写入确认和真机 BLE 节流验证。
- [ ] **56. GPS 页面与状态提示**：连接状态、最后更新时间、诊断信息、权限提示和动画。
  - 已修：GPS 页面显示安卓对应的状态文案、相机/坐标/海拔/精度/上次更新时间及重试入口。
  - 已修：状态摘要改为直接复用安卓 `gpsStatusText` 的中文结果（寻找相机、连接相机、等待位置更新、自动写入位置等），不再使用 iOS 自行改写的“正在启动/配对成功”等文本。
  - 已修：连接页 GPS 入口恢复安卓语义：主胶囊恒定显示“GPS”并只控制展开/收起；展开面板承载手机/相机准备说明、首次配对路径、已配对标记、频率拨轮、状态启停按钮、长按关闭、清除配对和诊断复制。
  - 已修：`GPSCoordinator` 由根页面持有，离开 GPS 页面不会意外停止已启用的定位/BLE 会话；页面按 Android `GpsStatus` 展示状态。
  - 待完成：连接帮助气泡、AP 冲突状态、诊断信息和状态动画逐项对照。

### J. 验证、性能与发布

- [ ] **57. 协议样本单元测试**：固定响应、元数据、事件、错误和三种网络链路的结果样本。
  - 已有：PTP 解析/事务、PTP/IP 封装、局域网扫描策略、GPS 配对/GEO 与监看显示/状态的 39 个 macOS Foundation 测试；已覆盖损坏包、事务错配、取消超时和安卓捕获向量。
  - 待完成：PTP 事件、Wi‑Fi 三链路样本、ImageCaptureCore 回调和真机样本。
- [ ] **58. UI 状态和动画测试**：页面状态快照、弹窗进出、列表筛选、预览翻页和取消路径。
- [ ] **59. 性能基线**：列表滚动帧率、预览首帧、大图内存、批处理耗时、主线程占用和 Instruments 记录。
- [ ] **60. USB 真机回归**：至少覆盖一台 Nikon 机身、冷启动、插拔、授权、下载大文件、断线和恢复。
- [ ] **61. 其他链路真机回归**：STA、AP、Wi‑Fi、BLE/GPS 的可行性和系统权限边界逐项记录。
- [ ] **62. iOS 发布构建**：Debug、Archive、签名、TestFlight 包、崩溃符号、隐私说明和发布检查清单。
  - 已有：`dist-debug-ios/build.command` 提供 macOS 真机 Debug 构建、签名校验、时间戳 `.app/.ipa` 产物，以及 `devicectl` / `xcdevice + ios-deploy` 安装并脱离调试会话启动；脚本语法已校验。
  - 验证：`ZTransfer-ios-debug-1.82-20260913-094147` 已通过真机 Debug 构建、签名与 IPA 校验，并安装到 iPhone 14 / iOS 16.0.3；首次启动受开发者信任阻止，信任后 USB 重新连接并单独重试脱离调试器启动成功，进程与屏幕已确认。未为重试启动重复构建。
  - 待完成：本轮布局的真机复验、整条脚本无人工干预的启动验证、Archive/TestFlight、发布签名和最终隐私检查；不能由一次安装推断全部功能通过。

## 每个任务的完成记录格式

完成任务时在对应项目下追加：

```text
实现：<主要类型/文件>
行为依据：<安卓文件、函数或测试样本>
动画/交互：<安卓依据；时长、延迟、曲线/弹簧、交错顺序、手势、连续触发及中断/恢复/关闭规则>
差异：<无；或用户已确认的平台差异及原因，未确认的差异保持待讨论>
验证：<单元测试、模拟数据或真机结果>
提交：<commit>
```

## 当前进度

### 2026-09-13 边框逐项复刻进展（进行中）

- 已对照安卓 `PhotoFrameExporter.kt` 的标准四种边框、铭牌、沉浸、品牌铭牌/画廊、经典签名、画廊衬纸、彩色档案、胶片画廊和胶片边缘分支，重排 iOS `PhotoEffectsRenderer.swift` 的画布比例、信息行顺序、边框水印归属、胶片装饰和调色板采样。
- 新增 EXIF GPS 坐标/海拔解析，并按安卓的四位小数坐标、方向字母和米制海拔格式进入边框信息行；日期/时间显隐与格式在渲染前统一解析。
- 已通过一次无签名 iOS 真机构建；最终真机安装、每个边框的视觉截图对照和导出回归仍待完成，当前不将这部分标记为已完成任务。
- 本轮签名脚本构建成功并安装启动真机：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-130810.app`；仍需在设备上逐个切换预设做视觉截图回归。
- 随后修正画廊衬纸/胶片画廊的绘制层级（外框先于照片），并再次签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-131503.app`。
- 继续对照安卓几何规则，修正经典签名、画廊衬纸、胶片画廊、胶片边缘的方形照片层，并补回经典签名细边框；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-132411.app`。

**2/62**。已验收工程骨架和 Swift 6/iOS 16 配置。其余 60 项保留为待完成或进行中。

2026-09-13 纠正原来的 `10/62`、`11/62` 混用：旧记录将接口存在、模拟器编译成功或顺序调用的测试误当成功能闭环，已撤回任务 5–12、34 的完成标记。代码没有删除；具体已做部分、缺陷和验证证据记录在对应任务下，不把同一局部修复再计成一个整项完成。

### 本轮已验证的协议修复

- 生产代码：`PTPCodec.swift`、`PTPDataset.swift`、`PTPSession.swift`、`AsyncDeadline.swift`。
- 系统适配与纯协议拆开：`SelectedUSBPTPTransport.swift` 放在 USB 目录；新增 `ios/Package.swift` 只编译生产协议源码，测试与 Xcode 测试 target 复用同一份 `ios/ProtocolTests/`。
- 命令：`swift test --package-path ios --scratch-path /tmp/ztransfer-protocol-tests`。
- 结果：**40 tests，0 failures**（PTP/PTP-IP/扫描策略 28 + GPS 2 + 监看状态/帧解析 4 + 曝光/显示参数 5 + 对焦状态 1）；这是 macOS 上的 Foundation 单元测试，不是 App 构建、模拟器测试或真机验收。
- 删除原有错误 DeviceInfo fixture 和“顺序调用证明串行”的测试；其他领域模型旧测试移至 `DomainModelTests.swift`，本轮未运行，不能据此声称领域模型与安卓一致。
- USB 监听改由 `RootView` 持有，修正进入照片列表时连接页 `onDisappear` 停掉监听并清空相机的问题；连接代际校验阻止拔线后旧回调覆盖等待态。新增 `ClassicUSBIcon`，几何对齐安卓自绘三叉图标。SwiftUI 改动尚未运行 App 验证。
- XcodeGen 已重新生成工程文件；未构建/安装 App，未提交/推送。
- 本轮修复了 iOS 16 Swift 6 的工程阻断项：目录 bookmark 改用 iOS 可用 API、统一旧式 `onChange`、修正根页面效果配置注入与列表类型检查；新增 `PhotoEffectsRenderer` 和工作台批处理阶段/安卓文案草稿。全量 iOS 源码类型检查通过（仅 ImageCaptureCore 非 Sendable 警告），尚未构建/安装 App。

### 其他模块当前实际状态

- 连接 reducer、USB 重试和根页面切换已写出草稿；连接服务已串行化握手任务、传播取消，并在独立任务中限时清理旧会话；设备对象身份、断线保留列表、精确文案与 Wi-Fi 链路尚未闭环。
- 照片列表/筛选/连拍/预览已有界面和模型草稿；未知日期顺序、筛选数据来源、连拍显隐/批量操作、手势和动画仍待完整对照。
- 队列已有状态和串行下载草稿；本轮已修正手动重复入队、自动按 `文件名|大小|拍摄时间` 去重、重试生成新任务 ID、开始传输不重置暂停，以及已有文件按名称（含安卓副本后缀）和大小判断。生产队列已通过 Swift 6 类型检查；Effects 快照、目录索引、断线恢复、页面胶囊动画仍未闭环。
- 照片效果和工作台只建立部分设置、导入和选择状态；真实滤镜、渲染、元数据配置、批处理及交互动画未完成，不能当作功能已实现。
- GPS 只有 Core Location 和页面草稿；Nikon BLE 配对、协议写入和节流尚未完成。
- 监看、STA/AP、完整动画和性能验收仍未完成。原生 Form/sheet、临时按钮和系统默认转场需要逐项替换或验证，不能视为安卓复刻结果。

### 下一步顺序

1. 完成 USB 会话生命周期：超时/取消收尾、设备身份和旧回调失效、关闭后重连，先用可控 transport 测试验证。
2. 统一 USB 媒体 I/O 调度与缓存身份，补齐扫描、缩略图和下载的安卓错误/取消/回退规则。
3. 在可靠数据流基础上依次完成连接页、照片列表/预览、队列、设置、工作台、监看、GPS 与 STA/AP；每项先读安卓，再实现，再验收。
4. 需要 App 构建时先取得用户确认；当前构建请求尚未获得回复，不把目标自动继续当作许可。
