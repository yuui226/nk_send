# Z传 iOS 原生复刻计划与任务清单

## 目标与边界

本计划对应 `ios` 分支。iOS 使用独立的 Swift/Xcode 工程，代码放在仓库根目录的 `ios/`，不引入 Kotlin、KMP 或 `shared`。安卓 `app/` 作为行为对照，不参与 iOS 编译；不为 iOS 复刻改动安卓结构，也不自动删除安卓参考代码。

安卓端已经迭代验证过的连接判断、协议顺序、状态流转、错误处理、取消、重试、持久化和文案含义，是 iOS 的功能基准。iOS 可以使用 SwiftUI、UIKit、Core Image、Metal 等原生实现来获得自然的页面和动画体验，但不能因为换了 UI 技术而改变业务结果。

## 强制复刻原则

本节与 [项目记忆](../项目记忆-iOS复刻规则.md) 同为后续开发、审查和验收的长期约束，由仓库 `AGENTS.md` 引用。每次开始或恢复任务时先阅读，子 agent 执行同一规则。

1. **安卓行为是事实来源。** 所有 UI、动画、点击/长按/拖动操作、判断条件、状态流转、协议时序、错误、取消、重试、持久化和文案含义，都必须先从安卓代码和测试中确认，再在 iOS 中实现。不得凭经验补功能或修改语义。
2. **不添加用户没有要求的内容。** 不增加多余文本、页面、按钮、入口、跳转、提示或源码/项目地址展示；安卓没有的能力，iOS 不自行创造。
3. **平台实现可以不同，结果必须一致。** SwiftUI、UIKit、Core Image、Metal 和 Swift Concurrency 可以按 iOS 习惯选用，但只能改变实现方式，不能改变用户可观察的结果。确实无法保持一致时，先讨论并在任务记录中写明差异。
4. **公共组件优先且保持可维护。** 字体、颜色、间距、按钮、拨轮、弹窗、状态模型、动画曲线和错误映射应集中封装；组件有清晰职责，避免复制粘贴、临时占位和无法测试的巨型文件。
5. **每项功能必须闭环。** 入口、正常路径、边界、错误、取消、重试、恢复、持久化、动画和测试全部完成后，才能在清单中标记完成。
6. **文本与可见行为逐项对照。** 所有文本逐字一致，包含标点、空格、大小写和版本号格式；页面元素位置、显隐条件、按钮功能、手势、动画触发/中断/恢复均须对照安卓。截图辅助核对，不能代替阅读实现。不把原生系统默认交互直接当作复刻完成。
7. **验证记录如实填写。** 编译成功或接口存在不代表功能完成；未执行的测试不写通过。发现过去的完成状态不准确时立即更正，保留问题和后续工作记录。
8. **动画参数与时序也是复刻内容。** 时长、延迟、曲线/弹簧参数、位移、缩放、透明度、交错顺序和手势联动逐项依据安卓实现；连续触发、中断、取消、恢复和退出清理一并核对。不能用系统默认效果替代，优化性能也不能擅改行为。
9. **复用不改变状态归属。** 复用组件、模型类型、渲染器和公共逻辑；安卓独立的配置、草稿、持久化作用域及任务生命周期继续独立。工作台与相机传输的照片效果配置分别保存，不能因使用相同控件而合并。不为 iOS 复用改动安卓结构。

第一条实际连接链路选择 USB 有线连接。先把“插入相机—授权—建立 PTP 会话—读取文件—预览—下载”跑通，再扩展 Wi‑Fi STA、AP 和 BLE/GPS。USB 能力必须以真实 iPhone、转接设备和 Nikon 机身验证结果为准，不能只按模拟器结果判断。

## 进度规则

- 总任务数：62
- 当前进度：**2/62**（完成工程基础 2 项；其余 60 项尚未完成验收）
- 每完成一个可验收任务，将勾选对应项目、补充实现提交和验证证据，并更新本节进度。
- “完成”必须包含入口、正常路径、错误/取消路径、状态恢复和对应测试；只有写出界面不算完成。
- 未经确认不进行真机打包；代码阶段优先使用单元测试、模拟数据和 Xcode 预览，最后集中真机回归。

## 待处理问题（2026-09-13 用户真机反馈）

以下问题由用户在当前工作台版本中直接操作发现，全部保持未完成状态；在逐项修复并完成真机回归前，不得把相关任务标记为完成，也不得以“已能编译”代替验收。

1. **滤镜预览未生效**：工作台选择滤镜后，预览图没有展示对应滤镜效果；需要沿安卓的滤镜目录、参数和预览渲染路径逐项核对，不能只显示选项名称。
2. **边框效果不是安卓实现**：当前 13 个边框样式为臆想实现，未按安卓端完整版式、颜色、间距、元数据布局和水印锚点复刻；必须以安卓 `PhotoFrameExporter.kt` 及相关预览代码为唯一依据重做。
3. **滤镜收藏误触拨轮**：点击滤镜收藏按钮会改变当前选项，并可能把错误的滤镜加入收藏；需要隔离按钮与拨轮的命中区域，并验证收藏前后当前滤镜和强度保持不变。
4. **边框收藏状态错误**：边框无法正确加入收藏，取消收藏也不能稳定生效；需要按安卓完整收藏对象（边框预设 + 水印配置）进行单次原子更新，并验证重新选择、取消、重启后的持久化。
5. **水印控制全部失效**：水印无法开启或关闭，水印设置入口及展开后的各项按钮点击后没有效果；需要核对开关、展开状态、内容类型、文本/图片和各拨轮的绑定及状态写回。
6. **批量生成速度过慢**：工作台生成效果图的速度明显低于安卓；需要对照安卓批处理管线检查图片解码、滤镜/边框/水印渲染、并发限制、缓存和写入流程，不能只通过降低质量掩盖性能问题。

### 2026-09-13 本轮修复记录

- **边框信息排版修正（代码已落地，真机待验收）**：纠正原先直接把 CoreText 向上的字形坐标当成 UIKit 向下坐标的错误；旧版“按实际字形居中”的记录不代表绘制正确。新增 `PhotoFrameTextRow` / `PhotoFrameTextLayout`，用同一组 CTLine 测量和绘制，统一混合字体基线、真实墨迹高度、上下对称安全留白及拥挤时先压行距、再整体缩小字号的顺序。
  - 接入标准四种边框、铭牌左右列、品牌内嵌/留白、沉浸和编辑类信息区；空内容不占一行，标题/长镜头/参数按各区域宽度收缩，铭牌两列共享基线、分隔线跟随信息实际高度，沉浸保留安卓的照片底部安全距离。前四种沿用安卓 6% 上下留白和 `min(画布宽 × 0.0125, 信息区高 × 0.09)` 行距上限；其余原本没有内部留白的信息条也保留 6% 安全边距，响应用户本轮“不能贴边”的要求。毛玻璃字号仍按画布宽计算，不再误用较窄面板宽度二次缩小。
  - 依据：安卓 `PhotoFrameExporter.kt` 的 `drawMetadata`、`centeredFrameTextBaselines`、`frameTextScaleToFit`、铭牌/沉浸/编辑区布局及 `PhotoFrameExporterTest.kt` 中的单行居中、混排、拥挤行距、超大水印用例。iOS 的预览和导出共用此排版器。标准边框的检查样例按真实顺序排列：品牌/型号 → 可选镜头 → 曝光参数与日期同一行 → 可选位置与海拔同一行 → 水印末行；纠正初版检查样例把水印夹在日期和位置之间的错误，生产标准渲染路径已核对仍为水印末行。
  - 验证：`ios/scripts/test-frame-text.sh` 在 macOS 直接渲染生产 CoreText 排版代码；7 项定向测试通过，含 320/960/1280 宽度、1/2/3/4/5 行的实际像素居中与边距检查、三种内置水印字体、超宽标题及拥挤行距。本地检查图 `/tmp/ztransfer-metadata-layout.png` 已查看；iOS 16 / Swift 6 源码类型检查通过，仅有既有滤镜并发指针警告。原有 SwiftPM 整包测试被 `PhotoEffects.swift` 的 UIKit 依赖阻断，因此本轮提供独立排版测试入口，未声称整套测试通过。未打包、安装或真机验收，不增加 2/62 的已完成任务数。
- **标准照片阴影统一修正（代码已落地，真机待验收）**：雾白、暗夜、简白、毛玻璃、品牌内嵌、品牌留白、彩色档案及艺术装裱的黑色衬纸外框不再走会在 iOS 缩放预览中扩散成灰色圆角块的四分之一尺寸代理阴影，改用主画布上的窄接触阴影；暗夜只提高接触阴影透明度。毛玻璃下方的半透明信息面板保持独立绘制，与安卓设计区分开。未打包、安装或真机验收，不增加 2/62 的已完成任务数。
- **多选预览横向分页手势（代码已落地，真机待验收）**：对照安卓 `LocalPhotoPreviewPager` 的原生横向 Pager，修正 iOS 工作台外层纵向 `ScrollView` 与预览 `TabView` 的手势竞争。工作台返回手势现在只接受纵向主方向；预览子视图的长按对比拖拽改为长按后再识别移动，并在长按保持阶段继续显示原图，普通左右滑动不会被零距离拖拽抢占。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- **水印编辑行布局（代码已落地，真机待验收）**：按安卓 `PhotoFrameWatermarkEditor` 重排工作台水印设置展开区：第一行使用 1/3 宽的“水印类型”拨轮，右侧 2/3 依内容显示同一行文字输入框或带图片图标的“更换 Logo”按钮；图片类型统一显示“Logo”，类型切换和无 Logo 时仍进入图片选择器。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- **水印输入焦点与页面手势隔离（代码已落地，真机待验收）**：工作台水印文字输入框接入焦点状态；输入或键盘滚动期间屏蔽外层向下返回连接页手势，失焦后恢复页面切换。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- **少选项拨轮与页面翻页手势隔离（代码已落地，真机待验收）**：`DetentWheel` 仅给可连续上下调节的长选项列表挂载高优先级拖拽；两三个选项的点击型拨轮不再用高优先级点击拦截按住滑动，使外层工作台页面可以继续响应翻页。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- **水印设置区域页面翻页（代码已落地，真机待验收）**：取消工作台返回手势的顶部起点限制，并移除短选项拨轮的高优先级及空长按识别器；在水印设置区域下滑时，页面可直接响应返回连接页，拨轮点击行为仍保留。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- **水印展开视觉与描边修正（代码已落地，真机待验收）**：水印设置展开区补齐安卓同款紫色内嵌色块、边框和顶部展开过渡；描边效果改为按实际字号 7.5% 线宽、根据填充色选择对比色并先描边后填充，避免固定线宽和固定白色造成粗细不均。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- **展开/收起动画回退稳定实现（代码已落地，真机待验收）**：边框信息和水印设置统一使用顶部锚定的缩放淡入过渡，移除额外的外层动画和移动/裁剪效果，避免展开末尾跳动、收起高度异常。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- **工作台预览时序与中间结果复用（代码已落地，真机待验收）**：对照安卓 `PhotoEffectsRenderedPreview` 的当前帧优先、对比帧延迟策略，iOS 先显示当前滤镜/边框/水印效果，140ms 空闲后再生成长按对比原图；滤镜结果单独缓存，边框或水印调整不再重复执行滤镜。Xcode iOS Simulator generic build 成功，未打包或安装，不增加 2/62 的已完成任务数。
- 已修复上述 1、3、4、5、6 的代码路径：预览设置变化会清空旧图并重新渲染；收藏按钮与拨轮隔离且采用原子快照；水印控件统一通过原子绑定写回；滤镜像素处理改为受控并行，避免批量任务 CPU 争抢；无边框水印沿安卓 watermark-only 路径保留原照片尺寸。
- 已重做上述第 2 项的渲染管线：13 个边框均有独立布局/装饰分支，品牌与型号按安卓规则规范化，颜色档案从照片采样调色板，水印尺寸和锚点按安卓分段比例计算。
- 验证：Swift 全量语法解析通过；Xcode iOS Debug generic build 成功；`dist-debug-ios/build.command` 已签名、安装并启动真机（iPhone 14，iOS 16.0.3）。

## 安卓对照资料

照片列表的加载顺序、扫描代际、取消/暂停恢复、缩略图内存与磁盘缓存、后台填充队列、清理和对账规则已单独整理在
[安卓照片列表加载与缓存策略-复刻基线](./安卓照片列表加载与缓存策略-复刻基线.md)。实现照片列表相关任务前必须先按该文档核对 `CameraViewModel.loadFiles`、`ThumbnailDiskCache` 和 `ThumbnailFillQueue`，并在任务记录中补充对应的 iOS 代码位置和验证证据。

### 2026-09-13 照片列表加载基线落地（进行中）

- 新增详细对照文档：`docs/技术调研/安卓照片列表加载与缓存策略-复刻基线.md`。文档记录了 `loadFiles` 的 18 步顺序、generation 与同相机快照恢复、STA `DEVICE_BUSY` 重试、12 项批次发布、双卡逻辑去重、缩略图 LRU/in-flight/负缓存、后台填充队列、相机级磁盘缓存、90 天清理、成功扫描后的 reconcile，以及 FHD/EXIF/效果预览让路规则和安卓源码位置。
- iOS `PhotoListViewModel` 已先落地扫描状态骨架：每轮递增 generation，取消旧任务，按安卓 `FILE_THUMBNAIL_PIPELINE_BATCH_SIZE=12` 原子发布批次，批次间 `Task.yield()` 让界面渐进更新；旧任务不能覆盖新一轮，失败保留已发布文件，不把不完整扫描当成空列表。
- 新增 `ios/ZTransfer/Domain/PhotoThumbnailDiskCache.swift`：按相机身份 SHA-256 分目录，普通/STA 句柄键、旧键迁移、原子临时写入、非空校验、90 天相机目录清理和完整扫描后 reconcile。
- 验证：`xcodebuild -project ios/ZTransfer.xcodeproj -scheme ZTransfer -sdk iphonesimulator -configuration Debug CODE_SIGNING_ALLOWED=NO build` 成功。尚未进行真实相机扫描、缓存命中/迁移/清理测试或真机验收；任务状态保持进行中。
- 新增 `ios/ZTransferTests/PhotoThumbnailDiskCacheTests.swift` 覆盖键稳定性、写入/读取、非空文件和成功扫描后的 reconcile；执行 Xcode 测试时被工程既有的 `ios/ProtocolTests/PTPIPDiscoveryTests.swift` 对 `ZTransferProtocol` 模块依赖阻断，未声称测试通过，待统一修复测试 target 依赖后重跑。
- `CameraSession.thumbnail(file:)` 已接入 `PhotoThumbnailStore`，列表缩略图现在按安卓顺序查找内存、负缓存、相机磁盘并共享同文件的进行中请求，最后一个等待者取消时会取消底层请求；成功结果写回磁盘。缺少稳定机身序列号时会放弃持久化缓存，避免不同相机串缓存。STA 直读使用 `sta + handle + size` 专用键，普通键仍支持迁移；`prefetchThumbnail` 已接入扫描批次，并按安卓顺序逐项预取新加入列表的文件。仍缺真实相机验证和系统错误码映射。
- 日期分组已纠正为安卓使用的原始 `YYYYMMDD` 键和 `zzz_unknown` 未知日期键；渲染时才格式化为 `YYYY-MM-DD`，避免折叠状态、未知日期排序和筛选键与安卓分叉。对应 `PhotoCatalog.swift`、`PhotoListView.swift` 及 `DomainModelTests`。
- `CameraRepository.loadCatalog(onBatch:)` 与 `CameraSession.scanCatalog(onBatch:)` 已把元数据结果按 12 项边界流出；批次回调是可挂起的，`PhotoListViewModel` 在每批发布后逐项执行 `prefetchThumbnail`，因此相机扫描会等待本批预取完成再读取下一批。每批发布前检查 generation/取消，扫描完成后才置 `hasCompletedFileScan`。StorageID/句柄快照、双卡 logicalIdentity 去重和 STA 直读专用缓存键仍待下一阶段接入。
- 流式扫描后的模拟器 Debug 构建再次成功；Xcode 测试仍受既有 `ZTransferProtocol` target 缺失阻断，未把测试结果记为通过。

实现每项功能前先阅读对应安卓入口和状态来源，记录到提交说明中：

- 连接与 PTP：`app/src/main/java/com/ztransfer/protocol/`、`CameraViewModel.kt`、`TransferViewModel.kt`
- 页面与交互：`HomeScreen.kt`、`FileListScreen.kt`、`PhotoPreview.kt`、`TransferScreen.kt`、`RemoteScreen.kt`、`SettingsScreen.kt`
- 照片效果：`PhotoEffectsBatch.kt`、`LocalPhotoEffectsOverlay.kt`、`LocalPhotoPreviewPager.kt`
- GPS：`gps/` 下的状态、BLE 客户端和协议文件
- 验收清单：`docs/测试与验证/`、`docs/技术调研/安卓非shared特性回放手册.md`

视觉参考来自 `/Users/orys/code/z传截图/`。当前文件包括 11 张 1272×2800 的界面截图和 1 张 Nikon Z 30 TIFF 样本。复刻时逐张登记页面、状态和对应任务，不把微信文件名当作页面语义。

## 任务清单

### A. 工程与基础设施

- [x] **1. Xcode 工程骨架**：建立 `ios/ZTransfer.xcodeproj`、App target、Tests target、资源目录和 scheme。
  - 实现：`ios/project.yml`、`ios/ZTransfer.xcodeproj`、`ios/ZTransfer/App/`、`ios/ZTransferTests/`
  - 行为依据：安卓 `MainActivity.kt` 的单入口页面结构；连接状态先独立于 UI 建模。
  - 动画/交互：暂未进入页面交互实现；后续按对应任务补齐。
  - 验证：`xcodebuild -project ios/ZTransfer.xcodeproj -list` 已确认两个 target 和 `ZTransfer` scheme 存在；模拟器 Debug build 成功。
- [x] **2. Swift 并发与最低系统版本**：部署 iOS 16，启用 Swift 6；工程支持 Swift Concurrency。各链路的取消、超时和会话清理由任务 6–13 单独验收。
  - 实现：`ios/project.yml`、`ImageCaptureUSBTransport.swift`
  - 行为依据：安卓连接任务的可取消生命周期；未引入后台线程阻塞 UI 的同步 I/O。
  - 验证：模拟器 Debug build 成功。
- [ ] **3. iOS DesignSystem**：统一颜色、字号、间距、圆角、阴影、触控热区、图标和动画曲线，建立浅色/深色主题。
  - 已修：`ZTransferDesign` 的全局字号改用 Android Material 对应的 system sans 语义；连接强调色改用 Android `Color.kt` 的浅色/深色 token。
  - 待完成：所有页面迁移到同一套 token、皮肤/主题持久化、图标与动画曲线逐项对照；当前只算基础 token 修正，不能标记完成。
- [ ] **4. 行为状态图**：把连接、文件扫描、预览、下载、队列、监看、GPS 的状态、事件、错误和取消写成可测试模型。

### B. USB 有线连接与 PTP

> 2026-09-13 审计：以下项目原先只有部分接口/编译记录，却被标记完成；已全部退回进行中。纯协议单测成功也不代表系统传输适配和生命周期已通过验收。

- [ ] **5. USB 设备发现**：通过 ImageCaptureCore 仅浏览本地相机，处理新增、移除和重复 UUID。
  - 已有：`ImageCaptureUSBTransport` 的浏览器和事件模型；发现流程移至 `RootView` 持有，避免连接页消失时清空设备。连接页卡片使用与安卓相同几何的自绘三叉 USB 图标。
  - 依据：安卓 `CameraViewModel` 的设备枚举/拔出流程。
  - 已修：设备映射、事件流和系统回调访问统一经过锁；保存已打开的 `ICCameraDevice` 对象，避免相同 UUID 重连时旧关闭回调关掉新会话；停止/拔出会结束挂起的缩略图请求。
  - 已修：无 UUID/名称的驱动使用进程内稳定对象身份，`didRemove`、`ready` 和错误回调不会因随机回退 ID 失配；重复设备事件仍按描述符去重。
  - 待完成：重复事件的去重策略、事件流生命周期的完整测试；真实插拔待任务 60。
- [ ] **6. 授权与会话生命周期**：未决定/拒绝/受限/已授权，打开/关闭会话、取消连接、设备替换和断线事件。
  - 已有：ImageCaptureCore 授权、会话接口。
  - 已修：`CameraConnectionService` 改为单一连接任务，使用不会等待不合作子任务的 deadline；调用方取消会取消共享握手，失败/取消后在独立收尾任务中限时关闭旧会话。
  - 已修：关闭会话按已打开对象身份匹配，不会因相同 UUID 的新设备替换而误关新会话；拔出/停止会清理等待者。
  - 已修：打开、读取和下载回调统一使用一次性 continuation box；调用者取消立即结束等待，迟到的 ImageCaptureCore 回调不会二次恢复或污染新会话；下载取消同时取消 NSProgress。
  - 已修：打开中的设备单独保留并纳入关闭收尾；USB PTP 发送也支持取消；连接任务取消后显式断开服务，避免 OpenSession 刚完成时遗留会话。
  - 已修：授权拒绝/受限沿用安卓“未获得 USB 权限，请重新插线并允许访问”失败态，重新收到设备事件时清除旧错误并重新尝试；部分驱动只发 ready 时也会触发连接。
  - 待完成：关闭回调错误的安卓文案映射、系统请求的物理取消能力和真实系统回调验证。
- [ ] **7. PTP 数据层**：命令、响应、事务 ID、端序、超时、错误映射和分包读取。
  - 已修：`PTPCodec` 的非对齐读取、带非零起始索引的 Data、32 位容器长度、最多五个命令参数；`AsyncDeadline` 在系统回调不返回时也能结束调用方等待。
  - 依据：`UsbPtpConnection.sendCommand/receiveResponseWithPayload/checkTransaction`，PTP 完整事务被消费后才能放行后续操作。
  - 验证：独立协议测试已运行；正常响应、损坏长度/类型、事务不匹配、超时、取消和迟到响应已覆盖。
  - 待完成：系统适配层回调响应/载荷的真实设备样本、权限/断线/超时错误映射、关闭旧通道后再连接；不能把结束等待当作物理 I/O 已取消。
- [ ] **8. PTP 事件串行队列**：每会话的事务号、命令排队和事件分发；缩略图/下载/预览不能争用命令通道。
  - 已修：`PTPSession` 使用显式等待队列，跨 `await` 保留事务所有权；排队取消不发命令、不消耗事务号；活动请求超时/取消使本会话失效，拒绝后续请求；完整的 busy/unsupported 响应不使通道失效。
  - 验证：40 个并发调用时最大活动 I/O 数为 1；另外覆盖排队取消、活动取消、超时、迟到响应、busy、不支持、错误事务号/容器类型。
  - 待完成：ImageCaptureCore 媒体请求与 PTP 请求的统一仲裁，以及 PTP 事件接入、断线清理。此前两个顺序调用的测试不能证明串行，已替换。
- [ ] **9. 相机信息与能力读取**：读取 DeviceInfo 字段与安卓能力集合。
  - 已修：补回 FunctionalMode 和五组数组，保留 vendor description、operations、events、properties、capture/image formats，之后读取制造商、型号、版本和序列号。
  - 依据：`RemoteLab.parseDeviceInfo`；旧解析缺 FunctionalMode 和 ImageFormats，旧测试样本也缺 FunctionalMode，均已修正。
  - 验证：非空五组数组样本、所有截断前缀、非对齐和非零起始索引 Data 测试通过。
  - 待完成：连接层能力初始化、真实响应样本和异常重试，不只验收解析函数。
- [ ] **10. 文件对象与元数据解析**：ObjectHandles、ObjectInfo、文件名、格式、大小、拍摄时间、保护状态和目录过滤。
  - 已修：ObjectInfo 文件名从固定偏移 52 读取；32 位无符号大小保留 `0xFFFFFFFF`；目录成功但不形成照片；扩展名从实际文件名获取，缺文件名时按安卓格式表和 handle 低 16 位生成兜底名称。
  - 已修：`PTPObjectInfoResult` 区分完整身份与不完整载荷；缺失/截断/无终止符日期保留 UI 名称但不能授权缓存清理；显式零长度日期合法。
  - 依据：`NikonCamera.getObjectInfoInternal/parseObjectCacheIdentity/FileInfo.extension`、`PtpConstants.FORMAT_EXT`、`ObjectInfoCacheIdentityTest`。
  - 验证：已移植安卓缓存身份测试场景，补齐固定偏移、未知大小、目录、扩展名和数组边界测试。
  - 已修：`CameraRepository.loadCatalog()` 对多卡逐卡请求 ObjectHandles 并去重，保留每个对象的实际 StorageID，单卡/无卡列表仍分别走对应查询。
  - 待完成：扫描层传播完整性/失败状态，取消不能被 `try?` 吞掉，缓存身份/增量更新待对照实现。
- [ ] **11. 缩略图和原图读取**：请求合并、取消、回退、缓存及预览首帧。
  - 已有：ImageCaptureCore 缩略图、文件前缀读取接口，`ThumbnailCache` 有容量限制。
  - 已修：缓存键包含设备会话身份；取消与等待者登记在同一锁内，取消先发生时不发起请求；下载 Progress 和进度轮询会随任务取消。
  - 已修：打开/读取/下载的迟到回调不会二次恢复；停止或拔出会结束挂起缩略图等待者。
  - 已修：缩略图等待键包含设备会话身份，同一 PTP handle 在设备替换时不会串回旧相机的回调。
  - 已修：网络会话可通过同一 PTP 仓库请求缩略图和前缀读取，避免把无线会话误接到 USB 回调；预览按 FHD→LargeThumb→标准缩略图顺序回退。
  - 待修：按设备维度结束等待者、系统错误码映射、FHD/RAW/视频缩略图的安卓回退路径和真实样本。
- [ ] **12. USB 下载保存**：授权目录、命名、已存在文件判断、进度、取消、部分文件清理和真实大文件。
  - 已有：ImageCaptureCore 下载接口和进度回调。
  - 已修：取消先于 Progress 登记时会记录取消并在 Progress 出现后立即取消；独立进度任务可被回收。
  - 已修：下载 continuation 支持立即取消，迟到完成回调被忽略；Progress 登记与取消竞态已处理。
  - 已修：PTP/IP 会话使用分段读取写入临时文件，完成后原子移动，取消/断线会清理半成品；未知大小先按安卓专有 GetObjectSize 取 64 位长度。
  - 待修：返回文件名/路径处理、断线错误映射和真实大文件；需对照安卓按名称及大小判断已有文件，不能见同名就跳过。
- [ ] **13. 断线/重连恢复**：下载取消、队列保留、会话重建、列表刷新和用户可见错误保持一致。
  - 待完成：旧连接结果不能覆盖新设备/新会话；按安卓保留断线列表和队列；关闭失败、回调迟到和重试限制的完整状态测试。

### C. 连接页

- [ ] **14. 连接页结构**：复刻 Z 标、USB/Wi‑Fi 卡片、GPS 入口、设置入口和底部工作台入口。
  - 2026-09-13 真机现状：iPhone 14 / iOS 16.0.3 已成功安装；开发者信任及 USB 重新连接后，脱离调试器启动成功，`ios-deploy --get_pid` 返回 1343。截图显示上下黑边，内容区呈旧 320×480 比例，Wi‑Fi 标题溢出、切换条与图标重叠、底部入口过宽。安装包 Info.plist 缺少启动屏与场景配置。
  - 本轮修正：按本机 Xcode SwiftUI App 模板补齐生成启动屏/场景清单；抽出 `ConnectionPage`/`ConnectionMethodCard`，按 `HomeScreen.kt` 使用 56 顶栏、真机测得 310/250 双卡、12 卡间距、10 GPS 间距及 `(可用高度−56−310−62)×0.28/1.28×0.94` 的顶部比例留白；工作台使用自然宽度与底部 18 留白。卡内改为 42 图标底座、21 步骤圆点、30 模式条、34 工具入口、42 连接按钮，使用 `Type.kt` 的字体/行高常量；双 Z 按 `ZMark.kt` 重绘全尺寸路径与分隔缝。
  - 验证边界：新的布局仅通过 iOS 16 / Swift 6 类型检查，尚未重新构建或真机截图；底部工具入口的动作、AP/STA 完整反馈、设置锚点浮层和其它皮肤仍未完成。GPS 仅搬出原连接页内嵌视图并调整入口字号/圆角，没有改动 GPS 协议及状态流转。
  - 已修：根页面持有连接与 GPS 状态；GPS 入口改为连接页内可直接切换的胶囊按钮，不再额外弹出一套独立入口。
  - 已修：GPS 启用期间连接卡片按安卓语义降级显示并禁止发起 USB/Wi‑Fi 连接，避免无线和定位同时争用系统链路。
  - 待完成：Z 标/按钮几何、工作台位置和安卓截图逐项测量验收。
- [ ] **15. USB 卡片状态动画**：不可用、等待插线、发现设备、连接中、已连接、失败和断开状态。
  - 本轮修正：呼吸保留安卓 2.4 秒、0.38/0.82 分段五次平滑曲线和 Wi‑Fi 半周期错位；将逐帧更新移到捕获卡片内容的变换修饰器，后台或禁用时暂停时钟。完整连接成功图标转场及真机帧率仍待验证。
- [ ] **16. Wi‑Fi/AP/STA 连接链路**：按安卓实现发现、配对、相册访问判定和连接生命周期。
  - 2026-09-13 STA 实现依据：完整对照 `NikonCamera.connectSta` / `initializeStaBrowsingSession` / `completeInitialPairing`、`PtpIpDiscovery.discover`、`CameraViewModel.startStaDiscovery` / `tryConnectStaCandidate` / `activateStaCamera` / `startKeepalive`、`StaCameraProfileStore.kt`。安卓代码与资源未修改；本分支仅增加 Swift 实现。
  - 已落实报文修正：无出站数据命令使用 phase=1；PTP/IP 响应转为标准 PTP 容器时长度为 payload+6。STA 使用持久化的 16 字节 ASCII 身份、ZTransfer、32 位 0x00010000 版本，OpenSession(1) 的事务号从 0 开始；AP 保留其 NikonPTP/随机 GUID/16 位版本报文，OpenSession 改为使用 ACK 会话号，并接受 SessionAlreadyOpen。
  - 已落实发现：保存 IP 必须匹配实际本地路由且 15740 开放；Bonjour `_ptp._tcp` / `_nikon._tcp` 与最多 /24 的扫描并发，48 个一批、24 个并发探测、450ms 超时。Nikon 握手逐个执行，失败继续下一个候选；预期相机 GUID 不匹配的首个候选留到正常扫描结束后再试。iOS 网络适配绑定已证明的 localAddress，包含个人热点的桥接接口，避免把 STA 限死在 en0/Wi‑Fi 默认路由上。
  - 已落实身份与配对：两个安装级身份分别保存；只有 0x935A(0x2001) 的成功响应写入每台相机的 `sta_paired_` 标记，路由/旧 paired 字段不能代替协议配对。首次配对按 0x952B → 0x935A → 可选 DeviceInfoChanged（最多 8 秒）→ CloseSession，标记在可选事件等待前同步落盘。重置等待旧连接任务清理完成再清空全部相机档案及身份，保留无线模式。
  - 已落实重连判断与时序：首次配对等待 6600ms；服务未就绪仅重试一次，等待 1200ms；满足安卓条件后仅尝试一次另一身份，等待 900ms。手动扫描在已有可复用档案时继续看护，扫描重试间隔为 3/8/15/30 秒并封顶。相册验证与协议配对确认同时满足才进入照片列表。取消、切换模式、USB 接管按代次隔离旧任务，清理后再发起新连接。
  - 已落实相册访问验证：兼容初始化 0x941C、StorageIDs、完整配对能力集合、首/中/末 ObjectInfo 抽样、缩略图/大小/64KiB 部分对象探测、应用模式 1 探测及失败回退模式 0，顺序与分支按安卓。空 handle 列表与非空但不可读分别处理，不能把设备上传队列当成完整相册。验证阶段的 storage/handle 数据供首次列表读取复用；STA 的 DeviceBusy 列表请求按 750ms、最多 3 次处理。
  - 已接入 STA 直接读取相册：相机拒绝 ObjectInfo 时，使用安卓的文件名属性索引、0x9434 日期索引、Nikon MakerNote 文件号锚点及文件头回退；JPEG 先读 68KiB，必要时补至 128KiB，前缀只取缺少的后段。最近前缀最多 4 个、每个 512KiB，编码缩略图缓存上限 4MiB。预览使用相机 FHD/LargeThumb、JPEG MPF、NEF TIFF 内嵌图索引及有界探针；视频封面使用最多 8MiB 的前缀交给平台解码，临时文件始终清理。下载仍走已有分段传输，修正超过 4GiB 时低 32 位偏移转换溢出。
  - 已接入连接后维护：事件 socket 持续读取并原通道响应 PING；空闲 10 秒心跳把任意完整响应视为链路存活，前台传输期间跳过。STA 事件进入同一新增/删除入口，90ms 合并、每批最多 16 个、最多 5 次元数据解析及 180/360/720/1400ms 退避；2 秒命令事件兜底、10 秒仅核对 handles，传输/预览/监看及完整扫描期间让路。列表订阅增量目录结果，断开时结束订阅及任务。
  - 已接入连接按钮的寻找/配对/连接/成功状态与忙碌时点击取消，重置确认窗及相机数使用现有安卓 resource key；无线模式和两个身份/多台相机档案可跨启动保留。STA 灯泡帮助与热点设置入口属于任务 18，不以本次协议接入冒充完成。
  - 验证（2026-09-13）：`ZTRANSFER_PROTOCOL_ONLY=1 swift test --package-path ios` 共 60 项通过、0 失败；包含安卓配对/档案/重连分支的命令轨迹测试、协议报文、取消与心跳互斥、直接读取索引、MakerNote 双字节序、MPF/NEF/事件格式。iOS 全量 Swift 源码 `swiftc -typecheck` 通过；仅有既有 `Np3BitmapFilter.swift` 指针 Sendable 警告。未运行 App 构建、签名、安装或真机操作，未提交/推送。
  - 保持本任务未勾选：本项同时覆盖 AP，AP 专用网络门控、失败显示及恢复流程仍需继续对照；iOS 没有公开 DHCP gateway API，已有 AP 子网门控属于此前记录的平台差异。不能把此次 STA 代码接入及协议测试当成整项 AP/STA 验收完成。
- [ ] **17. 连接结果流转**：成功进入照片列表，失败停留连接页，重复点击、拔线和返回行为与安卓一致。
- [ ] **18. 连接页弹窗与提示**：权限、失败、帮助和设备信息弹窗的样式、锚点、遮罩、外部点击和关闭动画。
  - 已修：工程补充本地网络权限用途文案，PTP/IP 连接前走系统 Wi‑Fi 网络接口。
- [ ] **19. 连接页真机验收**：冷启动、插入、拔出、拒绝授权、重新授权和多次进入退出。

### D. 照片列表与大图预览

- [ ] **20. 日期分组和网格**：分组标题、折叠展开、空态、加载态、保护/连拍标记和布局自适应。
  - 已修：照片筛选保留原始文件集；新增安卓对应的连拍筛选条件；连拍折叠开关与合集展开状态已接入列表草稿；移除安卓网格没有的文件名额外文本。
  - 已修：日期分组增加可折叠标题与数字计数，网格列数读取设置；缩略图补回扩展名与保护角标，避免显示安卓没有的文件名文本。
  - 待完成：传输状态标记、空态/加载态和安卓网格测量与动画对照。
- [ ] **21. 列表滚动体验**：缩略图复用、下拉刷新、回到顶部、滚动中加载策略和动画性能。
  - 已有：列表扫描暴露可取消 `reload()`，接入 SwiftUI 下拉刷新，取消不会替换现有目录。
  - 已修：增加单次 `ScrollViewReader.scrollTo` 回到顶部按钮，按钮显隐使用滚动偏移淡入/缩放过渡，避免分段跳顶。
  - 待完成：缩略图复用/预取、滚动中加载策略和安卓动画性能对照。
- [ ] **22. 筛选浮层**：类型、保护、连拍、未传输、卡槽、日期和清除筛选，保留选择状态和关闭行为。
  - 已修：筛选数据源改用未过滤的完整目录；补齐“连拍”筛选状态和选择保留；筛选表单改为安卓同结构的类型/状态/卡槽/日期分组按钮，不再使用原生 Form 的额外行和默认控件。
  - 已修：队列完成状态会回流到列表筛选的“未传”判定，筛选变化即时重算。
  - 待完成：浮层锚点/动画、磁盘已有文件索引、日期拨轮与安卓文案逐项对照。
  - 已验证：新增连拍筛选后，纯领域规则测试覆盖“3 张连续连拍”和存储卡过滤，语法检查通过；协议、PTP/IP、GPS、监看状态/帧解析、曝光参数/显示参数与对焦状态回归共 39 tests、0 failures。
- [ ] **23. 连拍合集**：合集封面、展开/收起、成员关系、批量操作和对应过渡动画。
  - 已修：按安卓的连续编号/时间规则生成合集；列表支持点击封面展开，关闭“连拍成组”会清理展开状态。
  - 待完成：合集角标、成员批量传输、预览快照、展开级联动画和回收位置对照。
- [ ] **24. 单选与长按入口**：点击预览、长按预览、长按分类帮助、触控冲突和无障碍操作。
  - 已有：点击/长按根据“照片列表操作”设置切换预览与传输；连拍封面点击优先展开。
  - 待完成：安卓长按阈值、触感、预览起点、无障碍标签和快速重复点击边界。
- [ ] **25. 大图预览容器**：从缩略图位置展开、沉浸式黑底、缩放、双击、拖动、旋转和返回原位置。
  - 已修：iOS 预览使用黑底分页容器，支持缩放、双击、放大后拖动、旋转和异步高清预览回退。
  - 待完成：从缩略图位置展开/收起的锚点动画、旋转持久化和安卓手势冲突规则。
- [ ] **26. 预览信息与翻页**：文件名、曝光参数、保护/连拍标记、传输状态、加载占位和翻页淡入淡出。
  - 已修：预览移除无意义的“第几张/总数”文本；文件名翻页时淡入更新，曝光信息沿用同一字号常量；新增基于当前缩略图的直方图数据加载入口。
  - 待完成：高清原图/FHD 回退、保护/连拍/传输标记和安卓翻页时序对照。
- [ ] **27. 预览入队飞行动画**：单张/合集入队、队列胶囊回弹、取消、快速重复点击和页面退出清理。

### E. 传输队列

- [ ] **28. 队列模型**：等待、传输、暂停、完成、失败、取消、跳过和重复任务去重。
  - 已修：加入等待任务撤下、终态清理、批量重试；活动任务不被清理/取消，重试创建新的任务 ID。
  - 已修：队列项目写入 UserDefaults；应用重启时将未完成的 transferring 项恢复为 waiting，保留完成/失败/取消记录。
  - 待完成：暂停/断线恢复、效果配置快照和真实下载恢复测试。
- [ ] **29. 顶部队列胶囊**：图标态、数量态、速度态、完成态、宽度变化、安装/回弹动画和点击入口。
- [ ] **30. 队列页面**：任务分组、缩略图、文件名、进度、错误信息、重试和清理。
- [ ] **31. 暂停与继续**：暂停当前文件/暂停后续、继续、断线恢复和按钮状态动画。
- [ ] **32. 取消与异常**：单任务/批量取消、失败重试、已存在文件、目录不可写和清理未完成输出。
  - 已修：队列页加入安卓对应文案的清空/重试确认入口；清空先撤下等待项，短动画后移除终态项；不打断正在传输项。
  - 已修：队列页改为复用照片页同一个 `DirectoryAccessStore`，设置中的目录选择会立即影响队列开始传输；根页面再统一注入设置页与照片页，跨页面目录状态不会分裂。
  - 待完成：目录不可写、断线部分文件清理和真实下载回调验证。

### F. 设置页

- [ ] **33. 设置主页面**：按钮顺序、版本号、会员状态、反馈、帮助和返回行为。
  - 已修：照片效果摘要按安卓结构显示“照片滤镜”及名称/强度、以及“边框和水印”下的边框/水印两行；连接页继续通过 `showPhotoEffectsEntry=false` 隐藏该模块。
  - 已修：iOS 主设置弹窗标题固定单行显示；传输目录、照片列表、外观和页脚按安卓分区顺序排列；布尔项统一使用 50pt `DetentWheel`，移除无动作的续费占位按钮；帮助按钮显示设置说明。
  - 已修：高级版功能尚未接入期间隐藏“检查更新”和“我要换机”入口；页脚版本改用安卓同款双圆点铭牌，只保留反馈按钮。
  - 待完成：高级版徽标/续费、检查更新、换机、主设置说明气泡和目录关注动画。
- [ ] **34. 通用拨轮组件**：`DetentWheel` 集中处理数据源、上下拖动、边界、选中态、统一字号/行高、松手提交、回弹动画和档位触感回调。
  - 实现：`ios/ZTransfer/UI/Components/DetentWheel.swift`
  - 行为依据：安卓 `ReleaseCommitWheel`，拖动预览、松手提交，选项不越界。
  - 已修：组件改为安卓同语义的 50pt 三行拨轮；超过 3 档才接管纵向拖动，拖动只预览位置、松手吸附并提交；短选项点击换档；拖动中标签淡出、档位变化触感回调、外部值变化同步。
  - 本轮重构：统一补齐 `readOnly`、圆角/强调边框、激活与长按回调、收藏星标、中心图标、多行文本、拖动提示和环境高亮参数，所有页面继续使用同一 `DetentWheel`，待真机逐项对照安卓手势细节。
  - 已修：修正 SwiftUI `ZStack` 重复居中偏移导致拨轮文字落到底部的问题；相邻档位只按行高差移动，中心值保持真实居中。
  - 已修：选项列表重建或恢复值失效时回退到第一档，避免沿用旧位置造成默认显示末档。
  - 已修：工作台边框信息拨轮补齐安卓的日期/时间格式选项，水印设置补齐“文字/图片”类型，展开内容按类型显示对应编辑控件。
  - 已修：工作台顶部下滑返回手势增加起始区域限制，不再与边框/水印/滤镜拨轮的纵向拖动冲突。
  - 待完成：长按分类选择弹窗、收藏图标/拖动提示、无障碍描述、边界与真机动画对照。
- [ ] **35. 照片效果设置**：滤镜、强度、边框、水印、曝光信息和保存/取消草稿。
  - 已修：设置页接收根页面持有的相机传输 `PhotoEffectsStore`，设置入口复用同一份传输配置；工作台保持独立配置作用域。设置保存仍通过草稿回调提交。
  - 已修：编辑器拨轮标题使用安卓文案“边框”；补齐内置滤镜名称目录，支持选择滤镜及强度，效果摘要文本与安卓一致。
  - 已修：编辑器改为可控的安卓式卡片、开关和拨轮布局，避免 iOS `Form` 产生安卓没有的系统行和控件。
  - 待完成：安卓完整分类选择、边框水印位置约束、曝光信息细节和设置弹窗动画/渲染真机对照；当前仍不能勾选。
- [ ] **36. 设置弹窗动画**：锚点展开、收起、遮罩、页面切换、外部点击、系统返回和连续开关。
  - 本轮已将连接页设置从系统底部 sheet 改为同层级全屏锚点覆盖层：保留背景、遮罩点击关闭，并按设置按钮位置计算面板顶边；当前仅完成外壳与连接页“隐藏照片效果”入口，尚未完成安卓全部弹窗内容、返回处理和真机动画验收。
  - 已修：主面板不再因标题压缩成竖排；分隔线固定为卡片内部细线，不会产生额外空白圆角容器；模拟器截图确认面板内容可滚动且底部不顶出屏幕。
  - 已修：面板高度限制为屏幕 82% 且保留底部安全留白，避免设置窗顶天立地。
- [ ] **37. 传输目录选择**：使用 iOS 目录授权能力，处理授权、重新选择、失效目录和无权限错误。
  - 已修：目录书签保存/恢复改用 iOS 可用的无选项 bookmark API，并在使用期间调用 `startAccessingSecurityScopedResource()`；旧书签失效时刷新书签。
  - 已修：根页面统一持有目录授权对象，设置选择、更改和队列/下载读取使用同一份书签状态。
  - 已修：设置页“选择目录/更改目录”按钮对齐安卓 GlassButton 的 30pt 高度、12pt 内边距和圆角样式。
  - 待完成：选择器失败/目录不可写的用户可见安卓文案、失效授权清理和下载链路联动。
- [ ] **38. 更新与授权**：检查更新、版本展示、购买/激活状态和错误提示，不暴露项目地址或源码入口。

### G. 工作台与照片效果

- [ ] **39. 本地照片入口**：照片权限、目录/相册读取、空态、拒绝权限和重新授权。
  - 已修：工作台持有 `PhotoEffectsStore(scope: .localPhotos)`，与相机传输设置分别持久化，复用编辑控件和渲染实现；空态/多选入口使用安卓文案“选择图片”“（可多选）”，替换入口使用“更换图片”。
  - 本轮结构修正：工作台改为连接页下方的纵向分页，可通过上下滑动切换；连接页底部入口与工作台返回按钮使用同一分页状态，不再以 sheet 弹出。仅完成结构落地，尚未真机验证分页手势、边界和释放生命周期，任务仍未完成。
  - 已修：生成前请求 `PHPhotoLibrary` addOnly 权限，拒绝时进入失败态；工程声明读取与写入相册用途文案。
  - 已修：非空态才显示“更换图片”；工作台说明灯泡包含安卓中文文案；预览长按临时显示未处理原图，松手恢复效果图。
  - 待完成：照片权限拒绝与重新授权的安卓样式提示、原图来源持久化和系统相册读取失败状态。
- [ ] **40. 多选模式**：进入/退出、单选、取消、全选、跨滚动选择、选择数量和底部操作栏。
  - 已修：工作台支持多张 PhotosPicker 选择、缩略图横向选择、选中角标、单项切换和“更换图片”替换整组选项；生成按钮按安卓批处理阶段显示数量文本。
  - 已修：多选预览支持原生左右分页；隔离外层纵向切页和长按对比手势，避免横向滑动被拦截。当前已通过 Xcode 模拟器编译，仍需真机实际滑动验收。
  - 待完成：全选/取消入口、跨滚动选择保持、底部栏的精确动画和真实输出结果。
- [ ] **41. 本地筛选与分类**：分类顺序、收藏、强度独立性、长按帮助和筛选动画。
- [ ] **42. 滤镜/边框/水印编辑**：预览源、渲染顺序、位置约束、旋转、文本编辑和配置快照。
  - 已修：新增独立 `PhotoEffectsRenderer`，预览按“原图→滤镜→边框→水印”顺序渲染；工作台与设置复用渲染实现，各自保留独立配置。
  - 已修：无边框时将边框专属水印位置归一到照片内位置；水印支持上/中/下与左右锚点，避免隐藏边框预设改变实际输出。
  - 待完成：安卓完整滤镜参数/目录、边框版式、旋转/长按前后对比、配置快照与原生字体资源。
- [ ] **43. 批量处理管线**：逐项输出、并发上限、原片只读、命名、重复输出隔离和缓存释放。
  - 已有：工作台按所选照片逐项进入生成状态并复用同一效果配置。
  - 已修：生成阶段逐项调用 `PHPhotoLibrary` 写入效果图，不再用仅渲染内存图冒充“已保存”。
  - 待完成：与安卓“原目录旁保存”的命名/重复隔离语义、并发上限、取消和缓存释放；当前不能视为批处理闭环。
- [ ] **44. 批量进度与取消**：进度、单项失败、重试失败项、取消清理、完成结果和返回工作台动画。
  - 已有：工作台状态模型包含 ready/generating/complete/partial/failed，并按安卓文案展示进度和结果。
  - 待完成：单项错误来源、取消清理、失败重试、真实保存结果和返回动画。
- [ ] **45. 本地大图预览**：分页、缩放、旋转、效果预览、加载占位、手势冲突和退出过渡。

### H. 遥控监看

- [ ] **46. 监看会话**：进入/退出、取帧、帧丢弃、前后台、断线和相机通道独占。
  - 已修：`PTPConstants` 补齐安卓 `RemoteLab` 的 Start/End/GetLiveView/DeviceReady/Capture 操作码；`CameraRepository` 增加忙重试、DeviceReady 轮询和增强帧→标准帧回退。`RemoteState` 先建立进入、预热、首帧、丢帧恢复、拍摄/录像、取消/断线的纯 reducer；`RemoteFrameParser` 按安卓规则从 Nikon 前后缀载荷提取 JPEG，并以状态/帧解析测试锁定边界。
  - 已修：`RemoteExposureParameters` 集中定义照片/录像属性组、兼容属性优先级和安卓读数格式化规则；测试覆盖值域选择和 EV/ISO/光圈/快门格式。
  - 已修：`RemoteViewModel` 与 `RemoteView` 已接入照片列表入口，执行可取消的连续取帧、JPEG 解码、首帧状态、丢帧重试、缩放、拍摄和退出清理；USB/Wi‑Fi 共用同一 `RemoteCameraControlling` 会话接口。
  - 已修：按安卓 `StartTracking(x,y) → AfDrive → DeviceReady` 时序接入点按对焦；仅在明确返回“不支持”时回退 `ChangeAfArea → AfDrive`，再次点按结束追踪，MF 模式忽略点按自动对焦；iOS 对焦框覆盖聚焦/成功/失败动画并延时收起。
  - 待完成：安卓完整曝光拨轮、反挤压、直方图/斑马线/水平仪、录像、横竖屏/全屏工具和真实页面前后台生命周期；当前代码可类型检查但协议/页面仍未真机闭环，不能勾选。
- [ ] **47. 曝光参数控制**：四格参数、读取/写入、乐观更新、拨轮、完整值表和错误恢复。
  - 已修：`RemoteExposureParameters` 集中维护照片/录像四格属性、跨 Nikon 属性兼容选择与读数格式；`RemoteViewModel` 已按能力读取描述、支持写入后回读确认并在失败时恢复旧值；`RemoteExposureTile` 接入安卓同向逐档上下拖动、松手吸附和只读压暗，数值点击进入相机值表。
  - 已修：监看会话页面内轮询 Nikon `LiveViewSelector(0xD1A6)`，照片/录像属性组会随机身拨杆切换重新读取；录像快门在录像命令未接入前保持禁用，避免把照片命令误发到录像模式。
  - 已修：Auto ISO 按安卓候选顺序（录像 D0AD→D16A→D054，照片 D054→D16A）探测，ISO 卡片只在相机返回可写描述时显示 AUTO 角标，写入失败回滚。
  - 待完成：曝光模式切换、设备属性事件刷新和错误文案/动画；当前 UI 还没有覆盖安卓完整的模式切换和事件轮询。
- [ ] **48. 反挤压功能**：档位、循环顺序、真实画面比例变换、持久化、选择器动画和换相机处理。
  - 已修：新增安卓同序列的 1x/1.33x/1.5x/1.8x/2x 循环与 UserDefaults 持久化，并接入监看画面比例按钮。
  - 待完成：与增强帧元数据和横竖屏布局联动的精确画面比例、选择器外观和换相机重置。
- [ ] **49. 拍照控制**：快门、倒计时、拍摄中状态、结果回传、失败和按钮反馈。
- [ ] **50. 视频录制控制**：开始/停止、录制状态、时长、异常结束和恢复。
  - 已修：接入安卓 `0x920A/0x920B` 开始/停止命令、忙重试、录像态切换和 `REC` 计时徽标；失败回到当前监看页并清理本地计时。
  - 待完成：安卓应用模式前置序列、完成/中断事件确认、异常结束恢复和录像文件回流。
- [ ] **51. 直方图与辅助显示**：直方图数据、显示开关、帧同步、性能和退出清理。
  - 已修：大图预览按钮可按安卓语义切换直方图，并从当前缩略图计算 24 档亮度数据；翻页会清理旧图数据。
  - 已修：监看页加入 24 档亮度直方图叠加、D067 水平仪读取/轮询与显示开关；开关状态按安卓监看偏好持久化。
  - 待完成：监看帧直方图、绘制样式、性能和退出清理逐项对照。
- [ ] **52. 全屏与横竖屏**：沉浸式切换、方向锁定、安全区域、控件动画和返回逻辑。

### I. GPS

- [ ] **53. BLE 设备发现与配对**：权限、扫描、连接、断线、重连和设备选择。
  - 已有：`NikonGPSBluetoothClient` 按安卓 UUID 扫描 Nikon 服务，串行发现特征、开启 pair/not1 通知、执行四消息配对并处理重连/断线；控制器 ID 写入保持 32 字节 ASCII 规则。
  - 已修：设备号和 nonce 按安卓键名语义写入 UserDefaults；后续启动会复用身份，提供清除配对入口并停止当前 BLE 会话。
  - 待完成：Classic 配对替代路径、CoreBluetooth 权限文案/系统状态和真实设备回归。
- [ ] **54. Nikon GPS 数据协议**：坐标、时间、海拔、精度、端序、校验和错误映射。
  - 已修：新增 `GPSProtocol.swift`，逐字节实现 41 字节 GEO payload、坐标方向/度分拆分、海拔符号与 UTC 时间；Nikon 四消息配对的 Blowfish/盐值计算通过安卓捕获向量校验。
  - 待完成：CoreBluetooth 设备连接、特征读写、校验/错误状态和真实 Nikon 设备验证。
- [ ] **55. 更新频率与节流**：频率拨轮、后台限制、电量策略、失败退避和持久化。
  - 已修：新增与安卓相同的 30 秒/1 分钟/2 分钟/5 分钟档位，以及位置和网络采样间隔策略。
  - 已修：`GPSCoordinator` 接入频率拨轮、UserDefaults 持久化和 GEO 写入节流；位置采样与安卓状态模型分离。
  - 待完成：后台限制、电量策略、写入确认和真机 BLE 节流验证。
- [ ] **56. GPS 页面与状态提示**：连接状态、最后更新时间、诊断信息、权限提示和动画。
  - 已修：GPS 页面显示安卓对应的状态文案、相机/坐标/海拔/精度/上次更新时间及重试入口。
  - 已修：状态摘要改为直接复用安卓 `gpsStatusText` 的中文结果（寻找相机、连接相机、等待位置更新、自动写入位置等），不再使用 iOS 自行改写的“正在启动/配对成功”等文本。
  - 已修：连接页 GPS 入口恢复安卓语义：主胶囊恒定显示“GPS”并只控制展开/收起；展开面板承载手机/相机准备说明、首次配对路径、已配对标记、频率拨轮、状态启停按钮、长按关闭、清除配对和诊断复制。
  - 已修：`GPSCoordinator` 由根页面持有，离开 GPS 页面不会意外停止已启用的定位/BLE 会话；页面按 Android `GpsStatus` 展示状态。
  - 待完成：连接帮助气泡、AP 冲突状态、诊断信息和状态动画逐项对照。

### J. 验证、性能与发布

- [ ] **57. 协议样本单元测试**：固定响应、元数据、事件、错误和三种网络链路的结果样本。
  - 已有：PTP 解析/事务、PTP/IP 封装、局域网扫描策略、GPS 配对/GEO 与监看显示/状态的 39 个 macOS Foundation 测试；已覆盖损坏包、事务错配、取消超时和安卓捕获向量。
  - 待完成：PTP 事件、Wi‑Fi 三链路样本、ImageCaptureCore 回调和真机样本。
- [ ] **58. UI 状态和动画测试**：页面状态快照、弹窗进出、列表筛选、预览翻页和取消路径。
- [ ] **59. 性能基线**：列表滚动帧率、预览首帧、大图内存、批处理耗时、主线程占用和 Instruments 记录。
- [ ] **60. USB 真机回归**：至少覆盖一台 Nikon 机身、冷启动、插拔、授权、下载大文件、断线和恢复。
- [ ] **61. 其他链路真机回归**：STA、AP、Wi‑Fi、BLE/GPS 的可行性和系统权限边界逐项记录。
- [ ] **62. iOS 发布构建**：Debug、Archive、签名、TestFlight 包、崩溃符号、隐私说明和发布检查清单。
  - 已有：`dist-debug-ios/build.command` 提供 macOS 真机 Debug 构建、签名校验、时间戳 `.app/.ipa` 产物，以及 `devicectl` / `xcdevice + ios-deploy` 安装并脱离调试会话启动；脚本语法已校验。
  - 验证：`ZTransfer-ios-debug-1.82-20260913-094147` 已通过真机 Debug 构建、签名与 IPA 校验，并安装到 iPhone 14 / iOS 16.0.3；首次启动受开发者信任阻止，信任后 USB 重新连接并单独重试脱离调试器启动成功，进程与屏幕已确认。未为重试启动重复构建。
  - 待完成：本轮布局的真机复验、整条脚本无人工干预的启动验证、Archive/TestFlight、发布签名和最终隐私检查；不能由一次安装推断全部功能通过。

## 每个任务的完成记录格式

完成任务时在对应项目下追加：

```text
实现：<主要类型/文件>
行为依据：<安卓文件、函数或测试样本>
动画/交互：<安卓依据；时长、延迟、曲线/弹簧、交错顺序、手势、连续触发及中断/恢复/关闭规则>
差异：<无；或用户已确认的平台差异及原因，未确认的差异保持待讨论>
验证：<单元测试、模拟数据或真机结果>
提交：<commit>
```

## 当前进度

### 2026-09-13 边框逐项复刻进展（进行中）

- 已对照安卓 `PhotoFrameExporter.kt` 的标准四种边框、铭牌、沉浸、品牌铭牌/画廊、经典签名、画廊衬纸、彩色档案、胶片画廊和胶片边缘分支，重排 iOS `PhotoEffectsRenderer.swift` 的画布比例、信息行顺序、边框水印归属、胶片装饰和调色板采样。
- 新增 EXIF GPS 坐标/海拔解析，并按安卓的四位小数坐标、方向字母和米制海拔格式进入边框信息行；日期/时间显隐与格式在渲染前统一解析。
- 已通过一次无签名 iOS 真机构建；最终真机安装、每个边框的视觉截图对照和导出回归仍待完成，当前不将这部分标记为已完成任务。
- 本轮签名脚本构建成功并安装启动真机：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-130810.app`；仍需在设备上逐个切换预设做视觉截图回归。
- 随后修正画廊衬纸/胶片画廊的绘制层级（外框先于照片），并再次签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-131503.app`。
- 继续对照安卓几何规则，修正经典签名、画廊衬纸、胶片画廊、胶片边缘的方形照片层，并补回经典签名细边框；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-132411.app`。
- 品牌铭牌/画廊进一步按安卓的黑色细边框、品牌黑体与压缩斜体信息行、底部水印顺序和自适应颜色修正；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-132922.app`。
- 沉浸边框标题改为相机标题、分隔线、AUTO 水印三个独立组件；标准与经典边框的品牌/型号及 F、快门、ISO 文本格式统一到安卓；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-133739.app`。
- 编辑类边框的信息行改用安卓相同的区域宽度字号、真实字形高度和空间不足时整体缩放规则；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-133943.app`。
- 铭牌、品牌和编辑边框的边距/条带尺寸改为先按源照片像素取整，消除半像素布局偏移；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-134301.app`。
- 沉浸专用 `f/` 文本节奏与各预设水印圆角裁剪规则完成拆分；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-134822.app`。
- 胶片边缘改为安卓独立的橙色压缩字体、`0.016` 照片宽度字号、垂直间距和正确旋转基线；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-135112.app`。
- 彩色档案改用安卓专用的焦段/光圈/ISO/快门顺序与 `f/` 小写格式；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-135538.app`。
- 雾面、影院、胶片画廊补齐安卓对应的底部渐变压暗层及 alpha；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-135737.app`。
- 彩色档案调色板改为最近邻采样并跳过 alpha 小于 128 的像素；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-140002.app`。
- 标准边框信息超出安全区时加入安卓一致的 2% 抗锯齿缩放余量；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-140433.app`。
- 沉浸边框调整为先绘制照片水印、再绘制底部信息文字，与安卓装饰层级一致；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-140700.app`。
- 照片 elevation 按安卓 `drawPhotoElevation` 重做为四分之一尺寸透明代理上的两层阴影，使用短边比例、逐预设强度和安全的透明合成；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-141404.app`。
- 编辑类边框的照片水印绘制顺序调整为先于胶片孔、调色板和信息文字，和安卓 `drawEditorialFrameDecoration` 一致；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-142131.app`。
- 雾面、影院、磨砂和胶片画廊的背景代理改为安卓同款 192px 两阶段滑动窗口盒式模糊，避免 Core Image 高斯近似造成背景质感偏差；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-143100.app`。
- 修复单选项拨轮（包括“边框信息”）在 iOS 16 下透明点击层可能塌缩的问题，点击区域现在明确铺满控件；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-144429.app`。
- 单选项拨轮改用高优先级点击手势，避免透明按钮与工作台滚动手势竞争，确保“边框信息”点击触发展开；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-144704.app`。
- 按安卓整数色值校正雾白、暗夜、简白基础背景和磨砂/胶片暗部处理；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-145351.app`。
- 按截图对齐毛玻璃圆角的安卓信息区比例，并保留铭牌 EXIF 原始品牌大写规则（如 `NIKON CORPORATION`）；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-150416.app`。
- 沉浸边框的标题、镜头和参数文字改用安卓短边比例（0.030、0.0205、0.0235），保持竖图中的字号层级；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-150554.app`。
- 简白标准信息行按安卓字号层级、标题/镜头/参数最大宽度和统一缩放规则排版，修正长文本导致的视觉偏移；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-150807.app`。
- 简白信息区主色与次级灰色改用安卓原始 RGB 值，保持照片下方文字对比度一致；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-151119.app`。
- 简白信息行改用 CoreText 实际字形边界计算基线，按安卓 `getTextBounds`、6% 上下留白和可压缩行距规则垂直居中，避免 UIKit 字体行高造成标题/参数/水印偏移；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-151558.app`。
- 简白边框水印改用安卓对应字体、最大宽度和自适应 RGB 颜色（含默认透明度），不再用通用系统字体替代；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-151926.app`。
- 将安卓 `great_vibes_regular.ttf`、`bebas_neue_regular.ttf`、`cormorant_garamond_medium_italic.ttf` 加入 iOS 资源并注册，补回 `CFBundleShortVersionString`/构建号继承，确保字体生效且构建产物保持 1.82；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-152547.app`。
- 简白回归标准边框共用路径：复用安卓标准圆角、暖白背景、半透明白色照片边缘和信息排版；针对 CoreGraphics 在 iOS 上阴影扩散更宽的问题，改为主画布上的窄接触阴影，避免照片下方形成灰色圆角块；签名构建安装启动：`dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-155610.app`。

**2/62**。已验收工程骨架和 Swift 6/iOS 16 配置。其余 60 项保留为待完成或进行中。

2026-09-13 纠正原来的 `10/62`、`11/62` 混用：旧记录将接口存在、模拟器编译成功或顺序调用的测试误当成功能闭环，已撤回任务 5–12、34 的完成标记。代码没有删除；具体已做部分、缺陷和验证证据记录在对应任务下，不把同一局部修复再计成一个整项完成。

### 本轮已验证的协议修复

- 生产代码：`PTPCodec.swift`、`PTPDataset.swift`、`PTPSession.swift`、`AsyncDeadline.swift`。
- 系统适配与纯协议拆开：`SelectedUSBPTPTransport.swift` 放在 USB 目录；新增 `ios/Package.swift` 只编译生产协议源码，测试与 Xcode 测试 target 复用同一份 `ios/ProtocolTests/`。
- 命令：`swift test --package-path ios --scratch-path /tmp/ztransfer-protocol-tests`。
- 结果：**40 tests，0 failures**（PTP/PTP-IP/扫描策略 28 + GPS 2 + 监看状态/帧解析 4 + 曝光/显示参数 5 + 对焦状态 1）；这是 macOS 上的 Foundation 单元测试，不是 App 构建、模拟器测试或真机验收。
- 删除原有错误 DeviceInfo fixture 和“顺序调用证明串行”的测试；其他领域模型旧测试移至 `DomainModelTests.swift`，本轮未运行，不能据此声称领域模型与安卓一致。
- USB 监听改由 `RootView` 持有，修正进入照片列表时连接页 `onDisappear` 停掉监听并清空相机的问题；连接代际校验阻止拔线后旧回调覆盖等待态。新增 `ClassicUSBIcon`，几何对齐安卓自绘三叉图标。SwiftUI 改动尚未运行 App 验证。
- XcodeGen 已重新生成工程文件；未构建/安装 App，未提交/推送。
- 本轮修复了 iOS 16 Swift 6 的工程阻断项：目录 bookmark 改用 iOS 可用 API、统一旧式 `onChange`、修正根页面效果配置注入与列表类型检查；新增 `PhotoEffectsRenderer` 和工作台批处理阶段/安卓文案草稿。全量 iOS 源码类型检查通过（仅 ImageCaptureCore 非 Sendable 警告），尚未构建/安装 App。

### 其他模块当前实际状态

- 连接 reducer、USB 重试和根页面切换已写出草稿；连接服务已串行化握手任务、传播取消，并在独立任务中限时清理旧会话；设备对象身份、断线保留列表、精确文案与 Wi-Fi 链路尚未闭环。
- 照片列表/筛选/连拍/预览已有界面和模型草稿；未知日期顺序、筛选数据来源、连拍显隐/批量操作、手势和动画仍待完整对照。
- 队列已有状态和串行下载草稿；本轮已修正手动重复入队、自动按 `文件名|大小|拍摄时间` 去重、重试生成新任务 ID、开始传输不重置暂停，以及已有文件按名称（含安卓副本后缀）和大小判断。生产队列已通过 Swift 6 类型检查；Effects 快照、目录索引、断线恢复、页面胶囊动画仍未闭环。
- 照片效果和工作台只建立部分设置、导入和选择状态；真实滤镜、渲染、元数据配置、批处理及交互动画未完成，不能当作功能已实现。
- GPS 只有 Core Location 和页面草稿；Nikon BLE 配对、协议写入和节流尚未完成。
- 监看、STA/AP、完整动画和性能验收仍未完成。原生 Form/sheet、临时按钮和系统默认转场需要逐项替换或验证，不能视为安卓复刻结果。

### 下一步顺序

1. 完成 USB 会话生命周期：超时/取消收尾、设备身份和旧回调失效、关闭后重连，先用可控 transport 测试验证。
2. 统一 USB 媒体 I/O 调度与缓存身份，补齐扫描、缩略图和下载的安卓错误/取消/回退规则。
3. 在可靠数据流基础上依次完成连接页、照片列表/预览、队列、设置、工作台、监看、GPS 与 STA/AP；每项先读安卓，再实现，再验收。
4. App 构建按当前用户授权在必要时执行，避免无意义的重复打包。

### 2026-09-13 设置与拨轮适配

- `DetentWheel` 使用深浅色自适应材质、边框和文字透明度；短列表仍把触摸交给页面翻页，长列表保持拖动拨轮。
- “照片列表操作”按安卓两行高度显示“点击/长按”完整文案，禁止省略号。
- 语言拨轮使用与安卓一致的 `system`、`en`、`zh-Hans`、`zh-Hant` 标识，切换后立即更新根环境 Locale 并关闭设置窗；拨轮标签和值同步翻译。
- 共用按钮样式读取 `skinPreset`，毛玻璃、木纹、相机按键、钛合金切换会同步影响连接页和工作台入口材质。
- 设置遮罩提升到连接页最外层覆盖安全区，状态栏和底部指示区域不再漏出未遮罩的带状区域。
- 设置面板改用接近安卓 `glassSurfaceHeavy` 的不透明面板材质，避免底层连接页文字穿透；状态栏和 Home 指示区由 UIKit 系统栏遮罩同步覆盖。
- 版本铭牌使用同一语言环境显示产品名；USB transport 复核了打开/关闭会话取消收尾、重插替换、缩略图等待隔离和 ready 事件过滤。

### 2026-09-13 STA 无线链路对照实现（进行中）

- 已按安卓 `NikonCamera.connectSta`、`CameraViewModel.tryConnectStaCandidate`、`StaCameraProfileStore` 和 `RemoteLab.rcPollEvents` 对齐 iOS：稳定双身份、响应 GUID 校验、配对标记、配对事件等待、配对后 6.6 秒重连、就绪重试、候选地址延后尝试、连接代际取消和断线重连。
- 已对齐 STA 浏览初始化顺序：OpenSession(transaction 0)、0x941C、GetStorageIDs、精确配对能力集判断、GetObjectHandles 三点校验、ObjectInfo 被拒时 Thumb/Size/PartialObjectEx 直接读取回退、ChangeApplicationMode 探索及失败回滚；相册进入前必须同时满足配对标记和相册访问验证。
- 已对齐后台行为：事件通道 PING/PONG、ObjectAdded/ObjectRemoved、GetEventEx→GetEvent 回退、2 秒事件轮询、10 秒 handles 对账、DeviceBusy 750ms×2、空闲 GetStorageIDs 保活，以及直接读取模式下的缩略图/预览/元数据/文件名日期回退。
- 连接页 STA 的指引、帮助弹层、重置配对、热点设置和状态文案均从安卓多语言资源读取；iOS 热点设置使用系统设置入口作为平台等价动作，不新增业务文案。
- 验证：`ZTRANSFER_PROTOCOL_ONLY=1 swift test --package-path ios`，60 tests、0 failures；全量 iOS Swift 6/iOS 16 simulator 类型检查通过，仅保留既有 `Np3BitmapFilter.swift` 非 Sendable 警告。尚未进行 STA 真机链路验收，未将总任务标记完成。

### 2026-09-13 照片列表顶栏挂载修正（进行中）

- 安卓 `FileListScreen` 的悬浮顶栏按源码核对为：双 Z 设置入口、信号按钮（可展开信号详情）、类型筛选入口，以及由宿主持有的传输执行按钮和队列胶囊；照片预览时整组按安卓规则退场。
- iOS 原先把控件写在 `.toolbar` 中但页面没有 `NavigationStack`，导致真机照片列表只显示内容、所有顶栏控件都被 SwiftUI 丢弃。现改为页面内悬浮顶栏，内容顶部留白与安卓 `topInset + 54.dp` 策略对齐，设置入口连接到已有 `SettingsPopupOverlay`，筛选/队列动作沿已有模型执行。
- 验证：`xcodebuild ... -sdk iphonesimulator ... build` 成功；Debug 真机脚本生成并安装 `dist-debug-ios/ZTransfer-ios-debug-1.82-20260913-211431.app`，镜像确认连接页重启正常。需重新连接 STA 后继续核对照片页顶栏与队列状态动画。
- 构建脚本验证：`dist-debug-ios/build.command` 已成功签名、通过局域网 CoreDevice 安装并启动 `ZTransfer-ios-debug-1.82-20260913-210322.app`；脚本现在接受已配对但尚未建立隧道的无线设备，并在无设备时跳过安装而不再无限等待。

### 2026-09-13 照片列表扫描/缓存链路继续对齐（进行中）

- `CameraRepository.scanCatalog` 已拆出安卓同等的扫描结果与可恢复快照：按 StorageIDs → 每卡 ObjectHandles → 每卡反转为新到旧 → 12 条元数据批次；回调完成后才继续读取下一批。
- 增加 `preserveExisting`、`detectNewHandles`、已处理 handle 集合和完整扫描标志；刷新时只删除完整 handles 快照确认缺失的对象，取消/断线保留未完成快照，避免把失败误判为空卡。
- `PhotoListViewModel` 刷新改为保留现有行并应用 added/removed 差量；每批发布后逐项预取，结果进入 `PhotoThumbnailFillQueue` 的 pending/failed/settled 集合。
- 缩略图缓存补齐 STA direct 的 `sta + handle + size` 键、标准键迁移、相机目录隔离、90 天清理和仅完整权威扫描后的 reconcile；后台读取增加单 permit gate，避免并发淹没 PTP 通道。
- 验证：iOS Simulator Debug `xcodebuild ... build` 成功；`ZTRANSFER_PROTOCOL_ONLY=1 swift test --package-path ios` 通过 60 tests、0 failures。此前全量 Xcode 测试 target 被 `PTPIPDiscoveryTests.swift` 的旧模块引用阻断，已补齐 `SWIFT_PACKAGE` 条件导入并修正队列测试的共享偏好污染；2026-09-14 在 iPhone 17 Pro 模拟器执行 `xcodebuild test`，125 tests、0 failures。未据此声称真机扫描、缓存命中率或取消时序已验收。
- 仍待补齐：效果预览页面的数据源与渲染入口尚未接入 iOS 设置页；真实相机断线恢复和扫描/缓存时序仍需回归。
- 本轮新增 `CameraRepository.setFHDActive`：扫描在下一条元数据读取前让出通道，预览结束后继续同一快照；`PhotoPreviewView` 在 FHD/EXIF 任务期间负责成对设置和释放该状态。RAW/视频的 STA direct 预取已改为惰性处理。上述行为已通过模拟器编译，仍需真机时序验证。
- 完整扫描进入 loaded 后，iOS 以后台补漏任务仅对缓存未命中的文件再次走同一预取入口；命中项只做磁盘索引检查，失败项进入队列 failed 集合，外部重试再重新入队，避免扫描阶段的瞬时失败造成永久缺图。
- 队列在筛选日期范围变化时按范围重排并唤醒失败项；相机事件产生新文件时插入队首并启动 worker，删除事件同步移除 pending/failed/settled，和安卓的 wake/retry/remove 分支保持同样的触发点。
- 多卡读取已改为每卡保持一个 metadata head，按 captureDate 从新到旧动态选择下一项；列表顺序由扫描顺序数组保存，不再从 Dictionary 随机值恢复，刷新和事件更新也保持原有显示顺序。
- `CameraFile` 新增兼容的 `storageIDs` 归属集合，旧持久化数据缺少该字段时回退为单一 `storageID`；双卡同名/同大小/同拍摄时间对象合并归属，卡槽筛选和卡槽选项读取集合，避免误删逻辑照片。
- 修正权威空卡/拔卡刷新：完整 handles 快照确认移除时同时从稳定顺序、显示行和索引表删除，失败或中断的查询仍不会清空旧列表。
- ObjectHandles 查询现在保留响应级失败状态：STA 按安卓 750ms、最多 3 次 DeviceBusy 重试并在仍失败时交给重连；非 STA 保留已显示列表、将本轮标为不完整并跳过权威缓存对账，不把一次失败误判为新空卡。
- StorageIDs 的非 STA 异常与安卓一致映射为空结果，进入“无可用存储”完成分支；该分支清除已确认的旧句柄但返回不可对账标志，避免把异常响应当成可授权的空相机缓存。
- 预览抢占已接入列表生命周期：打开 FHD 预览时取消当前 metadata scan、保留已发布文件和 snapshot；预览关闭后按原有 `preserveExisting + resumeSnapshot` 继续，不重新从头枚举。
- 新扫描会清空 iOS 内存缩略图与负缓存但保留磁盘缓存；后台填充和批次预取在远程、前台读取或 FHD 状态激活时让路，避免把扫描阶段误判为可并行填充。
- EXIF 已按安卓 `loadExif` 接入独立会话缓存：键为 `fileName_size_captureDate`，null、非图片扩展名和确认解析失败进入负缓存；JPEG 读取 128 KiB，NEF/NRW/TIFF 读取最多 2 MiB。STA direct 通过 `STAObjectReader` 的 bounded prefix 缓存复用头部读取；光圈 APEX 回退、快门/ISO/曝光补偿/焦距/日期/GPS 的格式与安卓字段顺序对齐。传输错误和取消不会写入负缓存。
- 照片列表在传输队列进入 `isTransferring` 时暂停批次预取和后台填充，传输结束、远程监看退出或预览退出时重新唤醒队列；对应 Android 的 `transfersBusyFlow`/远程状态组合门控。
- 本轮继续对齐加载策略：普通连接的 StorageID 过滤改为丢弃低 16 位为 0 的槽位，STA 仅丢弃 `0/-1`；无可用存储时仅在存在已建立句柄基线时移除缺失项，未建立基线的已发布部分保留。扫描发现新增媒体后按安卓 `FORMAT_EXT` 扩展名过滤，并以 `fileName|size|captureDate` 去重，接入自动传输开关、目录条件和 `deferTransferStart` 启动策略。
- 验证：iOS Simulator Debug `xcodebuild ... build` 成功，`git diff --check` 通过；真实相机的 StorageID 异常、自动传输和断线恢复仍未验收。
- 远程监看取得相机通道时，扫描不再等待或继续旧快照，而是中止当前元数据扫描；监看关闭后重新枚举 handles，以覆盖监看期间新增照片，和安卓远程退出后的刷新策略一致。
- 持久化键继续按安卓定义收敛：相机照片效果从 iOS 私有 JSON 优先改为读取/写回 `ztransfer` 中的 `photo_frame_*`、`photo_filter_*`、`favorite_*` 和 `photo_frame_metadata_settings_v1`；保留旧 JSON 仅作升级回退。传输目录 bookmark 改用安卓同名 `transfer_dir` 键，远程变形使用 `remote_desqueeze_multiplier`；监看直方图对应安卓 `RemoteScreen` 的会话状态，不能持久化到 `preview_histogram_enabled`，水平仪同样保持会话态。
- 缩略图填充队列补上安卓 `seededRevision` 和日期优先级状态：同一扫描代际只播种一次，筛选日期变化才重排未完成项，避免批次回调和扫描收尾重复插入或改变顺序。
- 本轮仅完成代码级对照和模拟器构建；Android preference 编码/解码尚未用跨平台 fixture 做字节级回归，GPS/远程界面仍需按安卓页面源码和模拟器交互继续核对。
- 手机照片效果也改用独立的 `local_photo_effects` UserDefaults suite，并使用安卓 `decoration_enabled`、`frame_preset`、`watermark_*`、`filter_*` 和 favorite 键；筛选页、队列页、GPS 页的可见文案继续从 AndroidLocalization 的资源键读取。
- 照片效果编辑器切换边框预设时同步切换该预设的元数据草稿；元数据开关写入对应预设映射后再按安卓 12 字段 `|` 编码保存，避免退出编辑器后丢失设置。
- 设置页、筛选页、队列页、连接页和共用拨轮中剩余的可见操作文案已改为 AndroidLocalization 资源键；照片列表操作的两行内容使用安卓 `tap_transfer_hold_preview` / `tap_preview_hold_transfer` 原文，语言选项保留安卓 SettingsScreen 的四个原始标签。
- 补齐资源表中遗漏的四个边框名称键（经典签名、艺术装裱、胶片画廊、胶片边框），并按 Android 资源缺失繁体覆盖时的默认英文回退记录，避免拨轮在切换到这些预设时直接显示资源键名。

### 2026-09-14 测试 target 解阻与照片队列回归

- `ios/ProtocolTests/PTPIPDiscoveryTests.swift` 补齐 `SWIFT_PACKAGE`/Xcode 条件导入，Xcode 测试 target 现在直接复用 App 模块，不再引用不存在的 `ZTransferProtocol` 二进制模块。
- `ios/ZTransferTests/DomainModelTests.swift` 的队列测试在执行前清理并在结束后恢复 `transferQueue.items.v1`，避免共享标准偏好中的历史任务污染断言；生产队列的持久化实现未改变。
- 验证：`xcodebuild test -project ios/ZTransfer.xcodeproj -scheme ZTransfer -destination 'platform=iOS Simulator,id=CA046456-B859-45F4-9CB3-2C6E2F8E03B0' CODE_SIGNING_ALLOWED=NO`，125 tests、0 failures。

### 2026-09-14 安卓资源文案对账

- `TransferQueue` 的目录失效、相机连接中断/超时、照片元数据不可读提示改为直接读取安卓资源键；日期筛选拨轮的年/月/日标签也改为安卓资源键，覆盖系统、英文和繁体语言切换。
- 验证：iOS Simulator Debug `xcodebuild` 构建成功；未将真机连接错误分支标记为已验收。

### 2026-09-14 文案资源引用收敛

- `RemoteViewModel` 的监看连接失败提示改为安卓 `connection_failed_short` 资源键；传输队列错误和日期拨轮标签同样改为 `AppLocalized.resource`，不再在 iOS 代码中维护同义文本。
- 验证：iOS Simulator Debug `xcodebuild` 构建成功；真实远程监看异常路径仍待相机验收。

### 2026-09-14 远程监看元数据接入 UI 基础

- `RemoteView` 现在消费与 JPEG 同帧发布的安卓扩展帧元数据：AF 选中框按归一化坐标绘制，录像模式下按安卓 15 段双声道电平显示开关，构图参考线按 `OFF → THIRDS → FOURTHS → OFF` 循环；音频开关沿用安卓 `remote_audio_levels_visible` 持久化键，网格/直方图/水平仪保持进页会话状态。
- 验证：`xcodebuild test` 执行 127 tests、0 failures；仅验证解析、状态和渲染输入，尚未以真实 0x9428 相机帧验收视觉位置和动画时序。

### 2026-09-14 斑马纹输入与取景器叠加

- 远程监看按安卓 95 IRE、120×80 中心采样、250ms 节流规则增加 iOS 斑马掩码；斜线绘制只裁剪到过曝单元，构图网格也只落在实际 Fit 图片区域，不覆盖上下黑边。
- 验证：iOS Simulator Debug `xcodebuild` 构建成功；尚未以真实相机画面核对像素格式、斑马位置和控件动画。

### 2026-09-14 远程录制失败文案

- `RemoteRecordingHint` 的开始/停止失败主文案改为直接读取安卓 `remote_rec_start_failed`、`remote_rec_stop_failed`，仅保留安卓同样的协议诊断附行和显示时长。
- 验证：Xcode 测试 127 项、0 failures；未将相机录制错误回调标记为真机完成。

### 2026-09-14 GPS 详情资源对账

- `GPSView` 的相机名称和海拔详情改为直接读取安卓 `gps_camera_label`、`gps_altitude_value` 资源，海拔数值按安卓整数米格式化，覆盖简体、英文和繁体；不再在 Swift 中拼接平台自定义单位文案。
- 验证：iOS Simulator Debug 构建成功；GPS 页面当前仍未完成安卓完整详情层和真实定位链路验收。

### 2026-09-14 远程监看扩展帧元数据

- 按安卓 `LiveViewMetadata.kt` 增加 iOS 0x9428 帧头解析：校验头版本/长度/JPEG 边界，解析完整 AF 框表、选中框归一化坐标、对焦判断、焦点坐标网格，以及 512/1024 头型对应的双声道 15 段音频电平。
- `RemoteViewModel` 与 JPEG 同步发布 `frameMetadata`，停止监看时清理，未知头型或越界字段不生成伪数据。
- 验证：`xcodebuild test` 125 tests、0 failures；新增元数据解析覆盖通过。远程监看 UI 控件和真实相机帧仍未完成验收。

### 2026-09-14 远程监看 FPS 覆盖

- 安卓 `RemoteScreen` 的 `showFps` 默认值为 `true`，取景器右下角按 `%.1f fps`、10sp 等宽字体、黑色 0.5 背景和 8dp 圆角显示；iOS 增加同一会话态开关和覆盖层，关闭后不再绘制该信息。
- 验证：待本轮模拟器构建完成；真实监看帧率位置和字体仍需设备画面核对。

### 2026-09-14 远程监看模式徽标

- 安卓 `RemoteViewfinderPanel` 在取景器左上角显示当前照片/录像模式和对焦模式；iOS 现在使用 `liveViewSelector` 与 `focusMode` 的同一协议格式，仅在 `liveViewStable` 时绘制相同的黑底白字徽标，避免加载阶段显示伪状态。
- 验证：Xcode Simulator 测试 127 项、0 failures；真实相机模式切换和徽标位置仍待设备画面核对。

### 2026-09-14 远程监看清晰度选择

- 安卓 `hdLiveView` 默认关闭，FPS 工具旁的 HD 按钮切换后只改变后续取帧使用的增强/标准操作；iOS 改为同样的会话态默认值和取帧参数，不再默认强制增强帧。
- 验证：Xcode Simulator 测试 127 项、0 failures；真实机身对两种取帧操作的响应仍需设备核对。

### 2026-09-14 远程监看工具可访问名称

- 监看工具按钮补齐安卓对应的无障碍资源名称：XGA、FPS、直方图、水平仪、构图参考线、音频电平和斑马纹；变形倍率按钮不新增安卓没有的资源文案。
- 验证：Xcode Simulator 测试 127 项、0 failures；未改变工具的视觉文字和业务状态。

### 2026-09-14 照片预览显示偏好持久化

- 安卓 `TransferViewModel` 使用 `preview_rotation_quarter_turns` 保存预览四档旋转方向，并用 `preview_histogram_enabled` 保存直方图开关；iOS 预览现在读取和写回同名键，重新进入或重启后恢复相同显示状态。
- 验证：Xcode Simulator 测试 127 项、0 failures；未进行跨进程真实偏好迁移验收。

### 2026-09-14 照片预览缩略图优先

- 安卓 `PreviewPage` 先复用可见缩略图，再在 FHD 到位后以 180ms 曲线淡入高清图；iOS 预览现在同时请求缓存/相机缩略图和高清预览，先发布缩略图并按同一时长替换，高清任务期间继续持有 FHD 活跃标记让扫描让路。
- 验证：Xcode Simulator 测试 127 项、0 failures；真实相机缩略图命中和 FHD 到达顺序仍需设备核对。

### 2026-09-14 视频预览交互

- 安卓 `PreviewPage` 对视频禁用缩放和拖动，并在缩略图上显示 `video_no_preview`；iOS 现在按 `.mov/.mp4` 扩展名关闭同一组手势并复用安卓提示资源。
- 验证：Xcode Simulator 测试 127 项、0 failures；视频真实缩略图和提示位置仍需设备画面核对。

### 2026-09-14 监看录制指示动画

- 安卓录制徽标红点使用 600ms、0.3 到 1.0 的往返 FastOutSlowIn 动画；iOS 录制计时条的红点现在使用同一时长、透明度范围和曲线循环，不影响录制状态机。
- 验证：Xcode Simulator 测试 127 项、0 failures；真实录制链路和帧率仍需相机核对。

### 2026-09-14 预览文件名适配策略

- 安卓 `PreviewInfoText` 对文件名采用测量后缩小字号，避免直接用省略号；iOS 文件名标题增加同样的缩小优先策略和紧缩排版，保留安卓允许的可见文本。
- 验证：Xcode Simulator 测试 127 项、0 failures；超长文件名在真实设备字体缩放下仍需画面核对。

### 2026-09-14 GPS 展开层动画

- 安卓 `GpsDetailOverflowLayer` 的 GPS 详情层使用顶部锚定缩放/淡入淡出，展开主时长 260ms、收起 220ms，并在溢出层中保持邻卡测量不变；iOS 已移除单纯 opacity 过渡，采用同一锚点和缩放比例。
- 验证：Xcode Simulator 测试 127 项、0 failures；GPS 真机动画帧率和系统安全区仍需设备核对。

### 2026-09-14 HD 监看会话重启顺序

- 安卓切换 `hdLiveView` 会取消并等待旧监看任务、结束 LV，再写入 0xD1AC 并启动新会话；iOS 的 HD 操作现在按同一顺序串行重启，避免在旧会话仍占用时写分辨率。
- 验证：Xcode Simulator 测试 127 项、0 failures；真实相机的结束/启动响应和失败重试仍需设备核对。

### 2026-09-14 照片列表连接类型图标

- 安卓照片列表顶栏的 `SignalPill` 按当前连接类型显示 USB 或 Wi‑Fi；iOS `CameraSession` 现在保留不可变的传输类型，顶栏 USB 会话显示 USB 图标，网络会话显示 Wi‑Fi 图标。
- 验证：Xcode Simulator 测试 127 项、0 failures；未连接真实 USB 相机核对图标资源和 RSSI 展开状态。

### 2026-09-14 设置帮助浮层对账（进行中）

- 安卓 `SettingsScreen` 的灯泡入口使用 `MainSettingsInfoBubble` 通过 `AnchorPopup` 锚定在灯泡按钮下方，内容按“按拍摄日期分文件夹保存 / 连接期间新增媒体自动加入 / 延迟传输 / 连拍合集”四组标签与说明排列；它不是系统警告框，也不改变设置页状态。
- iOS `SettingsView` 已移除原先的系统 `alert`，改为面板内锚定浮层：按钮可切换显示，浮层点击外部收起，使用相同四组安卓资源文案、顺序、标题、顶部锚点和 240ms 淡入缩放过渡。设置页其它控件不因帮助浮层重建。
- 验证：iOS 模拟器 Debug 构建成功；`xcodebuild test` 执行 127 tests、0 failures。尚未在真机逐帧核对浮层坐标与系统安全区，任务保持未完成。
