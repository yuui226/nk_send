# 安卓照片效果编辑器复刻基线

本文件记录设置照片效果页与本地工作台的**编辑控件、配置边界和验证状态**。渲染器与相机预览源仍需单独完成；不得把控件接入视为整页验收。

## 事实来源

| 安卓位置 | 必须保留的行为 | iOS 对应位置 |
| --- | --- | --- |
| `ui/screen/SettingsScreen.kt:1528` `PhotoFilterEditor` | 无滤镜 + 收藏优先的目录；切换按目标滤镜取记忆强度；长按分类；强度只在启用且存在选择时可调 | `UI/PhotoEffectsEditorView.swift` `PhotoEffectsControls.filterCard` |
| `ui/screen/SettingsScreen.kt:1913` `PhotoFrameWatermarkEditor` | 边框行、内嵌边框信息、同一边框卡中的紫色水印区域；按边框开关/水印开关控制展开 | 同文件 `frameCard` |
| `ui/screen/LocalPhotoEffectsOverlay.kt:341/373` | 工作台直接调用上述两个编辑器，配置独立于相机传输；不显示位置字段 | `UI/LocalPhotoEffectsView.swift`，`PhotoEffectsStore.Scope.localPhotos` |
| `ui/screen/SettingsScreen.kt:329–416/792–880` | 活动滤镜/边框/水印采用草稿；返回、关闭提交；强度记忆、收藏、元数据即时保存 | `UI/SettingsView.swift`，`SettingsPopupOverlay.swift`，`PhotoEffectsSettings.persistingEditorPreferences` |
| `effects/PhotoEffectFavorites.kt` | 收藏按添加顺序；取消后再收藏移到收藏末尾；收藏不保存历史文本/图片身份 | `Domain/PhotoEffects.swift` |
| `filter/PhotoFilterCategory.kt` | 八个分类名称、顺序、目录名称匹配规则和收藏排序 | `PhotoEffectFilterCategory`、`PhotoFilterChooserOverlay` |
| `frame/PhotoFrameMetadataSettings.kt` | 元数据默认值、日期/时间格式列表和示例 | `PhotoFrameMetadataSettings`、共享编辑器 |
| `frame/PhotoFrameExporter.kt:174–270` | 水印尺寸 1…300，透明度 1…100，步长 1；渲染使用尺寸 +49 的分段函数 | `PhotoFrameWatermark`、`PhotoEffectsRenderer` |
| `effects/PhotoEffectFavoritesTest.kt` | 收藏编解码、顺序、重复/坏记录处理、当前内容身份、缺失 Logo 拒绝应用 | `ZTransferTests/DomainModelTests.swift` |

以上安卓路径均相对于 `app/src/main/java/com/ztransfer/`；测试相对于 `app/src/test/java/com/ztransfer/`。

## 编辑布局与输入规则

1. 滤镜卡：滤镜名称拨轮、强度拨轮、收藏方钮。两个拨轮分享扣除方钮与间距后的宽度，权重 4:3；方钮和拨轮高 50，横向间距 8。
2. 边框卡：边框预设拨轮、边框信息动作拨轮、收藏方钮，同一套 4:3:固定 50 布局。动作拨轮没有额外左上标签或拖动提示。
3. 边框信息在边框行下展开。第一行：焦距、曝光、镜头；第二行：品牌、型号、坐标（设置页）；第三行：海拔（设置页）。工作台隐藏坐标/海拔，不能改变独立配置中的其他字段。
4. 日期/时间拨轮在信息字段下面，分别包含关闭项和安卓四个格式。示例时间固定使用安卓的 `2026-08-17 14:32:08`。
5. 水印区域嵌在边框卡内，外层紫色填充 alpha 0.055、描边 alpha 0.18、圆角 12、内边距 8。关闭设置展开区时仍保留这一区域及其开关和设置按钮。
6. 水印展开首行：类型占可用宽度的 1/3，文本输入框或“更换 Logo”按钮占剩余 2/3。文本下面两行分别是字体/大小/透明度、位置/颜色/效果；图片下面是大小/透明度/照片内位置。
7. 文本输入过程中允许空串；结束编辑才将空白内容恢复为安卓默认水印。失焦/完成提交，不能每次输入一个字都提交相机传输活动配置。
8. 无边框或图片水印只展示照片内位置；临时显示的有效位置不能覆盖用户偏好位置。高级版/免费版有效水印仍需接入完整授权状态，不得凭控件存在声称完成。
9. 分类选择弹窗独立覆盖整个宿主，不能放在可滚动卡片内部遭裁切。宽 280，外部留 24；分类列宽 84，两列独立滚动；内容最大高度按 `(屏高 - 160).coerceIn(360, 440)`；行间距为分类 2、滤镜 4，行最小高 40。
10. 收藏图标与按钮复用同一实现：图标 22，按钮 50，圆角 13；收藏颜色依据按钮材质。入场缩放 0.55/190ms、淡入 150ms；退场缩放至 0.72/130ms、淡出 110ms；颜色 180ms。

## 配置与生命周期规则

- **本地工作台**：独立 `local_photo_effects` 偏好域；编辑变化持续写回自己的配置，不能影响传输配置。
- **相机传输设置**：打开效果页复制当前配置；切换活动滤镜、边框、普通水印样式先改变草稿。返回主设置或点外部遮罩关闭均提交草稿。
- **即时写入项**：滤镜强度记忆、滤镜收藏、边框收藏、每预设元数据即时写入；写入时不能连带改变当前生效的滤镜/边框选择。
- **水印文本/Logo**：安卓在文本结束编辑及 Logo 导入成功时立即提交边框配置；Logo 成功还同步当前边框的收藏展示设置。取消图片选择不修改配置，导入失败使用 `photo_frame_image_import_failed`。
- **滤镜强度**：目标目录键存在记忆时使用该值，否则 80。不能把上一款滤镜的强度带到新滤镜。关闭滤镜只关闭启用标记，保留所选滤镜身份与强度。
- **收藏顺序**：使用有序列表，不使用 `Set` 或字典遍历顺序。收藏优先后接目录原顺序。编解码保留首条合法重复记录，跳过无效键。
- **边框收藏**：只应用展示属性；文本和图片 hash 来自当前配置。图片收藏没有当前有效 Logo 时不切换预设，并使用 `photo_effect_favorite_image_missing`。
- **范围**：水印大小 300…1、透明度 100…1，每格 1。控件、保存、恢复、文本/图片渲染四层必须一致；不得保存后夹回 100 或跳过奇数百分比。

## 2026-09-14 当前落地与证据

- 删除原先设置页独立的简化 `PhotoEffectsEditorView` / 开关卡片实现。设置与工作台现在都使用 `PhotoEffectsControls`；分别传入自己的草稿、图片选择器、焦点和收藏弹窗状态。
- 对齐上述控件结构、两行 4:3 布局、卡片圆角/内边距/填充、紫色内嵌区域、坐标/海拔显隐和完整水印范围。边框信息及水印展开继续使用现有共用动画；尚未凭源码检查认定帧率和中断表现完成。
- 分类选择移到工作台根覆盖层/设置外层覆盖层，避免被设置卡片或 ScrollView 裁掉。
- 修正收藏顺序、目标滤镜强度读取、重复强度记录解码、图片收藏保留当前内容、设置遮罩关闭丢失草稿、文本/Logo 提交边界。
- 回归命令：`xcodebuild -project ios/ZTransfer.xcodeproj -scheme ZTransfer -destination 'platform=iOS Simulator,id=CA046456-B859-45F4-9CB3-2C6E2F8E03B0' GENERATE_INFOPLIST_FILE=YES -only-testing:ZTransferTests/DomainModelTests -only-testing:ZTransferTests/PhotoEffectsBatchTests test`。
- 结果：22 项通过（含本轮新增 6 项回归）。日志 `/tmp/ztransfer-shared-effects-test.log`；首次命令未传自动生成测试 Info.plist 参数，测试目标在编译前失败，补参数后通过。没有把首轮失败记录成成功。
- 画面检查：CUA 返回 Mac 锁屏，尚未完成模拟器点击、展开/收起录屏和设置/工作台对照。已经请求用户解锁；不以测试成功代替 UI 验收。

## 明确未闭环的后续项

1. 高级版/免费版有效水印与锁定状态按 iOS 的 App Store 产品方案处理，不复用 Android `LicenseManager`；用户明确排除的更新/换机按钮不得借此加回。两处图片导入忙碌态、后台导入和连续选择竞态已接入代码。
2. 输入法焦点与点击空白提交已改为内容背后的命中层，文本框、按钮和拨轮不再经过父级点击手势；输入清洗、取消 Logo、连续收藏及分类弹窗手势仍需完整交互验收。
3. 13 种边框的导出图、预览、文本/Logo 位置及尺寸仍需像素/截图验证；目前不能以有 13 个分支认定全部复刻完成。

## 2026-09-17 两处入口专项复查修正

- 连接后设置与本机工作台的 Logo 导入改为 Photo Picker 文件表示和 128 KiB 流式私有复制；20 MiB 边界、SHA-256、图片边界校验、忙碌态及导入代际由各自 Store 持有，关闭/重开设置窗不会取消或回放旧结果。
- 本机批量选择恢复 Android `image/*` 语义，不再静默删除 HEIF/RAW；每项由实际解码决定成功或失败。返回连接页不再触发批次取消，只有工作台对象真正释放才结束任务。
- 正式输出的 EXIF 方向改用 CI-backed 惰性变换，带边框滤镜继续按约 4M 像素分块；本机预览和输出使用临时文件 URL，不再让完整压缩 Data 与展开位图同时驻留。
- 两处编辑器补空白点击清焦提交；预览串行门控的等待者改为可取消队列。
- 遵照用户要求未构建、未启动模拟器。91 个 App Swift 源文件通过 Swift 6 / iOS 16 全目标仅类型检查；新增测试源码通过语法检查，尚未实际执行测试。

## 2026-09-17 二次专项复查修正

- 图片水印配置归一化只检查内容哈希私有文件，不再在主线程反复读取和构造完整 `UIImage`；导入阶段执行最长边 2048 的实际缩略解码，渲染阶段使用线程安全、插入顺序淘汰的三项缓存，对齐 Android 的采样和缓存上限。
- Store 仅在归一化后的有效配置仍持有同一图片 hash 时发布导入 revision；两处入口把被归一化拒绝的结果统一显示为导入失败，不会让连接后设置草稿短暂恢复无效 Logo。
- 空白点击清焦由 ScrollView 父级手势移到内容背后的透明命中层，交互控件保持前景命中；新增右转、右镜像、左镜像和上下镜像方向的带边框滤镜照片层像素回归，并补充水印 2048px、三项缓存和拒绝发布覆盖。
- 高级版/免费版仍按用户要求不在本轮改动；必须等待 iOS 原生权益方案，不能移植 Android 激活逻辑。
- 按用户要求未构建、未启动模拟器、未打包；91 个 App Swift 源文件再次通过 Swift 6 / iOS 16 全目标仅类型检查，新增测试源码通过语法检查但尚未执行。仅出现既有 `Np3BitmapFilter`、`RemoteMovieRecording` Sendable 警告。

## 预览接入对账（2026-09-14，进行中）

安卓 `SettingsOverlay` 的效果页不是静态设置表：打开时调用 `requestEffectPreview`，按最新文件身份去重；先发布缓存缩略图，再在传输/监看/FHD 空闲时取 FHD 和 EXIF，并保持旧帧到新帧就绪。设置变化只重新合成效果，不重新请求相机图片。

iOS 现已把预览源从 `PhotoListView` 传到 `SettingsPopupOverlay`/`SettingsView`。打开效果页会按 `handle|fileName|size|captureDate` 取最新静态文件（排除 `.mov/.mp4`，按拍摄时间再按句柄取最大值），先只查 `CameraSession.cachedThumbnail(file:)`，随后在效果预览占用期间读取 FHD，FHD 成功后才读取 EXIF；代际检查会丢弃迟到旧任务。`PhotoEffectsSettingsPreview` 对当前源异步执行 `PhotoEffectsRenderer`，保留上一帧直到新帧就绪。若效果页早于首批元数据打开，iOS 保留 demand，首批候选出现后自动触发同一流程。

当前预览接入已闭环：

- iOS 在 `PhotoListView` 保留“设置早于首批文件打开”的预览 demand，并在候选照片出现后按安卓候选规则自动继续；FHD 预览期间阻塞后台缩略图填充，效果页使用 2200ms 兜底宽限。
- iOS 将照片源、EXIF 和旋转方向纳入渲染代际 key，保留旧帧直到新帧就绪；主帧完成后按安卓 500ms 延迟生成无滤镜对比帧，长按时显示对比帧，松开恢复。最近三帧、向前两项预取和完整互斥渲染均已接入。
- 设置页效果预览已实现点击全屏、旋转保留和缩放/收回手势；全屏成片采用系统覆盖层承载，设置窗本身的 Genie 收回仍由原锚点动画负责。


### 2026-09-14 预览兜底源对账

设置效果页在无真实源时等待安卓规定的 2200ms，再显示同尺寸渐变、斜带和圆形示例；真实缩略图/FHD 源到达后按源 key 重新渲染并保留旧帧直到新帧可用。定向 DomainModelTests 15 项通过。

## 2026-09-14 文案来源对账

`ios/ZTransfer/UI/LocalPhotoEffectsView.swift` 的批处理按钮状态文本已改为直接读取
`local_photo_batch_*` 与 `local_photo_choose_short` 安卓资源键，数量占位符按安卓格式化规则
替换；不再在 Swift UI 中另写中文文案。缓存清理回归与本项改动均已通过对应 Xcode 定向测试。

## 2026-09-16 设置弹窗几何与照片效果 UI 对账

- 连接后的设置弹窗以照片列表左上角 Z 按钮的测量矩形作为唯一转场锚点；容器内部将锚点转换为本地坐标，打开和关闭都回到同一按钮位置。首次测量完成后再启动网格动画，避免临时可用高度先被捕获而造成面板高度闪变。
- 连接后的默认兜底位置位于顶部控制行下方，Z 按钮保持完整可见；连接前设置页仍使用紧凑顶部间距。
- 一级设置页的照片效果入口已改为安卓同款橙色外框、标题/箭头和橙蓝两块摘要卡；二级页无真实预览源时保留 4:3 加载表面，使 2200ms 兜底预览任务能正常显示，不再因 `EmptyView` 折叠为零高。
- 模拟器无签名 Debug 构建通过；`DomainModelTests` 与 `PhotoEffectsBatchTests` 共 51 项通过。已用 iPhone 17 Pro 模拟器检查连接后一级设置面板的 Z 按钮间距、照片效果摘要卡和二级预览占位/成片可见性。

## 2026-09-16 照片效果预览方向、手势与转场清晰度对账

- 二级预览沿用安卓的稳定视口：横图为 4:3、竖图为 3:4；边框增加底部信息带时仍在该视口内 `fit`，不再以渲染后画布比例重新撑高设置页。双击主预览按 90° 累加旋转源后重新合成，旋转期间清空旧方向的预取帧，避免边框高度短暂跳回。
- 单击预览打开全屏成片；全屏页支持 1…4 倍双指缩放、双指拖动边界约束、双击 2.5 倍/还原和 1 倍单击收回，与安卓 `ZoomablePreviewViewport` 的交互边界一致。长按对比滤镜基线仍在二级页保留。
- 设置窗 Genie 分段图层明确启用线性/三线性采样和边缘抗锯齿，并在源、目标两侧各留一像素重叠，消除旋转分段之间的锯齿白缝；打开与收回继续共享 Z 按钮锚点。
- 文件列表筛选弹窗也改用同一 `GeniePopupPanel`：从筛选按钮下缘 8pt 处展开、收回回到原按钮；横向按安卓 12pt 屏边距钳制，首次锚点尚未测量时落在顶部控制行下方，避免覆盖 Z/信号/筛选按钮。
- 模拟器无签名 Debug 构建通过；`DomainModelTests` 与 `PhotoEffectsBatchTests` 共 51 项通过。已在 iPhone 17 Pro 模拟器检查横图边框预览、设置窗展开中间帧和完成帧。

## 2026-09-14 效果预览渲染并发对账

安卓效果预览使用全局 `photoEffectsPreviewRenderMutex` 串行化完整成片，避免当前拨轮变化时旧
滤镜、边框和对比帧并发占满 CPU。iOS 新增 `PhotoEffectsPreviewRenderGate`，仅包住交互预览
（工作台、设置页主帧、500ms 对比帧和兜底帧）的滤镜/边框合成；批量导出不共享该门控，保持
安卓批处理自己的并发策略。等待任务在取消后会释放门控，旧任务不能阻塞新一代预览。模拟器
无签名 Debug 构建已通过。

## 2026-09-14 当前滤镜后续项预取对账

安卓 `nextPhotoFilterSelections` 按收藏优先后的目录顺序，从当前滤镜之后取最多两个选项，
分别使用各自持久化强度；当前成片与对比帧优先显示，后续预取不能阻塞当前 UI。iOS 设置页
预览现按相同规则生成后续两项，在当前帧显示并完成 500ms 对比帧后顺序渲染，结果以完整
`source/metadata/settings` 键缓存，切换命中直接复用并限制缓存最多两项；旧任务取消时由
预览门控释放。模拟器无签名 Debug 构建成功。

## 2026-09-14 工作台滤镜后续项预取对账

安卓本地工作台的预览页也接收同一 `nextPhotoFilterSelections`，当前照片显示后按收藏优先
顺序预取当前滤镜之后最多两项，每项使用独立强度。iOS `LocalEffectPreview` 现复用同一
选择函数和 `LocalPhotoOutput` 渲染管线，并按设置快照缓存预取成片；命中后仍按安卓延迟
生成无滤镜对比帧，不改变批量导出路径。
