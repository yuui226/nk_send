# 监看工具栏自定义与辅助工具需求清单

> 整理日期：2026-09-21
> 状态：第一版已提交为回退点 `f9407e0d`；第二版改为工具栏原位编辑，代码已接入，实际监看页触摸验证及 Debug 构建通过。相机相关联调限制仍见第 11 节。
> 范围：监看工具偏好持久化、布局锁定、白平衡与对焦区域调整、曝光辅助与波形图、工具栏管理。
> 快门 PC 控制兼容问题已暂时跳过，不作为本需求的前置条件。

## 1. 已确认的整体行为

- 用户退出监看页、杀死 Z传或重启后，工具开关和档位应保留；纯净全屏状态不持久化。隐藏工具时同时关闭其功能，重新显示入口不自动启用功能。
- 照片与录像分别保存排序、显隐；同一拍摄模式的横竖屏共用配置，仍保留各自布局；全屏与旋转固定在右侧，始终显示，不参加自由排序和显隐。
- 新增锁定布局、白平衡调整、对焦区域调整、伪色、波形图。
- 斑马纹和伪色合为一个“曝光辅助”按钮：**关闭 → 斑马纹 → 伪色 → 关闭**。
- 波形图保持独立，可以与斑马纹或伪色同时使用。
- 新增小尺寸齿轮管理按钮，固定在普通工具序列最后；全屏与旋转属于独立的右侧固定区域，其位置规则优先。
- 点击齿轮进入原位编辑：整颗按钮点击切换显隐，拖动排序；按钮主体轻微抖动，眼睛只表达状态。录制入口立即隐藏，后台正常停止并保存。

## 2. 工具栏原位编辑（最新确认，替代管理弹窗）

### 入口与退出

- 齿轮位于普通工具序列末尾，始终可达。点击进入编辑后变成对号；再次点击或系统返回退出编辑，顶栏返回也优先退出编辑。
- 不使用弹窗，不显示按钮名称，不增加按钮间距，不增加滚动区域。按钮保持现有尺寸，宽度不足自动换成多行。
- 编辑模式不保存；排序、显隐和功能关闭状态即时保存。
- 编辑期间临时暂停自动方向监听并清空待执行的旋转；全屏与旋转暂时不可点击。退出后恢复原来的方向锁定规则，不修改用户保存的锁定偏好。

### 点击与拖动

- 编辑期间整颗普通工具按钮点击即切换显隐，不执行原功能。已经开启的参考线等效果继续运行，直到被隐藏。
- 只有可见的普通工具轻微抖动并可拖动。拖动超过触摸阈值才开始排序，松手不能被识别为点击；拖动时停止抖动、稍微放大，周围按钮平滑让位，松手平滑落位。
- 睁眼表示可见，闭眼与主图标黯淡表示隐藏；眼睛始终保持正常亮度，可点击恢复，整颗按钮同样可点。无障碍描述保留名称与显隐状态，可见工具支持上移／下移操作。
- 可见普通工具在前，隐藏工具集中在后，完成按钮再接在后面。不增加分隔线或额外间距。隐藏按钮不能拖动；恢复显示后即可拖动。
- 编辑与正常显示共用模式适用规则：照片模式不显示音频电平，编辑时也不出现；录像模式可配置音频电平。照片／录像各自维护可见与隐藏队列，切换模式不覆盖另一套排序或显隐；切入某模式时会停止该配置中已隐藏工具的功能。功能档位继续沿用现有偏好，未额外拆分两套。

### 显隐与运行状态

- 隐藏时关闭对应功能，移到隐藏队列末尾并变暗；重新显示时追加到可见普通工具末尾，保持功能关闭，不恢复原来的排序位置或自动启动功能。
- 参考线、曝光辅助、直方图、波形、水平仪、FPS 和音频电平关闭各自显示／计算。HD 关闭，反挤压恢复 1×，隐藏锁定工具解除用户方向锁定；编辑期间的临时冻结持续到退出编辑。
- **录制入口立即隐藏，后台停止并完成 MP4 收尾，不显示处理中。**暂停录制同样处理。收尾期间重新显示入口也不会自动开录，不能启动另一段录制；失败仍提示错误，不能静默丢失保存异常。不会因此停止相机机身录像。
- 隐藏白平衡／对焦区域只关闭对应面板，不重置相机参数。
- **全屏、旋转始终显示，不可拖动，不可隐藏，不抖动、不显示眼睛。**不做旧配置迁移，此功能尚未对老用户发布。

## 3. 工具清单与建议名称

以下是整理清单，**不是最终默认顺序**。保留现有普通工具的相对顺序，新工具默认插入位置在实施时结合布局确定；齿轮在普通工具序列最后，全屏、旋转保持右侧固定位置。

| 工具名称 | 工具栏行为 | 状态保存与能力要求 |
|---|---|---|
| 高清监看 | 切换 HD | 保存用户选择；相机不支持或启动失败时按实际能力处理 |
| 帧率显示 | 显示／隐藏 FPS | 保存开关 |
| 音频电平 | 显示／隐藏音频电平 | 保留现有持久化；实际显示沿用录像模式及数据可用条件 |
| 直方图 | 显示／隐藏直方图 | 保存开关 |
| 参考线 | 沿用现有样式循环 | 保存具体样式，保留无实际画面时也能查看样式的行为 |
| 曝光辅助 | 关闭 → 斑马纹 → 伪色 → 关闭 | 保存具体档位；图标反映档位，不弹气泡提示 |
| 反挤压 | 沿用倍率循环 | 保留现有倍率持久化 |
| 水平仪 | 显示／隐藏水平仪 | 保存用户偏好；机型不支持时不显示虚假读数 |
| 监看录制 | 沿用端侧开始、暂停、停止流程；隐藏入口同时后台停止并收尾 | 仅保存入口配置，不恢复正在录制的状态；仍保留现有功能权限 |
| 纯净全屏 | 沿用全屏入口与返回方式；固定在旋转左侧，不参与拖动 | 全屏状态不持久化；入口始终显示，不得绕过方向冻结 |
| 旋转布局 | 沿用手动切换方向；固定在右侧区域最右，不参与拖动 | 入口始终显示；锁定或编辑时不可改变布局 |
| 锁定布局 | 锁定／解锁当前方向 | 保存锁定状态和锁定方向 |
| 白平衡 | 打开白平衡调整面板 | 每次连接读取相机真实值，不自动写入上次值 |
| 对焦区域 | 打开对焦区域选择面板 | 按相机能力提供区域选项；本轮不新增 AF-S/AF-C/MF 模式切换 |
| 波形图 | 显示／隐藏波形图 | 保存开关；第一版建议做亮度波形 |
| 管理工具 | 齿轮进入原位编辑，对号完成 | 普通工具序列最后、始终可见，不参与拖动或显隐；不占用全屏/旋转固定区域 |

调试入口继续遵循现有调试解锁规则，不因这次工具管理而对普通用户直接开放。

## 4. 持久化范围

### 必须保存

- 可开关工具的实际启用状态与档位；隐藏后的实际状态必须为关闭。可另存上次使用的参考线样式、反挤压倍率、曝光辅助档位，供用户下次主动启用使用。纯净全屏除外。
- 工具排序和显示／隐藏配置。
- 布局锁定状态、锁定时的方向。
- 新增工具未来具有的用户显示参数，应进入同一偏好管理方式。

### 需要区别的状态

- **用户偏好**与**当前相机是否可用**分开保存。暂时不支持某工具，不应抹掉用户偏好。
- 相机的白平衡、对焦区域、对焦模式、曝光等参数以相机当前值为准，重连读取，不自动恢复写入。
- 纯净全屏不持久化；重新进入监看页使用常规布局，但仍恢复已保存的锁定方向。正在录像、正在对焦、网络请求、调试 PC 控制等运行状态不随 App 重启自动继续。
- 首次升级应保留已有音频电平、反挤压设置；未设置过的工具沿用现有默认行为。
- 使用稳定工具标识和档位标识，避免以后增删工具或调整枚举顺序造成偏好错位。

## 5. 布局锁定

锁定布局的含义仅为**冻结方向**，不是锁定工具排序、显示配置或所有界面操作。当前监看页由内部布局处理竖向、左横向、右横向显示。

开启锁定时：

1. 以当前已经显示的方向为锁定方向。
2. 停止方向监听，不只是忽略回调。
3. 取消待执行的方向稳定延时、队列请求，避免锁定后仍转动一次。
4. 手动旋转按钮保留原位并变暗不可操作（若用户已隐藏则保持隐藏）；全屏入口也不能强制转横屏。工具管理、排序和其他工具操作仍可使用。
5. 重新进入页面时直接恢复锁定方向，避免先竖屏再闪到横屏。

解除锁定时恢复正常的方向检测和防抖机制。横竖屏、全屏、返回页面和 App 前后台切换均需遵循锁定状态。

## 6. 新工具实现范围

### 白平衡

- 新增 WB 图标入口，面板显示当前白平衡和相机实际提供的选项。
- 第一阶段优先模式切换；色温与微调是否加入，取决于目标相机可读写能力。
- 不硬编码一套所有机型都可写的选项；只读或不支持时给出对应状态。
- 写入后读取确认，并适配相机端修改后的刷新。

### 对焦区域

- 用户明确需要的是**调整对焦区域**，不是 AF-S、AF-C、MF 对焦模式切换；本轮仅新增区域调整入口。
- 面板显示相机当前区域，以及相机实际支持的区域选项，例如单点、宽区域、自动区域等；不硬编码所有机型都有同一套列表。
- 分别验证照片和视频模式、AP/STA/USB 的能力；当前对焦模式可能影响区域是否可选，按实际能力显示，不自动更改对焦模式来强行解锁选项。
- 写入后读取确认，并刷新现有点击对焦、跟踪与区域框相关状态；不能只更新文字而让对焦行为沿用旧区域。

### 曝光辅助：斑马纹／伪色

- 一个按钮三档循环，保留现有斑马纹能力并增加伪色。
- 斑马纹与伪色同一时间只显示一种，属于本次交互选择，不是协议或算法上的必然限制。
- 伪色第一版建议固定一套配色，提供可理解的图例；不先增加多套预设。
- 根据相机传来的监看图像分析，不宣称反映 RAW 原始曝光或未经处理的传感器信号。
- 不修改拍摄照片或相机设置；辅助图像坐标需与反挤压后的显示、旋转和有效图像区域一致。

### 波形图

- 保留独立开关，第一版建议亮度波形，可与曝光辅助并用。
- 有效图像区域不包含界面黑边、文字、参考线或伪色层；分析原始监看帧，避免分析到自身绘制的辅助层。
- 复用已经解码的画面，限制采样量和刷新频率；关闭时不计算。
- 与直方图、音频电平等同时显示时要安排位置，不能相互覆盖到无法阅读。

## 7. 横竖屏共用工具定义

统一的是工具的标识、名称、图标、行为、状态、排序和显示配置；各方向仍有各自布局容器。

- 竖屏继续使用适合竖屏的位置和换行方式。
- 横屏继续使用适合横屏的位置和间距。
- 两个容器消费同一份有序工具列表，新增或隐藏工具无需分别维护两套按钮定义。
- 普通工具按用户顺序排列，最后追加齿轮；全屏、旋转由独立固定区域布局。不能将固定按钮混入自由排序后依靠“最后两项”推测身份。
- 因拍摄模式暂时不显示的工具保留配置位置，重新可用时回到原位。

### 全屏与旋转的固定位置（用户补充确认）

- **竖屏：**全屏、旋转只在第一排右侧出现，整体对齐监看画面下方工具栏的右侧安全边界。即使普通工具不足一行，也通过中间留白保持固定位置，不能紧跟普通按钮向左收缩，更不能掉到第二排。
- **横屏：**全屏、旋转保持工具栏第一排最右侧固定区域，不进入普通工具的自由排序及换行序列；右侧以当前横屏工具栏的可用边界为准，保留现有面板和安全间距。
- 两者从左到右沿用 **全屏 → 旋转**，始终显示，不参与显隐和拖动；编辑期间暂时不可点击。
- 第一排先为两个固定按钮预留宽度，普通工具放不下则按顺序移到后续行。齿轮是普通序列末项，允许随普通序列换行，不要求占用第一排右侧。

单行示意（空白代表用于保持右侧位置的剩余空间）：

```text
[HD] [参考线] [齿轮]                 [全屏] [旋转]
[齿轮]                              [全屏] [旋转]
```

实施结果：横竖屏仍共用 `AdaptiveRemoteToolBar`，但固定项始终为全屏和旋转，固定区起点为“可用宽度减固定区宽度”。已在模拟器验证只剩齿轮时，两种布局的全屏／旋转仍位于第一排右侧。

## 8. 录制输出与实施细节

### 用户已确认的监看录制输出

- 端侧录制保留监看画面本身，以及用户启用的反挤压效果。
- **不录入辅助图层**：参考线、斑马纹、伪色、直方图、波形图、水平仪、FPS、音频电平、对焦框等 App 叠加内容。
- 录制取帧与分析、绘制链路应分开，不能对已经绘制辅助层的界面直接录屏；实际录制行为必须通过输出视频验证。
- 这里排除的是 App 叠加的辅助内容；相机本身已嵌入源画面的内容无法仅靠跳过 App 图层移除。

### 尚需实施时确定的细节

- **新增工具默认排序与可见性：**在当前布局中确定，管理齿轮处于普通工具序列最后、全屏/旋转固定右侧的规则不变。
- **伪色图例和波形图位置：**按现有监看布局安排，避免与其他辅助工具相互遮挡。
- **相机控制能力：**WB 和对焦区域可选项必须经协议查询、写入回读和必要真机验证，不能以按钮做好作为功能完成标准。

## 9. 建议实施顺序与验收

### 实施顺序

1. 统一工具定义与偏好管理，实现原位编辑、排序、显示开关和状态持久化。
2. 加入布局锁定，处理手动旋转、全屏、方向事件与恢复顺序。
3. 加入白平衡、对焦区域设置，并验证相机读写能力。
4. 将斑马纹升级为曝光辅助三档，加入伪色及亮度波形，完成性能和显示适配。

### 当前版验收清单

- [ ] 齿轮进入原位编辑，对号、系统返回和页面返回均可退出；重进不恢复编辑模式。
- [ ] 原尺寸／原间距、多行换行，不出现管理弹窗、名称列表或滚动区域。
- [ ] 点击整颗按钮隐藏／恢复，不触发功能；拖动跨行排序，松手不切换显隐。
- [ ] 可见普通工具抖动、隐藏工具黯淡且不可拖动；眼睛开闭与实际显隐一致。
- [ ] 隐藏移到隐藏队列末尾，恢复移到可见队列末尾；重启后顺序和显隐一致。
- [ ] 全屏／旋转始终固定第一排右侧，不抖动、不显示眼睛、不参与编辑。
- [ ] 编辑时停止方向监听及残留旋转，退出后恢复原来的锁定／自动旋转行为。
- [ ] 录制中或暂停时隐藏入口立即生效，后台正常收尾；恢复入口不自动开录，失败提示可见。
- [ ] 照片编辑不出现音频电平；照片和录像的排序、显隐独立持久化，同一模式横竖屏共用。
- [ ] 显隐、排序和跨行时平滑让位，所有普通工具隐藏后仍可进入编辑并恢复。

## 10. 当前代码入口（供后续定位）

- [RemoteScreen.kt](../app/src/main/java/com/ztransfer/ui/screen/RemoteScreen.kt)：工具状态、横竖屏工具栏、方向监听、参数面板及监看帧处理。
- [RemoteViewfinderFeatures.kt](../app/src/main/java/com/ztransfer/ui/screen/RemoteViewfinderFeatures.kt)：参考线、直方图及现有辅助绘制。
- [RemoteLab.kt](../app/src/main/java/com/ztransfer/protocol/RemoteLab.kt)：白平衡／对焦属性定义、现有对焦读取和参数控制。

新增入口：`RemoteToolPreferences.kt` 管理偏好；`RemoteToolBar.kt` 原位编辑、手势与位置动画；`RemoteCameraTools.kt` 和 `RemoteCameraToolPanel.kt` 查询／调整相机参数；`RemoteExposureAnalysis.kt` 负责伪色、波形及采样缓存。

## 11. 第一版实现与验证记录（回退点 f9407e0d）

以下为改用原位编辑前的历史记录；涉及弹窗、固定按钮显隐及等待收尾后隐藏的规则，以第 2 节和第 12 节为准。

| 项目 | 实现情况 | 已验证／尚待验证 |
|---|---|---|
| 统一工具定义、横竖屏排序、右侧固定按钮 | 已实现 | 模拟器验证默认布局和仅剩齿轮的横竖屏；固定按钮不参与排序 |
| 齿轮管理弹窗、图标复用、拖动与隐藏 | 已实现 | 实际拖动参考线并重进恢复顺序；通过弹窗隐藏全部普通工具，齿轮仍可打开管理；横屏弹窗可用 |
| 开关、档位、排序和隐藏持久化 | 已实现 | Android SharedPreferences 重建、旧音频／反挤压偏好、未知标识、重复排序项、隐藏但误存开启的修复测试通过；纯净全屏不进入偏好 |
| 方向冻结 | 已实现 | 保存方向、关闭监听与取消队列；模拟器验证已保存的右横屏在进页直接恢复；修改模拟加速度、点击旋转、进入全屏均未改变锁定方向，隐藏锁定工具后解除锁定；物理传感器与前后台组合仍待真机 |
| 白平衡与对焦区域 | 已实现，待相机联调 | 照片／视频分开查询属性；只列相机提供的值，写入前重查，写后回读；面板轮询与写入串行；未接入相机，不能确认 AP／STA／USB 支持矩阵 |
| 曝光辅助三档、伪色、亮度波形 | 已实现 | 灰阶测试验证阈值、波形左右及亮度方向、源图不被修改；关闭／切档与缓存节流测试通过；98／197／250 dp 高度合成画面的实际 Compose 绘制检查 |
| 隐藏端侧录制工具时停止收尾 | 已实现 | 活跃／暂停录制共用正常 stop，收尾成功才隐藏，失败保留入口并提示；真实编码器的暂停收尾通过；带相机帧的完整“点隐藏”交互、麦克风和 SAF 故障仍待真机 |
| 录制反挤压、排除辅助层 | 已实现 | 真实 MP4 生成后重新解码，验证 1× 和 1.5× 几何、无伪色污染，录制中源图分辨率变化可继续；输出画布在开录时固定 |
| 多语言与主题 | 已接入 | 英文、简体、繁体资源齐全；沿用现有主题按钮，检查浅色主题及横竖屏；各材质、大字体完整交互仍待补验 |

### 已执行的检查

最终 Debug 产物：`dist-debug/ZTransfer-debug-1.83-20260921-150105.apk`。

- 仓库 macOS Debug 脚本 `bash dist-debug/build.command` 构建通过；产物只写入 `dist-debug`，未修改正式发布目录。
- 定向单元测试：`RemoteOrientationPolicyTest` 2 项、`RemoteLiveViewSchedulingTest` 8 项、`FramingGridTest` 6 项，共 **16 项通过**。
- 新增 `RemoteToolsInstrumentation`，在 Android 35 ARM64 模拟器运行通过。覆盖实际偏好读写、配置迁移／容错、隐藏即关闭、固定排序、分析缓存／图像，以及 MediaCodec／Muxer 的实际文件生成与解码。
- 编码验证采用 320×240 原始灰色帧，同时执行伪色／波形分析，检查输出仍为灰色；1.5× 输出为 480×240；途中改送 640×480 源帧仍可继续；暂停后 stop 的 MP4 可解码。
- 相机未连接时的监看工具管理可以操作；不生成虚构的白平衡、区域或水平读数。

### 首版边界与后续联调重点

1. **白平衡先支持模式选择。**色温数值和微调未扩展。对焦区域只给已确认的标准编码命名，未验证的厂商编码显示“相机选项 + 编号”，不能据此宣称所有 Z 机型区域名称已适配。
2. **不为解锁参数自动改变相机控制模式。**只读属性保持不可写，不自动进入完整 PC 控制，不切换 AF-S／AF-C／MF。需要分别在照片／视频及 AP／STA／USB 下核对相机实际能力。
3. **图像分析基于收到的 JPEG 亮度。**上限 256×192 采样、125 ms 刷新间隔；关闭时不计算，两个工具共用采样。伪色显示更新因此最高约 8 次／秒，真机联调需重点评估动态监看观感；不代表 RAW 或经过标定的传感器曝光。
4. **录制中切换反挤压不会改变 MP4 分辨率。**新比例居中适配开录时的输出画布，可能出现留黑；这是保持同一文件连续编码的处理方式。原始比例录制保留直接取像素路径。
5. **隐藏后重显保持关闭。**本版没有另存隐藏前的参考线／曝光辅助档位，用户再次点击从首档开始；正常退出、重进而未隐藏时，具体档位完整恢复。
6. **故障不伪装成功。**MP4 必须实际执行 muxer 收尾成功才显示保存成功；部分文件仍保留供恢复。相机写入必须回读确认；设备变化或只读时不提交旧值。

协议取值参考 libgphoto2 的 `camlibs/ptp2/ptp.h` 与 `config.c`：
[源码](https://github.com/gphoto/libgphoto2/tree/master/camlibs/ptp2)。公开源码中的属性定义是实现依据，不能替代目标机型的真机验证。

## 12. 原位编辑实施进度

- [x] 提交第一版回退点：`f9407e0d`。
- [x] 移除管理弹窗，接入原位编辑、眼睛状态与点击整颗按钮切换显隐。
- [x] 普通工具显示／隐藏双队列，恢复追加到显示队列末尾；固定按钮不参与显隐。
- [x] 拖动阈值、拖动期间取消点击、跨行目标位置判断与落位动画。
- [x] 编辑临时冻结方向；完成／返回退出后恢复原先策略。
- [x] 录制入口立即隐藏、后台收尾；异步结束不再反向覆盖用户随后恢复的显隐选择。
- [x] Android 真触摸事件验证：整颗点击显隐、恢复到末尾、跨行拖动、落下不误触、隐藏项不可拖动、固定按钮不移动、全部隐藏后可再进入编辑。
- [ ] 原位编辑横屏完整交互、方向传感器及带相机帧录制中隐藏的真机验证。
- [x] 最终 Debug APK 构建：`dist-debug/ZTransfer-debug-1.83-20260921-202418.apk`；已安装模拟器。

### 原位编辑验证补充

Android 35 ARM64 模拟器的 `RemoteToolEditorInstrumentation` 已通过，直接进入实际监看页执行触摸，不使用独立空白编辑页。`RemoteToolsInstrumentation` 同样通过，覆盖偏好重建、双队列顺序、固定项、图像分析与真实 MP4 编码及暂停收尾。隐藏只压暗主图标，眼睛保持正常亮度；根据最新反馈移除分隔竖线。

### 照片／录像工具配置分离

2026-09-21 调整：音频电平仅录像模式适用，照片模式连编辑列表也不出现。排序与显隐按照片／录像分别存储，横竖屏仍共用当前模式配置；不迁移未发布的旧工具布局。模式切换会取消进行中的拖动，并关闭新配置中已隐藏工具的功能。

验证：`RemoteToolsInstrumentation` 通过两套布局隔离、重建恢复、音频适用范围、隐藏功能停止检查；`RemoteToolEditorInstrumentation` 在实际照片监看页通过无音频入口、跨行拖动、显隐和固定按钮检查。测试会还原模拟器原有监看偏好。录像模式真实机身拨杆切换仍待相机联调。

本次调试曾发现进入监看页的 `Context.getFilesDir()` 空指针：Debug DEX 中上下文所在寄存器在大回调构造前被清空，随后又用于取目录。将 Context、协程作用域、剪贴板和触觉依赖合并为 `RemoteScreenServices`，减少监看大函数的局部变量与回调捕获后，同一真实页面触摸测试通过；没有吞掉异常或把空上下文当作正常情况继续运行。

### 锁定的默认位置与列对齐

锁定默认放在第二行第一个，不挤入第一行右侧；全屏、旋转仍固定在第一行右侧。锁定照常抖动、拖动和隐藏，拖走后按普通排序保存；隐藏后再恢复追加到普通工具末尾。只剩一行时按普通工具放在管理按钮前。默认位置标记随照片／录像配置分别保存。

所有行共用列轨道，剩余宽度均匀分配为列间距，使全屏左侧间距与其他按钮一致，第二行与第一行上下对齐。录制胶囊展开和大字体按整列跨度占位，不压缩内容；末行沿用同样列位置。

本轮实际监看页触摸测试通过：默认第二行首位、跨行拖动、锁定拖走／隐藏／恢复、固定按钮、隐藏项不可拖动、全部隐藏后管理入口可达。测试中的隐藏项拖动从图标内侧起手，以避开系统边缘返回手势；系统返回退出编辑仍保留。已检查实际页面截图的列对齐。

### 录制图标与相机选项说明

2026-09-21 用户最终确认：编辑态使用统一的录制工具图标，代表显隐管理；正常态仍使用原有红圆点／停止方块及录制控制，不在编辑态混用录制操作状态。

白平衡当前查询的是模式枚举。晴天、白炽灯、手动预设、自然光自动适应均为尼康真实模式，中文名称由 App 根据返回编码映射，并非从相机读取中文。手动预设用于测量或使用已保存的白平衡，不能等同于手动输入色温。直接调节 K 值需要另接色温属性和能力查询，当前尚未实现，不能只将模式名称改为固定 K 值。

对焦区域当前属性描述只提供数值，不含菜单名称。反馈中的 32784 为 0x8010，现有名称映射只用于 0x501C 分支；0xD05D／0xD1F8 分支仍回退为数字。公开实现中部分尼康机型的 0x8010 对应单点，但补齐映射前需结合机型、照片／录像模式、实际选中属性确认，避免把不同属性的同一编码误当同一选项。已向用户询问这些信息。

参考：[尼康 Z f 白平衡说明](https://onlinemanual.nikonimglib.com/zf/zh-cn/white_balance_36.html)、[libgphoto2 选项映射](https://github.com/gphoto/libgphoto2/blob/master/camlibs/ptp2/config.c)、[属性编号定义](https://github.com/gphoto/libgphoto2/blob/master/camlibs/ptp2/ptp.h)。

### RGB 直方图与波形图（2026-09-21）

- 直方图循环：关闭 → RGB 叠加 → 亮度 → 关闭；波形图循环：关闭 → 亮度 → RGB 叠加 → 关闭。各自保存档位，隐藏工具后关闭，恢复入口后仍为关闭。
- RGB 直方图分别统计三个通道，使用共同纵轴比例绘制填充与轮廓；照片预览也新增 RGB 档，亮度档保留原绘制样式。两处均使用已解码画面，不额外向相机取图。
- 波形图按画面横向位置／信号值统计点迹密度，输出由 128×64 提高到 256×128；亮度与 RGB 共用固定密度强度规则，避免每帧峰值变化带来整图忽亮忽暗。显示 0–100% 信号刻度，不标称标定 IRE 或 HDR 尼特。
- 直方图和波形图同时开时左下角并排；单开任一个时使用完全相同的左侧起点和卡片尺寸。关闭左侧直方图后，波形图用 300ms 缓动移向左侧；进入／退出时淡入淡出并轻微缩放，保留退出帧直至动画结束。快速反向切换从当前位置继续。
- 两图尺寸按可同时容纳两张的宽度计算，开关另一个图不改变单图尺寸；给 FPS 留出底部空间，显示内嵌音频电平时给电平表留出左侧空间。没有相机图像时不伪造分析数据。
- 新增 `RemoteMonitorScopes.kt` 统一卡片、绘图与动画；定向图像验证及 `RemoteScopeInstrumentation` 验证单开位置、双开间距、退出移除、快速切换与低高度边界。

追加需求：照片大图预览也循环“关闭 → RGB → 原本亮度 → 关闭”，与监看分开保存。RGB／亮度切换共用已经计算的统计，避免换档重查相机、重复计算或闪空；保留 118×62 dp 和原有预览位置／滑动动画。

本轮验证通过：RGB／亮度统计、波形几何与通道、缓存切档、持久化和隐藏关闭；监看真实页面进入及编辑回归；合成画面下并排、单图同位置同尺寸、快速切换、低高度布局；真实照片预览 RGB／亮度／关闭循环及关闭重开恢复。已查看绘制截图和过渡帧。相机实时画面的性能仍需真机联调。最终 Debug：`dist-debug/ZTransfer-debug-1.83-20260921-202418.apk`。预览重开测试最终使用界面正常的点击关闭动作；没有保留试验性的退出超时逻辑或诊断打印。
