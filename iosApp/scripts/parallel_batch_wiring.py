"""Reviewed September parallel batch deltas; strict inverse for all earlier source guards.

Each hunk is an exact, unique reviewed replacement, never a regex stripping arbitrary code.
Separate contracts exercise the current production chain; this inverse preserves older baselines.
"""
CHANGES = {}

CHANGES.setdefault('iosApp/ZTransfer.xcodeproj/project.pbxproj', []).append(('\n/* Begin PBXBuildFile section */\n        7A120000000000000000007E = {isa = PBXBuildFile; fileRef = 7A120000000000000000007F; };\n        7A1200000000000000000080 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000081; };\n        7A1200000000000000000082 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000083; };\n        7A1200000000000000000084 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000085; };\n        7A1200000000000000000086 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000087; };\n        7A1200000000000000000088 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000089; };\n        7A1200000000000000000076 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000077; };\n        7A1200000000000000000078 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000079; };\n        7A120000000000000000007A = {isa = PBXBuildFile; fileRef = 7A120000000000000000007B; };\n        7A120000000000000000007C = {isa = PBXBuildFile; fileRef = 7A120000000000000000007D; };\n\t\t7A1200000000000000000074 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000075; };\n\t\t7A1200000000000000000072 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000073; };\n', '\n/* Begin PBXBuildFile section */\n\t\t7A1200000000000000000074 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000075; };\n\t\t7A1200000000000000000072 = {isa = PBXBuildFile; fileRef = 7A1200000000000000000073; };\n'))

CHANGES.setdefault('iosApp/ZTransfer.xcodeproj/project.pbxproj', []).append(('\n/* Begin PBXFileReference section */\n        7A120000000000000000007F = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = UI/CameraWorkspace.swift; sourceTree = "<group>"; };\n        7A1200000000000000000081 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = Network/CameraEndpointHistory.swift; sourceTree = "<group>"; };\n        7A1200000000000000000083 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = Network/CameraDiscoveryCoordinator.swift; sourceTree = "<group>"; };\n        7A1200000000000000000085 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = MediaPreviewCompatibilityTests.swift; sourceTree = "<group>"; };\n        7A1200000000000000000087 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = CameraWorkspaceTests.swift; sourceTree = "<group>"; };\n        7A1200000000000000000089 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = CameraDiscoveryProfileTests.swift; sourceTree = "<group>"; };\n        7A1200000000000000000077 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = Network/CameraAutomaticTransferCoordinator.swift; sourceTree = "<group>"; };\n        7A1200000000000000000079 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = CatalogEventReconciliationTests.swift; sourceTree = "<group>"; };\n        7A120000000000000000007B = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = AutomaticTransferTests.swift; sourceTree = "<group>"; };\n        7A120000000000000000007D = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = ExifCompatibilityTests.swift; sourceTree = "<group>"; };\n\t\t7A1200000000000000000075 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = Configuration/OriginalDestinationPreferences.swift; sourceTree = "<group>"; };\n\t\t7A1200000000000000000073 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = Storage/OriginalFilesReading.swift; sourceTree = "<group>"; };\n', '\n/* Begin PBXFileReference section */\n\t\t7A1200000000000000000075 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = Configuration/OriginalDestinationPreferences.swift; sourceTree = "<group>"; };\n\t\t7A1200000000000000000073 = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = Storage/OriginalFilesReading.swift; sourceTree = "<group>"; };\n'))

CHANGES.setdefault('iosApp/ZTransfer.xcodeproj/project.pbxproj', []).append(('\t\t\tisa = PBXGroup;\n\t\t\tchildren = (\n                7A120000000000000000007F,\n                7A1200000000000000000081,\n                7A1200000000000000000083,\n                7A1200000000000000000077,\n\t\t\t\t7A1100000000000000000006 /* ZTransferApp.swift */,\n\t\t\t\t7A1100000000000000000007 /* ContentView.swift */,\n', '\t\t\tisa = PBXGroup;\n\t\t\tchildren = (\n\t\t\t\t7A1100000000000000000006 /* ZTransferApp.swift */,\n\t\t\t\t7A1100000000000000000007 /* ContentView.swift */,\n'))

CHANGES.setdefault('iosApp/ZTransfer.xcodeproj/project.pbxproj', []).append(('\t\t7A1200000000000000000021 /* ZTransferTests */ = {\n\t\t\tisa = PBXGroup;\n\t\t\tchildren = (7A1200000000000000000014, 7A120000000000000000006F, 7A1200000000000000000079, 7A120000000000000000007B, 7A120000000000000000007D, 7A1200000000000000000085, 7A1200000000000000000087, 7A1200000000000000000089,);\n\t\t\tpath = ZTransferTests;\n\t\t\tsourceTree = "<group>";\n', '\t\t7A1200000000000000000021 /* ZTransferTests */ = {\n\t\t\tisa = PBXGroup;\n\t\t\tchildren = (7A1200000000000000000014, 7A120000000000000000006F,);\n\t\t\tpath = ZTransferTests;\n\t\t\tsourceTree = "<group>";\n'))

CHANGES.setdefault('iosApp/ZTransfer.xcodeproj/project.pbxproj', []).append(('\t\t\tisa = PBXSourcesBuildPhase;\n\t\t\tbuildActionMask = 2147483647;\n\t\t\tfiles = (7A1200000000000000000004, 7A120000000000000000006E, 7A1200000000000000000078, 7A120000000000000000007A, 7A120000000000000000007C, 7A1200000000000000000084, 7A1200000000000000000086, 7A1200000000000000000088,);\n\t\t\trunOnlyForDeploymentPostprocessing = 0;\n\t\t};\n', '\t\t\tisa = PBXSourcesBuildPhase;\n\t\t\tbuildActionMask = 2147483647;\n\t\t\tfiles = (7A1200000000000000000004, 7A120000000000000000006E,);\n\t\t\trunOnlyForDeploymentPostprocessing = 0;\n\t\t};\n'))

CHANGES.setdefault('iosApp/ZTransfer.xcodeproj/project.pbxproj', []).append(('\t\t\tbuildActionMask = 2147483647;\n\t\t\tfiles = (\n                7A120000000000000000007E,\n                7A1200000000000000000080,\n                7A1200000000000000000082,\n                7A1200000000000000000076,\n\t\t\t\t7A1100000000000000000009 /* ContentView.swift in Sources */,\n\t\t\t\t7A1100000000000000000008 /* ZTransferApp.swift in Sources */,\n', '\t\t\tbuildActionMask = 2147483647;\n\t\t\tfiles = (\n\t\t\t\t7A1100000000000000000009 /* ContentView.swift in Sources */,\n\t\t\t\t7A1100000000000000000008 /* ZTransferApp.swift in Sources */,\n'))

CHANGES.setdefault('iosApp/ZTransfer/Configuration/BrowsePreferencesStore.swift', []).append(("        let organizeByDate: Bool\n        let deferStart: Bool\n        // Optional v1 extension: Android's original automatic-transfer default is false.\n        let autoTransferNewMedia: Bool?\n    }\n    init(defaults: UserDefaults = .standard) { self.defaults = defaults }\n", '        let organizeByDate: Bool\n        let deferStart: Bool\n    }\n    init(defaults: UserDefaults = .standard) { self.defaults = defaults }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Configuration/BrowsePreferencesStore.swift', []).append(('    @discardableResult\n    func save(_ value: NativeTransferPreferences) -> Bool {\n        guard let automatic = readAutomatic() else { return false } // Preserve corrupt/future bytes across downgrades.\n        let document = Document(version: 1, organizeByDate: value.organizeByDate, deferStart: value.deferStart,\n                                autoTransferNewMedia: automatic)\n        return write(document)\n    }\n\n    func readAutomatic() -> Bool? {\n        guard let raw = defaults.object(forKey: Self.key) else { return false }\n        guard let data = raw as? Data, data.count <= 4096,\n              let document = try? JSONDecoder().decode(Document.self, from: data), document.version == 1 else { return nil }\n        return document.autoTransferNewMedia ?? false\n    }\n\n    @discardableResult\n    func saveAutomatic(_ enabled: Bool) -> Bool {\n        guard let value = read() else { return false }\n        return write(Document(version: 1, organizeByDate: value.organizeByDate, deferStart: value.deferStart,\n                              autoTransferNewMedia: enabled))\n    }\n\n    /// The UI must obtain explicit confirmation. Ordinary reads/writes never erase unknown data.\n    @discardableResult\n    func resetAfterUserConfirmation() -> Bool {\n        return write(Document(version: 1, organizeByDate: false, deferStart: false, autoTransferNewMedia: false))\n    }\n\n    private func write(_ document: Document) -> Bool {\n        guard let data = try? JSONEncoder().encode(document), data.count <= 4096 else { return false }\n        defaults.set(data, forKey: Self.key)\n', '    @discardableResult\n    func save(_ value: NativeTransferPreferences) -> Bool {\n        guard read() != nil else { return false } // Preserve corrupt/future bytes across downgrades.\n        let document = Document(version: 1, organizeByDate: value.organizeByDate, deferStart: value.deferStart)\n        guard let data = try? JSONEncoder().encode(document), data.count <= 4096 else { return false }\n        defaults.set(data, forKey: Self.key)\n'))

CHANGES.setdefault('iosApp/ZTransfer/ContentView.swift', []).append(('import SwiftUI\n\n@MainActor\nstruct ContentView: View {\n    @ObservedObject private var appearance = AppAppearanceSettings.shared\n    @StateObject private var workspace = CameraWorkspaceBridge()\n    #if DEBUG\n    @State private var showDiagnostics = false\n    @State private var showSharedComponents = false\n    #endif\n\n    var body: some View {\n        CameraWorkspace(bridge: workspace)\n            .preferredColorScheme(appearance.colorScheme)\n            .onAppear { appearance.start() }\n            #if DEBUG\n            .overlay(alignment: .topTrailing) {\n                Button("开发诊断") { showDiagnostics = true }\n                    .font(.caption).padding(8).disabled(workspace.session.running)\n            }\n            .sheet(isPresented: $showDiagnostics) {\n                NavigationStack {\n                    ScrollView {\n                        VStack(spacing: 16) {\n                            Button("检查共享 Compose 组件") { showSharedComponents = true }\n                            CameraHandshakeProbeView(probe: workspace.session)\n                            Divider(); LocationProbeView(); BluetoothProbeView()\n                        }.padding()\n                    }\n                    .toolbar { Button("关闭") { showDiagnostics = false } }\n                }\n                .sheet(isPresented: $showSharedComponents) { SharedUiProbeView() }\n            }\n            #endif\n    }\n}\n\n#Preview { ContentView() }\n', 'import SwiftUI\n\nstruct ContentView: View {\n    @ObservedObject private var appearance = AppAppearanceSettings.shared\n    #if DEBUG\n    @State private var showSharedComponents = false\n    #endif\n    private var version: String {\n        Bundle.main.object(forInfoDictionaryKey: "CFBundleShortVersionString") as? String ?? ""\n    }\n\n    var body: some View {\n        ScrollView {\n            VStack(spacing: 12) {\n                Text("Z传")\n                    .font(.largeTitle.bold())\n\n                Text("iOS 共享工程已就绪")\n                    .foregroundStyle(.secondary)\n\n                Text("v\\(version)")\n                    .font(.footnote.monospacedDigit())\n                    .foregroundStyle(.tertiary)\n\n                #if DEBUG\n                Button("检查共享 Compose 组件") { showSharedComponents = true }\n                    .sheet(isPresented: $showSharedComponents) {\n                        NavigationStack {\n                            SharedUiProbeView()\n                                .toolbar { Button("关闭") { showSharedComponents = false } }\n                        }\n                    }\n                Divider()\n                CameraHandshakeProbeView()\n                Divider()\n                LocationProbeView()\n                BluetoothProbeView()\n                #endif\n            }\n            .padding(24)\n        }\n        .preferredColorScheme(appearance.colorScheme)\n        .onAppear { appearance.start() }\n    }\n}\n\n#Preview {\n    ContentView()\n}\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('import Foundation\nimport SwiftUI\n', '#if DEBUG\nimport Foundation\nimport SwiftUI\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('}\n\n#if DEBUG\nprivate struct ProbeDocumentRequest: Identifiable {\n    let id = UUID()\n    let purpose: SystemDocumentPicker.Purpose\n}\n#endif\n\nstruct CameraProductSessionState: Equatable {\n    let requestID: Int64\n    let phase: String\n    let message: String?\n}\n\n/// Sole AP/STA session owner, reused by the shared product home and DEBUG diagnostic controls.\n/// The historical name is retained to avoid cloning or moving the camera/queue workflow.\n@MainActor\nfinal class CameraHandshakeProbe: ObservableObject {\n    @Published private(set) var productState: CameraProductSessionState?\n    @Published private(set) var sessionReady = false\n    @Published private(set) var status = "先在系统设置中加入相机热点或相机所在的 Wi-Fi。"\n    @Published private(set) var running = false\n', '}\n\nprivate struct ProbeDocumentRequest: Identifiable {\n    let id = UUID()\n    let purpose: SystemDocumentPicker.Purpose\n}\n\n/// Temporary native diagnostic UI, not the product\'s shared Compose screens.\n/// Exercises real standard AP/STA sessions and platform adapters. This is not acceptance evidence\n/// until built and run on a Mac/iPhone; STA-direct and the shared product UI remain separate work.\n@MainActor\nfinal class CameraHandshakeProbe: ObservableObject {\n    @Published private(set) var status = "先在系统设置中加入相机热点或相机所在的 Wi-Fi。"\n    @Published private(set) var running = false\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('    private var workspaceNavigation: UInt64 = 0\n    var canOpenSharedWorkspace: Bool {\n        running && sessionReady && !downloading && originalQueue != nil && apConnection != nil && previewStore != nil && catalog != nil\n    }\n    private var originalQueue: CameraOriginalQueue?\n', '    private var workspaceNavigation: UInt64 = 0\n    var canOpenSharedWorkspace: Bool {\n        running && !downloading && originalQueue != nil && apConnection != nil && previewStore != nil && catalog != nil\n    }\n    private var originalQueue: CameraOriginalQueue?\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('    private var apConnection: CameraWiFiConnection?\n    private var catalog: CameraCatalog?\n    private let transferPreferences = TransferPreferencesStore()\n    private var automaticTransfer: CameraAutomaticTransferCoordinator?\n    private var catalogTask: Task<Void, Never>?\n    @Published private(set) var scanningCatalog = false\n', '    private var apConnection: CameraWiFiConnection?\n    private var catalog: CameraCatalog?\n    private var catalogTask: Task<Void, Never>?\n    @Published private(set) var scanningCatalog = false\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('    }\n\n    @discardableResult\n    func connectProduct(host: String, stationMode: Bool, allowPairing: Bool, requestID: Int64,\n                        expectedResponder: String? = nil, service: CameraBonjourService? = nil) -> Bool {\n        guard requestID > 0, !running, !downloading, !directoryBusy, closingTask == nil else { return false }\n        guard service != nil || NativeCameraEndpointAddress.shared.normalize(raw: host) != nil else { return false }\n        start(host: host, stationMode: stationMode, persistentAP: true, allowPairing: allowPairing,\n              expectedResponder: expectedResponder, service: service, productRequestID: requestID)\n        return running\n    }\n\n    private func publishProduct(_ phase: String, message: String? = nil) {\n        guard let current = productState else { return }\n        if current.phase == "closing" && phase == "ready" { return }\n        productState = CameraProductSessionState(requestID: current.requestID, phase: phase, message: message)\n    }\n\n    func start(host: String, stationMode: Bool, persistentAP: Bool = false,\n               allowPairing: Bool = false, forcePairing: Bool = false, expectedResponder: String? = nil,\n               service: CameraBonjourService? = nil, productRequestID: Int64? = nil) {\n        guard !running, !downloading, !directoryBusy else { return }\n        stopDiscovery()\n        running = true\n        sessionReady = false\n        productState = productRequestID.map { CameraProductSessionState(requestID: $0, phase: "connecting", message: nil) }\n        samples = []\n        sampleObjects = []\n', '    }\n\n    func start(host: String, stationMode: Bool, persistentAP: Bool = false,\n               allowPairing: Bool = false, forcePairing: Bool = false, expectedResponder: String? = nil,\n               service: CameraBonjourService? = nil) {\n        guard !running, !downloading, !directoryBusy else { return }\n        stopDiscovery()\n        running = true\n        samples = []\n        sampleObjects = []\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('        status = "正在检查命令与事件通道…"\n        task = Task {\n            defer {\n                running = false; task = nil; sessionReady = false\n                if productState?.phase != "failed" { publishProduct("idle") }\n            }\n            do {\n                let address = host.trimmingCharacters(in: .whitespacesAndNewlines)\n                try Task.checkCancellation()\n                if persistentAP || service != nil {\n                    try await inspectPersistentConnection(host: address, stationMode: stationMode,\n', '        status = "正在检查命令与事件通道…"\n        task = Task {\n            defer { running = false; task = nil }\n            do {\n                let address = host.trimmingCharacters(in: .whitespacesAndNewlines)\n                if persistentAP || service != nil {\n                    try await inspectPersistentConnection(host: address, stationMode: stationMode,\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('            } catch {\n                status = Task.isCancelled ? "诊断已取消，连接已关闭。" : error.localizedDescription\n                publishProduct(Task.isCancelled ? "idle" : "failed", message: Task.isCancelled ? nil : error.localizedDescription)\n            }\n        }\n', '            } catch {\n                status = Task.isCancelled ? "诊断已取消，连接已关闭。" : error.localizedDescription\n            }\n        }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('\n    func cancel() {\n        sessionReady = false\n        if running { publishProduct("closing") }\n        automaticTransfer?.close()\n        filesPage?.close(); filesPage = nil\n        queuePage?.close(); queuePage = nil\n', '\n    func cancel() {\n        filesPage?.close(); filesPage = nil\n        queuePage?.close(); queuePage = nil\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('\n    func disconnect() {\n        sessionReady = false\n        if running { publishProduct("closing") }\n        automaticTransfer?.close()\n        queueShareTask?.cancel()\n        directoryTask?.cancel()\n', '\n    func disconnect() {\n        queueShareTask?.cancel()\n        directoryTask?.cancel()\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('        let info = sampleObjects[index]\n        let transfer = filesPage?.model.currentTransferPreferences()\n            ?? transferPreferences.read() ?? NativeTransferPreferences.companion.defaults()\n        let dayKey = OriginalFilesPageBridge.localDayKey(at: Date(), timeZone: .current)\n        Task { await queue.enqueue(info, byDate: transfer.organizeByDate, dayKey: dayKey, deferred: transfer.deferStart) }\n', '        let info = sampleObjects[index]\n        let transfer = filesPage?.model.currentTransferPreferences()\n            ?? TransferPreferencesStore().read() ?? NativeTransferPreferences.companion.defaults()\n        let dayKey = OriginalFilesPageBridge.localDayKey(at: Date(), timeZone: .current)\n        Task { await queue.enqueue(info, byDate: transfer.organizeByDate, dayKey: dayKey, deferred: transfer.deferStart) }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('        let page = OriginalFilesPageBridge(connectionID: connection.connectionID, catalog: catalog,\n            queue: queue, previews: previews, exifSource: connection, exifCache: exifCache, stationMode: connection.stationMode,\n            originals: originals ?? transferDestination, transferPreferences: transferPreferences,\n            directoryDescription: queueDestinationSummary, directoryMessage: queueDestinationError,\n            selectDirectory: { [weak self] url, completion in\n                guard let self else { completion("连接已关闭，请重新连接后选择目录。"); return }\n                self.selectQueueDirectory(url, completion: completion)\n            }, automaticTransfer: automaticTransfer,\n            automaticTransferTargetAvailable: savesToSelectedDirectory && queueDestinationError == nil)\n        filesPage = page\n        Task {\n', '        let page = OriginalFilesPageBridge(connectionID: connection.connectionID, catalog: catalog,\n            queue: queue, previews: previews, exifSource: connection, exifCache: exifCache, stationMode: connection.stationMode,\n            originals: originals ?? transferDestination, directoryDescription: queueDestinationSummary, directoryMessage: queueDestinationError,\n            selectDirectory: { [weak self] url, completion in\n                guard let self else { completion("连接已关闭，请重新连接后选择目录。"); return }\n                self.selectQueueDirectory(url, completion: completion)\n            })\n        filesPage = page\n        Task {\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('        directoryStatus = restored.failure ?? (savesToSelectedDirectory ? "已恢复所选目录保存目标。" : "队列使用应用沙盒。")\n        originalQueue = queue\n        let sessionAutomatic = CameraAutomaticTransferCoordinator(connectionID: connection.connectionID,\n            queue: queue, preferences: transferPreferences)\n        automaticTransfer = sessionAutomatic\n        queueObserver = Task {\n            for await snapshot in queue.updates {\n', '        directoryStatus = restored.failure ?? (savesToSelectedDirectory ? "已恢复所选目录保存目标。" : "队列使用应用沙盒。")\n        originalQueue = queue\n        queueObserver = Task {\n            for await snapshot in queue.updates {\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('        apConnection = connection\n        let sessionCatalog = CameraCatalog(source: connection, stationMode: stationMode, previews: previews,\n            onAddition: { [weak self] addition in await self?.receiveCatalogAddition(addition) },\n            onChange: { [weak self] snapshot in await self?.receiveCatalogChange(snapshot) })\n        catalog = sessionCatalog\n        previewStore = previews\n        defer {\n            sessionAutomatic.close(); automaticTransfer = nil\n            sessionReady = false\n            filesPage?.close(); filesPage = nil\n            queuePage?.close(); queuePage = nil\n            catalogTask?.cancel(); catalogTask = nil; scanningCatalog = false; catalog = nil\n            previewTask?.cancel(); previewStore = nil; previewImage = nil; previewPNG = nil\n            apConnection = nil; originalQueue = nil; queueObserver?.cancel(); queueObserver = nil\n', '        apConnection = connection\n        let sessionCatalog = CameraCatalog(source: connection, stationMode: stationMode, previews: previews,\n            onAddition: { [weak self] addition in await self?.receiveCatalogAddition(addition) })\n        catalog = sessionCatalog\n        previewStore = previews\n        defer {\n            filesPage?.close(); filesPage = nil\n            queuePage?.close(); queuePage = nil\n            catalogTask?.cancel(); catalog = nil\n            previewTask?.cancel(); previewStore = nil; previewImage = nil; previewPNG = nil\n            apConnection = nil; originalQueue = nil; queueObserver?.cancel(); queueObserver = nil\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('            await previews.startBackgroundFill(startDay: browse.startDay, endDay: browse.endDay,\n                                               revision: BrowsePreferencesStore.nextUpdateRevision())\n            var stores: [Int32] = [], handles: [Int32] = []\n            if productState == nil {\n            stores = try await connection.storageIDs()\n            // Raw wildcard diagnostic only, not a replacement for shared dual-card catalog rules.\n            handles = try await connection.objectHandles(storageID: -1)\n            for handle in handles.prefix(20) {\n                try Task.checkCancellation()\n', '            await previews.startBackgroundFill(startDay: browse.startDay, endDay: browse.endDay,\n                                               revision: BrowsePreferencesStore.nextUpdateRevision())\n            let stores = try await connection.storageIDs()\n            // Raw wildcard diagnostic only, not a replacement for shared dual-card catalog rules.\n            let handles = try await connection.objectHandles(storageID: -1)\n            for handle in handles.prefix(20) {\n                try Task.checkCancellation()\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('                }\n            }\n            }\n            let summary = (productState == nil ? "\\(description)：原始存储 ID \\(stores.count) 个、对象 \\(handles.count) 个；仅抽样前 20 个对象，不代表完整照片列表。" : "\\(description)：") +\n                (diskReady ? "" : "缩略图磁盘缓存不可用，当前仅使用内存缓存。")\n            try Task.checkCancellation()\n            let readyState = await connection.snapshot()\n            guard readyState.phase == .ready, productState?.phase != "closing" else { throw CameraStreamError.closed }\n            var readyMessage = description\n            if productState != nil {\n                let responder = await connection.responderGUID()\n                do {\n                    try Self.recordVerifiedStationEndpoint(stationMode: stationMode, service: service, host: host,\n                        responderGUID: responder, displayName: description, history: CameraEndpointHistory.applicationStore)\n                } catch {\n                    readyMessage += "（地址历史未保存：\\(error.localizedDescription)）"\n                }\n            }\n            try Task.checkCancellation()\n            let finalReadyState = await connection.snapshot()\n            guard finalReadyState.phase == .ready, productState?.phase != "closing" else { throw CameraStreamError.closed }\n            sessionReady = true\n            publishProduct("ready", message: readyMessage)\n            status = summary + "连接保持中。"\n            // This owner existed before connect; revision zero includes opening events still retained.\n            var eventCursor = CameraEventCursor(connectionID: connection.connectionID, revision: 0)\n            // Establish the old-photo baseline even when the user never opens the files page.\n            // This task is separate from the ONE event observer so events during scanning are retained.\n            scanningCatalog = true\n            catalogTask = Task { [weak self] in\n                defer {\n                    if self?.catalog === sessionCatalog {\n                        self?.scanningCatalog = false; self?.catalogTask = nil\n                    }\n                }\n                do {\n                    let first = try await sessionCatalog.refresh()\n                    guard let self, !Task.isCancelled, self.catalog === sessionCatalog else { return }\n                    self.receiveCatalogChange(first)\n                } catch {\n                    guard let self, !Task.isCancelled, self.catalog === sessionCatalog else { return }\n                    self.catalogStatus = "首次目录读取未完成，请在文件页刷新；不会把旧照片当作自动传输新增。"\n                }\n            }\n            var terminalMessage: String?\n            for await state in connection.updates {\n                try Task.checkCancellation()\n                if state.phase == .closed {\n                    sessionAutomatic.close(); sessionReady = false\n                    terminalMessage = state.errorDescription\n                    publishProduct("closing", message: state.errorDescription)\n                }\n                await previews.setConnected(state.phase == .ready)\n                queuePage?.setConnected(state.phase == .ready)\n', '                }\n            }\n            let summary = "\\(description)：原始存储 ID \\(stores.count) 个、对象 \\(handles.count) 个；仅抽样前 20 个对象，不代表完整照片列表。" +\n                (diskReady ? "" : "缩略图磁盘缓存不可用，当前仅使用内存缓存。")\n            status = summary + "连接保持中。"\n            // This owner existed before connect; revision zero includes opening events still retained.\n            var eventCursor = CameraEventCursor(connectionID: connection.connectionID, revision: 0)\n            for await state in connection.updates {\n                try Task.checkCancellation()\n                await previews.setConnected(state.phase == .ready)\n                queuePage?.setConnected(state.phase == .ready)\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('            }\n            try Task.checkCancellation()\n            sessionAutomatic.close()\n            await sessionCatalog.close()\n            await previews.close()\n            await queue.stop()\n            await connection.abort()\n            publishProduct(terminalMessage == nil ? "idle" : "failed", message: terminalMessage)\n        } catch {\n            sessionAutomatic.close()\n            await sessionCatalog.close()\n            await previews.close()\n', '            }\n            try Task.checkCancellation()\n            await sessionCatalog.close()\n            await previews.close()\n            await queue.stop()\n            await connection.abort()\n        } catch {\n            await sessionCatalog.close()\n            await previews.close()\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('        filesPage?.publishAddition(addition.snapshot)\n        if let media = addition.newMedia { catalogStatus = "已发现新文件：\\(media.fileName)" }\n        automaticTransfer?.receive(addition, transfer: filesPage?.model.currentTransferPreferences())\n    }\n\n    /// This history belongs to standard STA. An AP hotspot must not overwrite the same camera\'s\n    /// LAN address, and a Bonjour service name is never guessed to be a resolved host address.\n    nonisolated static func recordVerifiedStationEndpoint(stationMode: Bool, service: CameraBonjourService?, host: String,\n        responderGUID: String?, displayName: String, history: () throws -> CameraEndpointHistory) throws {\n        guard stationMode, service == nil, let responderGUID else { return }\n        try history().recordSuccessful(responderGUID: responderGUID, displayName: displayName,\n                                       address: CameraEndpointAddress.parse(host))\n    }\n\n    private func receiveCatalogChange(_ snapshot: CameraCatalogSnapshot) {\n        guard apConnection?.connectionID == snapshot.connectionID else { return }\n        filesPage?.publishAddition(snapshot)\n    }\n\n', '        filesPage?.publishAddition(addition.snapshot)\n        if let media = addition.newMedia { catalogStatus = "已发现新文件：\\(media.fileName)" }\n        // Automatic queue admission is deliberately not enabled before W05\'s real option is wired.\n    }\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('}\n\n#if DEBUG\nstruct CameraHandshakeProbeView: View {\n    @ObservedObject var probe: CameraHandshakeProbe\n    @State private var address = PtpConstants.shared.CAMERA_IP\n    @State private var stationMode = false\n', '}\n\nstruct CameraHandshakeProbeView: View {\n    @StateObject private var probe = CameraHandshakeProbe()\n    @State private var address = PtpConstants.shared.CAMERA_IP\n    @State private var stationMode = false\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraBonjourDiscovery.swift', []).append(('    let endpoint: NWEndpoint\n    let name: String\n    let alternatives: [NWEndpoint]\n    // Selection binds a concrete service type; if the preferred service disappears a retry must\n    // require explicit re-selection, not silently route the old row ID to its alternate endpoint.\n    var id: String {\n        guard case .service(_, let type, _, _) = endpoint, let group = Self.advertisedKey(endpoint) else { return String(describing: endpoint) }\n        return group + "|type:" + type.lowercased()\n    }\n    init(endpoint: NWEndpoint, name: String, alternatives: [NWEndpoint] = []) {\n        self.endpoint = endpoint; self.name = name; self.alternatives = alternatives\n    }\n    var selectableAlternatives: [CameraBonjourService] {\n        alternatives.map { endpoint in\n            let type: String\n            if case .service(_, let value, _, _) = endpoint { type = value } else { type = "服务" }\n            return CameraBonjourService(endpoint: endpoint, name: name + " · " + type)\n        }\n    }\n    /// Group the same advertised instance across service types, NOT authenticated camera identity.\n    /// Keep alternate endpoints: equal display names alone must never discard an observed service.\n    static func coalesced(_ endpoints: [NWEndpoint]) -> [CameraBonjourService] {\n        var groups: [String: Set<NWEndpoint>] = [:]\n        for endpoint in endpoints {\n            guard let key = advertisedKey(endpoint), case .service(_, let type, _, _) = endpoint,\n                  CameraBonjourDiscovery.serviceTypes.contains(type) else { continue }\n            groups[key, default: []].insert(endpoint)\n        }\n        return groups.keys.sorted().compactMap { key in\n            let choices = (groups[key] ?? []).sorted {\n                let left = preference($0), right = preference($1)\n                return left == right ? String(describing: $0) < String(describing: $1) : left < right\n            }\n            guard let primary = choices.first, case .service(let name, _, _, _) = primary else { return nil }\n            return CameraBonjourService(endpoint: primary, name: name, alternatives: Array(choices.dropFirst()))\n        }\n    }\n    private static func preference(_ endpoint: NWEndpoint) -> Int {\n        guard case .service(_, let type, _, _) = endpoint else { return 2 }\n        return type == "_ptp._tcp" ? 0 : 1\n    }\n    private static func advertisedKey(_ endpoint: NWEndpoint) -> String? {\n        guard case .service(let name, _, let domain, let interface) = endpoint else { return nil }\n        let interfaceScope = interface.map { "\\($0.index):\\($0.name)" } ?? "any-interface"\n        return [name.precomposedStringWithCanonicalMapping.lowercased(), domain.lowercased(), interfaceScope]\n            .map { "\\($0.utf8.count):\\($0)" }.joined(separator: "|")\n    }\n}\n\n', '    let endpoint: NWEndpoint\n    let name: String\n    var id: String { String(describing: endpoint) }\n}\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraBonjourDiscovery.swift', []).append(('    let searching: Bool\n    let message: String?\n}\n\nenum CameraBonjourBrowserState { case ready, issue(String) }\nprotocol CameraBonjourBrowser: AnyObject {\n    var onResults: (([NWEndpoint]) -> Void)? { get set }\n    var onState: ((CameraBonjourBrowserState) -> Void)? { get set }\n    func start(on queue: DispatchQueue)\n    func cancel()\n}\n\nprivate final class AppleCameraBonjourBrowser: CameraBonjourBrowser {\n    var onResults: (([NWEndpoint]) -> Void)?\n    var onState: ((CameraBonjourBrowserState) -> Void)?\n    private let browser: NWBrowser\n    init(type: String) {\n        let parameters = NWParameters.tcp\n        parameters.requiredInterfaceType = .wifi\n        browser = NWBrowser(for: .bonjour(type: type, domain: "local."), using: parameters)\n    }\n    func start(on queue: DispatchQueue) {\n        browser.browseResultsChangedHandler = { [weak self] results, _ in self?.onResults?(results.map(\\.endpoint)) }\n        browser.stateUpdateHandler = { [weak self] state in\n            switch state {\n            case .ready: self?.onState?(.ready)\n            case .waiting(let error), .failed(let error): self?.onState?(.issue(error.localizedDescription))\n            default: break\n            }\n        }\n        browser.start(queue: queue)\n    }\n    func cancel() {\n        browser.browseResultsChangedHandler = nil; browser.stateUpdateHandler = nil\n        onResults = nil; onState = nil; browser.cancel()\n    }\n}\n\n', '    let searching: Bool\n    let message: String?\n}\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraBonjourDiscovery.swift', []).append(('    private let continuation: AsyncStream<CameraBonjourSnapshot>.Continuation\n    private let queue = DispatchQueue(label: "com.ztransfer.camera.bonjour")\n    private let browserFactory: (String) -> CameraBonjourBrowser\n    private var browsers: [String: CameraBonjourBrowser] = [:]\n    private var results: [String: [NWEndpoint]] = [:]\n    private var issues: [String: String] = [:]\n    private var generation = UUID()\n    private var deadline: DispatchWorkItem?\n\n    init(browserFactory: @escaping (String) -> CameraBonjourBrowser = { AppleCameraBonjourBrowser(type: $0) }) {\n        self.browserFactory = browserFactory\n        var output: AsyncStream<CameraBonjourSnapshot>.Continuation!\n        updates = AsyncStream(bufferingPolicy: .bufferingNewest(1)) { output = $0 }\n', '    private let continuation: AsyncStream<CameraBonjourSnapshot>.Continuation\n    private let queue = DispatchQueue(label: "com.ztransfer.camera.bonjour")\n    private var browsers: [String: NWBrowser] = [:]\n    private var results: [String: Set<NWBrowser.Result>] = [:]\n    private var issues: [String: String] = [:]\n    private var generation = UUID()\n    private var deadline: DispatchWorkItem?\n\n    init() {\n        var output: AsyncStream<CameraBonjourSnapshot>.Continuation!\n        updates = AsyncStream(bufferingPolicy: .bufferingNewest(1)) { output = $0 }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraBonjourDiscovery.swift', []).append(('            let id = UUID(); self.generation = id\n            for type in Self.serviceTypes {\n                let browser = self.browserFactory(type)\n                self.browsers[type] = browser\n                browser.onResults = { [weak self] results in\n                    self?.queue.async { [weak self] in\n                        guard let self, self.generation == id else { return }\n                        self.results[type] = results\n                        self.publish()\n                    }\n                }\n                browser.onState = { [weak self] state in\n                    self?.queue.async { [weak self] in\n                        guard let self, self.generation == id else { return }\n                        switch state {\n                        case .ready: self.issues[type] = nil\n                        case .issue(let message): self.issues[type] = message\n                        }\n                        self.publish()\n                    }\n                }\n                browser.start(queue: self.queue)\n', '            let id = UUID(); self.generation = id\n            for type in Self.serviceTypes {\n                let parameters = NWParameters.tcp\n                parameters.requiredInterfaceType = .wifi\n                let browser = NWBrowser(for: .bonjour(type: type, domain: "local."), using: parameters)\n                self.browsers[type] = browser\n                browser.browseResultsChangedHandler = { [weak self] results, _ in\n                    guard let self, self.generation == id else { return }\n                    self.results[type] = results\n                    self.publish()\n                }\n                browser.stateUpdateHandler = { [weak self] state in\n                    guard let self, self.generation == id else { return }\n                    switch state {\n                    case .ready: self.issues[type] = nil\n                    case .waiting(let error), .failed(let error): self.issues[type] = error.localizedDescription\n                    default: break\n                    }\n                    self.publish()\n                }\n                browser.start(queue: self.queue)\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraBonjourDiscovery.swift', []).append(('        deadline?.cancel(); deadline = nil\n        browsers.values.forEach {\n            $0.onResults = nil; $0.onState = nil; $0.cancel()\n        }\n        browsers.removeAll()\n', '        deadline?.cancel(); deadline = nil\n        browsers.values.forEach {\n            $0.browseResultsChangedHandler = nil; $0.stateUpdateHandler = nil; $0.cancel()\n        }\n        browsers.removeAll()\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraBonjourDiscovery.swift', []).append(('\n    private func publish() {\n        let candidates = CameraBonjourService.coalesced(results.values.flatMap { $0 })\n        let message: String?\n        if !issues.isEmpty { message = issues.keys.sorted().compactMap { issues[$0] }.joined(separator: "；") }\n', '\n    private func publish() {\n        var seen = Set<NWEndpoint>()\n        var candidates: [CameraBonjourService] = []\n        for result in results.values.flatMap({ $0 }) {\n            guard case .service(let name, _, _, _) = result.endpoint,\n                  seen.insert(result.endpoint).inserted else { continue }\n            candidates.append(CameraBonjourService(endpoint: result.endpoint, name: name))\n        }\n        candidates.sort { $0.id < $1.id }\n        let message: String?\n        if !issues.isEmpty { message = issues.keys.sorted().compactMap { issues[$0] }.joined(separator: "；") }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('    func snapshot() async -> CameraConnectionSnapshot\n    func newObjectInfo(handle: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> PtpObjectInfo\n    func catalogStorageIDs(permitted: @escaping @Sendable () async -> Bool) async throws -> [Int32]\n    func catalogObjectHandles(storageID: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> [Int32]\n    func catalogObjectInfo(handle: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> PtpObjectInfo\n}\n\nextension CameraCatalogSource {\n    func catalogStorageIDs(permitted: @escaping @Sendable () async -> Bool) async throws -> [Int32] {\n        guard await permitted() else { throw CameraStreamError.operationInProgress }\n        return try await storageIDs()\n    }\n    func catalogObjectHandles(storageID: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> [Int32] {\n        guard await permitted() else { throw CameraStreamError.operationInProgress }\n        return try await objectHandles(storageID: storageID)\n    }\n    func catalogObjectInfo(handle: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> PtpObjectInfo {\n        guard await permitted() else { throw CameraStreamError.operationInProgress }\n        return try await objectInfo(handle: handle)\n    }\n    func newObjectInfo(handle: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> PtpObjectInfo {\n        guard await permitted() else { throw CameraStreamError.operationInProgress }\n', '    func snapshot() async -> CameraConnectionSnapshot\n    func newObjectInfo(handle: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> PtpObjectInfo\n}\n\nextension CameraCatalogSource {\n    func newObjectInfo(handle: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> PtpObjectInfo {\n        guard await permitted() else { throw CameraStreamError.operationInProgress }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('/// Normal AP/STA catalog only. One immutable camera generation; failed enumeration never replaces\n/// the last good snapshot with an empty list. Shared code owns handle order and backup merging.\n/// One connection-owned worker coalesces event invalidations. Only authoritative, complete,\n/// same-generation results replace rows; automatic admission remains a separate coordinator.\nactor CameraCatalog {\n    private let source: CameraCatalogSource\n', '/// Normal AP/STA catalog only. One immutable camera generation; failed enumeration never replaces\n/// the last good snapshot with an empty list. Shared code owns handle order and backup merging.\n/// Event revisions are invalidations, not lossless events; callers must schedule another scan when\n/// changedWhileScanning is true. Automatic transfer/event reconciliation is a separate coordinator.\nactor CameraCatalog {\n    private let source: CameraCatalogSource\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('    private(set) var needsEventRescan = false\n    private let onAddition: (@Sendable (CameraCatalogAddition) async -> Void)?\n    private let onChange: (@Sendable (CameraCatalogSnapshot) async -> Void)?\n    private struct ChangeRequest: Sendable { let token: UInt64; let full: Bool }\n    private var changeSerial: UInt64 = 0\n    private var pendingChange: ChangeRequest?\n    private var changeWorker: Task<Void, Never>?\n    // Full manual enumeration commits its baseline before metadata, just like Android. Keep these\n    // candidates across a failed/raced scan so that this early commit cannot swallow event catch-up.\n    private var scanCatchupHandles = Set<Int32>()\n    private var scanCatchupMedia: [Int32: CameraFileInfo] = [:]\n    private var activeScanNewHandles = Set<Int32>()\n\n    init(source: CameraCatalogSource, stationMode: Bool, previews: CameraPreviewStore? = nil,\n         onAddition: (@Sendable (CameraCatalogAddition) async -> Void)? = nil,\n         onChange: (@Sendable (CameraCatalogSnapshot) async -> Void)? = nil) {\n        self.source = source; self.stationMode = stationMode; self.previews = previews\n        self.onAddition = onAddition\n        self.onChange = onChange\n    }\n    deinit { resolver?.cancel(); changeWorker?.cancel() }\n    func snapshot() -> CameraCatalogSnapshot? { latest }\n\n    func refresh(detectNewHandles: Bool = false) async throws -> CameraCatalogSnapshot {\n        try await scanCatalog(detectNewHandles: detectNewHandles, change: nil)\n    }\n\n    private func scanCatalog(detectNewHandles: Bool, change: ChangeRequest?) async throws -> CameraCatalogSnapshot {\n        guard !closed else { throw CameraStreamError.closed }\n        guard !scanning else { throw CameraStreamError.operationInProgress }\n', '    private(set) var needsEventRescan = false\n    private let onAddition: (@Sendable (CameraCatalogAddition) async -> Void)?\n\n    init(source: CameraCatalogSource, stationMode: Bool, previews: CameraPreviewStore? = nil,\n         onAddition: (@Sendable (CameraCatalogAddition) async -> Void)? = nil) {\n        self.source = source; self.stationMode = stationMode; self.previews = previews\n        self.onAddition = onAddition\n    }\n    deinit { resolver?.cancel() }\n    func snapshot() -> CameraCatalogSnapshot? { latest }\n\n    func refresh(detectNewHandles: Bool = false) async throws -> CameraCatalogSnapshot {\n        guard !closed else { throw CameraStreamError.closed }\n        guard !scanning else { throw CameraStreamError.operationInProgress }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('        scanning = true\n        scanGeneration &+= 1\n        let generation = scanGeneration\n        publicationRevision &+= 1\n        activeScanNewHandles.removeAll()\n        defer { scanning = false; activeScanNewHandles.removeAll(); wakeChanges(); wakeResolver() }\n        let fillScan = await previews?.beginCatalogScan()\n        do {\n', '        scanning = true\n        scanGeneration &+= 1\n        publicationRevision &+= 1\n        defer { scanning = false; wakeResolver() }\n        let fillScan = await previews?.beginCatalogScan()\n        do {\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('            guard before.phase == .ready else { throw CameraStreamError.notConnected }\n            guard before.connectionID == source.connectionID else { throw CameraStreamError.closed }\n            let admission: @Sendable () async -> Bool = { [weak self] in\n                guard let change else { return true }\n                return await self?.mayChange(change, generation: generation, duringScan: true) ?? false\n            }\n            let stores: [Int32]\n            if change != nil { stores = try await source.catalogStorageIDs(permitted: admission) }\n            else { stores = try await source.storageIDs() }\n            let scan = NativeCameraCatalogScan(rawStorageIds: Self.native(stores), stationMode: stationMode)\n            var enumeratedHandles: [Int32] = []\n            for index in 0..<Int(scan.storageCount) {\n                try Task.checkCancellation()\n                let handles: [Int32]\n                if change != nil {\n                    handles = try await source.catalogObjectHandles(storageID: scan.queryStorageId(index: Int32(index)), permitted: admission)\n                } else { handles = try await source.objectHandles(storageID: scan.queryStorageId(index: Int32(index))) }\n                guard scan.addHandles(index: Int32(index), handles: Self.native(handles)) else { throw CameraStreamError.invalidArgument }\n                enumeratedHandles.append(contentsOf: handles)\n', '            guard before.phase == .ready else { throw CameraStreamError.notConnected }\n            guard before.connectionID == source.connectionID else { throw CameraStreamError.closed }\n            let stores = try await source.storageIDs()\n            let scan = NativeCameraCatalogScan(rawStorageIds: Self.native(stores), stationMode: stationMode)\n            var enumeratedHandles: [Int32] = []\n            for index in 0..<Int(scan.storageCount) {\n                try Task.checkCancellation()\n                let handles = try await source.objectHandles(storageID: scan.queryStorageId(index: Int32(index)))\n                guard scan.addHandles(index: Int32(index), handles: Self.native(handles)) else { throw CameraStreamError.invalidArgument }\n                enumeratedHandles.append(contentsOf: handles)\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(("            // The raw handle list is already authoritative, even if later ObjectInfo is partial.\n            // Like Android, disabled detection still advances the baseline; no first-scan catch-up.\n            if handleBaseline.hasSnapshot {\n                activeScanNewHandles = Set(enumeratedHandles.filter {\n                    handleBaseline.shouldResolve(handle: $0, visibleFiles: latest?.files ?? [])\n                })\n                scanCatchupHandles.formUnion(activeScanNewHandles.filter {\n                    change != nil || detectNewHandles || pendingObjects[$0] != nil\n                })\n            }\n            // An event-driven scan is speculative until every metadata read and revision fence pass.\n            // Manual scan keeps Android's early-enumeration semantics and explicit partial result.\n            var handleDelta: CameraHandleDelta?\n            if change == nil {\n                handleDelta = handleBaseline.acceptEnumeration(handles: Self.native(enumeratedHandles), detectNewHandles: detectNewHandles)\n            }\n            while true {\n                try Task.checkCancellation()\n                if let handle = scan.nextReadHandle()?.int32Value {\n                    let info: PtpObjectInfo?\n                    do {\n                        if change != nil { info = try await source.catalogObjectInfo(handle: handle, permitted: admission) }\n                        else { info = try await source.objectInfo(handle: handle) }\n                    }\n                    catch is CameraOperationError {\n                        // A fully consumed object-level rejection/incomplete dataset is a partial scan,\n", '            // The raw handle list is already authoritative, even if later ObjectInfo is partial.\n            // Like Android, disabled detection still advances the baseline; no first-scan catch-up.\n            let handleDelta = handleBaseline.acceptEnumeration(handles: Self.native(enumeratedHandles), detectNewHandles: detectNewHandles)\n            while true {\n                try Task.checkCancellation()\n                if let handle = scan.nextReadHandle()?.int32Value {\n                    let info: PtpObjectInfo?\n                    do { info = try await source.objectInfo(handle: handle) }\n                    catch is CameraOperationError {\n                        // A fully consumed object-level rejection/incomplete dataset is a partial scan,\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('            try Task.checkCancellation()\n            guard !closed, after.phase == .ready, after.connectionID == before.connectionID else { throw CameraStreamError.closed }\n            if let change {\n                guard await mayChange(change, generation: generation, duringScan: true),\n                      before.eventRevision == after.eventRevision,\n                      after.eventRevision <= (receivedEventRevision ?? 0) else {\n                    throw CameraStreamError.operationInProgress\n                }\n                guard scan.metadataComplete else {\n                    throw CameraOperationError.malformedDataset(operation: PtpConstants.shared.GET_OBJECT_INFO)\n                }\n                handleDelta = handleBaseline.acceptEnumeration(handles: Self.native(enumeratedHandles), detectNewHandles: true)\n            }\n            var files: [CameraFileInfo] = []\n            var infos: [Int32: PtpObjectInfo] = [:]\n', '            try Task.checkCancellation()\n            guard !closed, after.phase == .ready, after.connectionID == before.connectionID else { throw CameraStreamError.closed }\n            var files: [CameraFileInfo] = []\n            var infos: [Int32: PtpObjectInfo] = [:]\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('            // Keep old complete rows on partial metadata failure; return the partial attempt explicitly\n            // for diagnostics. A future incremental reconciler may merge it, never infer missing=deleted.\n            let previousFiles = latest?.files ?? []\n            if result.metadataComplete && !result.changedWhileScanning { latest = result }\n            resolverFailed = false\n            if result.metadataComplete && !result.changedWhileScanning && result.revision >= (receivedEventRevision ?? 0) {\n                needsEventRescan = false\n                if change == nil || pendingChange?.token == change?.token { pendingChange = nil }\n            }\n            if result.changedWhileScanning { requestChange(full: true) }\n            if let fillScan { _ = await previews?.finishCatalogScan(fillScan, snapshot: result) }\n            if result.metadataComplete && !result.changedWhileScanning {\n                if change != nil, !closed, let onChange { await onChange(result) }\n                await publishScanAdditions(result, previousFiles: previousFiles)\n            }\n            return result\n        } catch {\n', '            // Keep old complete rows on partial metadata failure; return the partial attempt explicitly\n            // for diagnostics. A future incremental reconciler may merge it, never infer missing=deleted.\n            if result.metadataComplete { latest = result }\n            resolverFailed = false\n            if result.metadataComplete && !result.changedWhileScanning && result.revision >= (receivedEventRevision ?? 0) {\n                needsEventRescan = false\n            }\n            if let fillScan { _ = await previews?.finishCatalogScan(fillScan, snapshot: result) }\n            return result\n        } catch {\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('        if let receivedEventRevision, batch.cursor.revision <= receivedEventRevision { return }\n        receivedEventRevision = batch.cursor.revision\n        if batch.requiresRescan { requestChange(full: true); return }\n        for event in batch.events {\n            let handle = Int32(truncatingIfNeeded: event.firstParameter)\n            if event.code == Lab.shared.EVT_OBJECT_ADDED, scanning, activeScanNewHandles.contains(handle) {\n                // This scan may already have committed its raw baseline. Preserve the actual event,\n                // without turning a manual detectNewHandles=false enumeration into auto discovery.\n                scanCatchupHandles.insert(handle)\n            }\n            if event.code == Lab.shared.EVT_OBJECT_REMOVED {\n                pendingObjects.removeValue(forKey: handle) // Withdraw even an in-flight metadata read.\n                pendingOrder.removeAll { $0 == handle }\n                scanCatchupHandles.remove(handle)\n                scanCatchupMedia.removeValue(forKey: handle)\n                // An unknown removed handle may still invalidate an in-progress first scan.\n                if scanning || !handleBaseline.hasSnapshot || handle == 0 || handle == -1 ||\n                    !handleBaseline.shouldResolve(handle: handle, visibleFiles: latest?.files ?? []) {\n                    requestChange(full: false)\n                }\n            } else if [Int32(0x4004), 0x4005, 0x4007, 0x400C].contains(event.code) {\n                // Standard StoreAdded/Removed, ObjectInfoChanged, StorageInfoChanged. NOT\n                // DevicePropChanged (0x4006): exposure changes must not churn the photo catalog.\n                requestChange(full: true)\n            } else if event.code == Lab.shared.EVT_OBJECT_ADDED,\n                      handleBaseline.shouldResolve(handle: handle, visibleFiles: latest?.files ?? []), pendingObjects[handle] == nil {\n', '        if let receivedEventRevision, batch.cursor.revision <= receivedEventRevision { return }\n        receivedEventRevision = batch.cursor.revision\n        if batch.requiresRescan { needsEventRescan = true; return }\n        for event in batch.events {\n            let handle = Int32(truncatingIfNeeded: event.firstParameter)\n            if event.code == Lab.shared.EVT_OBJECT_REMOVED {\n                pendingObjects.removeValue(forKey: handle) // Withdraw even an in-flight metadata read.\n                pendingOrder.removeAll { $0 == handle }\n                needsEventRescan = true // Actual removed-row reconciliation belongs to W03/W04.\n            } else if event.code == Lab.shared.EVT_OBJECT_ADDED,\n                      handleBaseline.shouldResolve(handle: handle, visibleFiles: latest?.files ?? []), pendingObjects[handle] == nil {\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('\n    func close() {\n        closed = true; scanGeneration &+= 1; resolver?.cancel(); changeWorker?.cancel()\n        pendingChange = nil; scanCatchupHandles.removeAll()\n        scanCatchupMedia.removeAll()\n        pendingObjects.removeAll(); pendingOrder.removeAll()\n    }\n\n    private static func nowMs() -> Int64 { Int64(ProcessInfo.processInfo.systemUptime * 1000) }\n\n    private func requestChange(full: Bool) {\n        changeSerial &+= 1\n        pendingChange = ChangeRequest(token: changeSerial, full: full || pendingChange?.full == true)\n        needsEventRescan = true\n        wakeChanges()\n    }\n\n    private func wakeChanges() {\n        guard !closed, !resolverFailed, !scanning, pendingChange != nil, changeWorker == nil else { return }\n        changeWorker = Task { [weak self] in\n            // Coalesce Event + GetEventEx duplicates, without blocking the connection observer.\n            do { try await Task.sleep(nanoseconds: 90_000_000) } catch { return }\n            while !Task.isCancelled, let delay = await self?.reconcileChange() {\n                do { try await Task.sleep(nanoseconds: UInt64(delay) * 1_000_000) } catch { break }\n            }\n            await self?.changesFinished()\n        }\n    }\n    private func changesFinished() { changeWorker = nil; wakeChanges(); wakeResolver() }\n\n    private func mayChange(_ request: ChangeRequest, generation: UInt64, duringScan: Bool) async -> Bool {\n        guard !closed, !Task.isCancelled, !resolverFailed, scanGeneration == generation,\n              scanning == duringScan, pendingChange?.token == request.token else { return false }\n        if let previews, !(await previews.allowsCatalogReconciliation()) { return false }\n        return !closed && !Task.isCancelled && !resolverFailed && scanGeneration == generation &&\n            scanning == duringScan && pendingChange?.token == request.token\n    }\n\n    /// Like Android syncCameraHandleCatalog: all required GetObjectHandles must succeed before\n    /// removing anything; no ObjectInfo/thumbnail/EXIF read on this lightweight deletion path.\n    private func reconcileChange() async -> Int64? {\n        guard !closed, !resolverFailed, !Task.isCancelled, let request = pendingChange else { return nil }\n        guard !scanning else { return 90 }\n        let generation = scanGeneration\n        guard await mayChange(request, generation: generation, duringScan: false) else { return 90 }\n        do {\n            if request.full || latest == nil || !handleBaseline.hasSnapshot {\n                _ = try await scanCatalog(detectNewHandles: true, change: request)\n            } else if let base = latest {\n                let admission: @Sendable () async -> Bool = { [weak self] in\n                    await self?.mayChange(request, generation: generation, duringScan: false) ?? false\n                }\n                let before = await source.snapshot()\n                guard before.phase == .ready, before.connectionID == source.connectionID else { throw CameraStreamError.closed }\n                let plan = NativeCameraCatalogScan(rawStorageIds: Self.native(base.storageIDs), stationMode: stationMode)\n                var queries = Set<Int32>()\n                var handles = Set<Int32>()\n                var handleOrder: [Int32] = []\n                for index in 0..<Int(plan.storageCount) {\n                    let storage = plan.queryStorageId(index: Int32(index))\n                    if queries.insert(storage).inserted {\n                        for handle in try await source.catalogObjectHandles(storageID: storage, permitted: admission) {\n                            if handles.insert(handle).inserted { handleOrder.append(handle) }\n                        }\n                    }\n                }\n                let after = await source.snapshot()\n                guard await mayChange(request, generation: generation, duringScan: false),\n                      after.phase == .ready, after.connectionID == before.connectionID,\n                      before.eventRevision == after.eventRevision,\n                      after.eventRevision <= (receivedEventRevision ?? 0) else { return 90 }\n                let nativeHandles = Self.native(handleOrder)\n                guard let files = NativeCameraCatalogReconciliation.shared.reconcile(publishedFiles: base.files,\n                    currentHandles: nativeHandles, indexedInfos: base.indexedObjectInfos),\n                    let delta = handleBaseline.acceptIdleEnumeration(handles: nativeHandles) else { return 2_000 }\n                let indexed = base.indexedObjectInfos.filter { handles.contains($0.handle) }\n                let infos = base.objectInfos.filter { handles.contains($0.key) }\n                pendingOrder.removeAll { !handles.contains($0) }\n                pendingObjects = pendingObjects.filter { handles.contains($0.key) }\n                scanCatchupHandles.formIntersection(handles)\n                scanCatchupMedia = scanCatchupMedia.filter { handles.contains($0.key) }\n                for handle in handleOrder where handleBaseline.shouldResolve(handle: handle, visibleFiles: files) {\n                    if pendingObjects[handle] == nil {\n                        pendingObjects[handle] = PendingObject(next: Self.nowMs() + NewCameraObjectPolicy.shared.COALESCE_MS)\n                        pendingOrder.append(handle)\n                    }\n                }\n                publicationRevision &+= 1\n                let updated = CameraCatalogSnapshot(connectionID: base.connectionID, revision: after.eventRevision,\n                    storageIDs: base.storageIDs, files: files, objectInfos: infos, totalHandles: handles.count,\n                    metadataComplete: true, changedWhileScanning: false, handleDelta: delta,\n                    publicationRevision: publicationRevision, indexedObjectInfos: indexed)\n                latest = updated\n                // Consume THIS request before awaiting publication. A later event owns a new token.\n                pendingChange = nil; needsEventRescan = false\n                _ = await previews?.reconcile(updated)\n                if !closed, !Task.isCancelled, let onChange { await onChange(updated) }\n            }\n        } catch CameraStreamError.operationInProgress { return 90 }\n        catch is CameraOperationError { return 2_000 } // Busy/malformed/partial is never deletion.\n        catch is CancellationError {\n            if !closed { resolverFailed = true; needsEventRescan = true }\n            return nil\n        }\n        catch {\n            // A transport failure is not a reason to keep sending on a poisoned connection.\n            if !closed { resolverFailed = true; needsEventRescan = true }\n            return nil\n        }\n        return pendingChange == nil ? nil : 90\n    }\n\n    private func publishScanAdditions(_ result: CameraCatalogSnapshot, previousFiles: [CameraFileInfo]) async {\n        var compared = previousFiles\n        var additions: [CameraCatalogAddition] = []\n        for info in result.indexedObjectInfos where scanCatchupHandles.contains(info.handle) {\n            guard let file = NewCameraObjectPolicy.shared.publicationFile(info: info) else { continue }\n            let isNew = NewCameraObjectPolicy.shared.isNew(files: compared, handle: info.handle, info: file)\n            compared = NewCameraObjectPolicy.shared.publish(files: compared, handle: info.handle, info: file)\n            let saved = scanCatchupMedia[info.handle]\n            let sameSavedIdentity = saved?.fileName == file.fileName && saved?.size == file.size && saved?.captureDate == file.captureDate\n            let media = (isNew || sameSavedIdentity) && NewCameraObjectPolicy.shared.automaticMedia(file: file) ? file : nil\n            if let media { scanCatchupMedia[info.handle] = media }\n            additions.append(CameraCatalogAddition(snapshot: result, info: info, newMedia: media))\n        }\n        // A complete scan accounts for every raw handle, including non-media/folders. Only failed\n        // or raced scans retain candidates; repeating a successful scan must not enqueue them again.\n        scanCatchupHandles = scanCatchupHandles.intersection(Set(result.objectInfos.keys))\n        scanCatchupMedia = scanCatchupMedia.filter { scanCatchupHandles.contains($0.key) }\n        for addition in additions {\n            guard !closed, !Task.isCancelled, !needsEventRescan,\n                  latest?.publicationRevision == result.publicationRevision else { return }\n            scanCatchupHandles.remove(addition.info.handle)\n            scanCatchupMedia.removeValue(forKey: addition.info.handle)\n            if let onAddition { await onAddition(addition) }\n        }\n    }\n\n    private func wakeResolver() {\n', '\n    func close() {\n        closed = true; scanGeneration &+= 1; resolver?.cancel()\n        pendingObjects.removeAll(); pendingOrder.removeAll()\n    }\n\n    private static func nowMs() -> Int64 { Int64(ProcessInfo.processInfo.systemUptime * 1000) }\n\n    private func wakeResolver() {\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraPreviewStore.swift', []).append(('    }\n    func allowsObjectResolution() -> Bool { !closed && foregroundUses.isEmpty }\n    /// Idle catalog reconciliation must yield to transfers as well as foreground image requests.\n    /// Do not test scanToken here: this admission also belongs to the active catalog scan itself.\n    func allowsCatalogReconciliation() -> Bool { !closed && !transfersBusy && foregroundUses.isEmpty }\n    func endForegroundUse(_ token: UUID) {\n        if foregroundUses.remove(token) != nil { wakeFill(retryFailures: foregroundUses.isEmpty) }\n', '    }\n    func allowsObjectResolution() -> Bool { !closed && foregroundUses.isEmpty }\n    func endForegroundUse(_ token: UUID) {\n        if foregroundUses.remove(token) != nil { wakeFill(retryFailures: foregroundUses.isEmpty) }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraWiFiConnection.swift', []).append(('    private let previewPolicy = NativePreviewPolicy()\n    private var thumbnailCacheIdentity: String?\n    private var verifiedResponderGUID: String?\n\n    init(command: CameraTCPStream, event: CameraTCPStream, stationMode: Bool = false) {\n', '    private let previewPolicy = NativePreviewPolicy()\n    private var thumbnailCacheIdentity: String?\n\n    init(command: CameraTCPStream, event: CameraTCPStream, stationMode: Bool = false) {\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraWiFiConnection.swift', []).append(('            thumbnailCacheIdentity = previewPolicy.cameraKey(info: identity, responderGuid: ack.responderGuidHex,\n                                                             sessionId: connectionID.uuidString)\n            verifiedResponderGUID = NikonStaBridge.shared.normalizeGuid(value: ack.responderGuidHex)\n            phase = .ready\n            publish()\n', '            thumbnailCacheIdentity = previewPolicy.cameraKey(info: identity, responderGuid: ack.responderGuidHex,\n                                                             sessionId: connectionID.uuidString)\n            phase = .ready\n            publish()\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraWiFiConnection.swift', []).append(('\n    func thumbnailCacheKey() -> String? { phase == .ready ? thumbnailCacheIdentity : nil }\n    /// Pairing/history may use only the responder acknowledged by this ready session, never a\n    /// display name, Bonjour candidate or thumbnail-cache fallback containing a session UUID.\n    func responderGUID() -> String? { phase == .ready ? verifiedResponderGUID : nil }\n\n    private func initializeStation(options: StationConnectionOptions, responder: String?,\n', '\n    func thumbnailCacheKey() -> String? { phase == .ready ? thumbnailCacheIdentity : nil }\n\n    private func initializeStation(options: StationConnectionOptions, responder: String?,\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/CameraWiFiConnection.swift', []).append(('        }\n        return info\n    }\n\n    func catalogStorageIDs(permitted: @escaping @Sendable () async -> Bool) async throws -> [Int32] {\n        // Event-driven storage changes require a fresh wire query, never handshake-prefetched IDs.\n        try await catalogIdentifiers(operation: PtpConstants.shared.GET_STORAGE_IDS, parameters: [], permitted: permitted)\n    }\n\n    func catalogObjectHandles(storageID: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> [Int32] {\n        try await catalogIdentifiers(operation: PtpConstants.shared.GET_OBJECT_HANDLES,\n                                     parameters: [storageID, -1, 0], permitted: permitted)\n    }\n\n    func catalogObjectInfo(handle: Int32, permitted: @escaping @Sendable () async -> Bool) async throws -> PtpObjectInfo {\n        let operation = PtpConstants.shared.GET_OBJECT_INFO\n        let data = try await catalogMetadata(operation: operation, parameters: [handle], limit: 64 * 1024, permitted: permitted)\n        guard let info = PtpIPChannel.objectInfo(handle: handle, payload: data) else {\n            throw CameraOperationError.malformedDataset(operation: operation)\n        }\n        return info\n    }\n\n    private func catalogIdentifiers(operation: Int32, parameters: [Int32],\n                                    permitted: @escaping @Sendable () async -> Bool) async throws -> [Int32] {\n        let data = try await catalogMetadata(operation: operation, parameters: parameters, limit: 16 * 1024 * 1024, permitted: permitted)\n        guard let ids = PtpIPChannel.identifiers(data) else { throw CameraOperationError.malformedDataset(operation: operation) }\n        return ids\n    }\n\n    private func catalogMetadata(operation: Int32, parameters: [Int32], limit: Int,\n                                 permitted: @escaping @Sendable () async -> Bool) async throws -> Data {\n        let result = try await previewCommand(operation: operation, parameters: parameters, limit: limit, admission: { [weak self] in\n            // PtpIPCommandSession invokes this after FIFO acquisition, BEFORE allocating a TID.\n            guard let self, await self.backgroundReadsAllowed() else { return false }\n            return await permitted()\n        })\n        guard result.code == PtpConstants.shared.RESPONSE_OK else {\n            throw CameraOperationError.rejected(operation: operation, response: result.code)\n        }\n        guard let data = result.payload else { throw CameraOperationError.malformedDataset(operation: operation) }\n        return data\n    }\n\n', '        }\n        return info\n    }\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/StationProfileStore.swift', []).append(("\n/// Only this installation's stable PC identity and authoritative pairing acknowledgements.\n/// Address metadata stays in CameraEndpointHistory; this existing identity format is preserved.\n/// Atomic local writes precede pacing. Forgetting one responder never regenerates the initiator.\nfinal class StationProfileStore: @unchecked Sendable {\n    private struct Document: Codable {\n", "\n/// Only this installation's stable PC identity and authoritative pairing acknowledgements.\n/// Full profile history/selection/Bonjour remain separate. Atomic local writes precede pacing.\nfinal class StationProfileStore: @unchecked Sendable {\n    private struct Document: Codable {\n"))

CHANGES.setdefault('iosApp/ZTransfer/Network/StationProfileStore.swift', []).append(('\n    static func applicationStore() throws -> StationProfileStore {\n        try StationProfileStore(file: applicationFile())\n    }\n\n    private static func applicationFile() throws -> URL {\n        let support = try FileManager.default.url(for: .applicationSupportDirectory, in: .userDomainMask,\n                                                 appropriateFor: nil, create: true)\n        return support.appendingPathComponent("ZTransfer/station-identity.json")\n    }\n\n    @discardableResult static func recoverApplicationIdentity(confirmed: Bool) throws -> URL? {\n        try recoverCorruptStore(file: applicationFile(), confirmed: confirmed)\n    }\n\n', '\n    static func applicationStore() throws -> StationProfileStore {\n        let support = try FileManager.default.url(for: .applicationSupportDirectory, in: .userDomainMask,\n                                                 appropriateFor: nil, create: true)\n        return try StationProfileStore(file: support.appendingPathComponent("ZTransfer/station-identity.json"))\n    }\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/StationProfileStore.swift', []).append(('        defer { Self.diskLock.unlock() }\n        guard let normalized = NikonStaBridge.shared.normalizeGuid(value: responder) else { return false }\n        return try currentDocument().pairedResponders.contains(normalized)\n    }\n\n', '        defer { Self.diskLock.unlock() }\n        guard let normalized = NikonStaBridge.shared.normalizeGuid(value: responder) else { return false }\n        return try Self.read(file).pairedResponders.contains(normalized)\n    }\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/StationProfileStore.swift', []).append(('        defer { Self.diskLock.unlock() }\n        guard let normalized = NikonStaBridge.shared.normalizeGuid(value: responder) else { throw CameraStationError.missingIdentity }\n        var document = try currentDocument()\n        if document.pairedResponders.contains(normalized) { return }\n        document.pairedResponders.append(normalized)\n        try JSONEncoder().encode(document).write(to: file, options: .atomic)\n    }\n\n    func pairedResponderGUIDs() throws -> [String] {\n        Self.diskLock.lock(); defer { Self.diskLock.unlock() }\n        return Array(Set(try currentDocument().pairedResponders)).sorted()\n    }\n\n    func forgetResponder(_ responder: String) throws {\n        guard let normalized = NikonStaBridge.shared.normalizeGuid(value: responder) else { throw CameraStationError.missingIdentity }\n        Self.diskLock.lock(); defer { Self.diskLock.unlock() }\n        var document = try currentDocument()\n        guard document.pairedResponders.contains(normalized) else { return }\n        document.pairedResponders.removeAll { $0 == normalized }\n        try JSONEncoder().encode(document).write(to: file, options: .atomic)\n    }\n\n    /// Only an explicitly confirmed recovery may archive an unreadable/unsupported document.\n    /// A valid identity store is never reset here; use forgetResponder for one-camera re-pairing.\n    @discardableResult static func recoverCorruptStore(file: URL, confirmed: Bool) throws -> URL? {\n        guard confirmed else { throw CameraEndpointError.confirmationRequired }\n        guard file.isFileURL else { throw CameraStationError.corruptIdentityStore }\n        Self.diskLock.lock(); defer { Self.diskLock.unlock() }\n        guard FileManager.default.fileExists(atPath: file.path) else { return nil }\n        if (try? Self.read(file)) != nil { return nil }\n        let values = try file.resourceValues(forKeys: [.isRegularFileKey, .isSymbolicLinkKey])\n        guard values.isRegularFile == true, values.isSymbolicLink != true else { throw CameraStationError.corruptIdentityStore }\n        let archive = file.deletingLastPathComponent().appendingPathComponent("station-identity-backup-\\(UUID().uuidString).json")\n        // Move only this exact regular file. Its complete bytes survive; next explicit open creates\n        // a fresh installation identity and requires pairing again (never pretend old markers apply).\n        try FileManager.default.moveItem(at: file, to: archive)\n        return archive\n    }\n\n', '        defer { Self.diskLock.unlock() }\n        guard let normalized = NikonStaBridge.shared.normalizeGuid(value: responder) else { throw CameraStationError.missingIdentity }\n        var document = try Self.read(file)\n        if document.pairedResponders.contains(normalized) { return }\n        document.pairedResponders.append(normalized)\n        try JSONEncoder().encode(document).write(to: file, options: .atomic)\n    }\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Network/StationProfileStore.swift', []).append(('        return document\n    }\n\n    private func currentDocument() throws -> Document {\n        let document = try Self.read(file)\n        guard Data(document.initiator.utf8) == identity else { throw CameraStationError.corruptIdentityStore }\n        return document\n    }\n}\n', '        return document\n    }\n}\n'))

CHANGES.setdefault('iosApp/ZTransfer/Storage/IndexedOriginalReader.swift', []).append(('            }\n            guard (try input.read(upToCount: 1) ?? Data()).isEmpty else { throw OriginalIndexError.incompleteMetadata }\n            var finalState = stat()\n            guard fstat(input.fileDescriptor, &finalState) == 0, finalState.st_size == size else {\n                throw OriginalIndexError.incompleteMetadata\n            }\n            try checkCancellation()\n            return data\n', '            }\n            guard (try input.read(upToCount: 1) ?? Data()).isEmpty else { throw OriginalIndexError.incompleteMetadata }\n            try checkCancellation()\n            return data\n'))

CHANGES.setdefault('iosApp/ZTransfer/Storage/PreviewExifReader.swift', []).append(('           let properties = CGImageSourceCopyPropertiesAtIndex(source, 0, nil) as? [String: Any] {\n            result = metadata(properties, locale: locale, rawRationals: rationals)\n        } else {\n            // A valid TIFF/NEF metadata directory need not contain a decodable image in this\n            // range. The bounded shared reader still owns tag selection and original rules.\n            result = metadata([:], locale: locale, rawRationals: rationals)\n        }\n        if cancellation.isCancelled { throw CancellationError() }\n        if reader.failed { throw OriginalIndexError.incompleteMetadata }\n', '           let properties = CGImageSourceCopyPropertiesAtIndex(source, 0, nil) as? [String: Any] {\n            result = metadata(properties, locale: locale, rawRationals: rationals)\n        } else { result = nil }\n        if cancellation.isCancelled { throw CancellationError() }\n        if reader.failed { throw OriginalIndexError.incompleteMetadata }\n'))

CHANGES.setdefault('iosApp/ZTransfer/Storage/PreviewImageDecoder.swift', []).append(('    nonisolated static func rawPreviewPixels(_ data: Data) throws -> Int64 {\n        try Task.checkCancellation()\n        return try autoreleasepool {\n            guard data.count <= Int(Int32.max), NativeRawPreviewBridge.shared.isCompleteJpeg(data: data as NSData),\n                  let source = CGImageSourceCreateWithData(data as CFData,\n                      [kCGImageSourceShouldCache: false] as CFDictionary),\n                  let properties = CGImageSourceCopyPropertiesAtIndex(source, 0, nil) as? [CFString: Any],\n                  let dimensions = Self.dimensions(properties) else { return -1 }\n            try Task.checkCancellation()\n            return LocalRawPreviewPolicy.shared.pixelCount(width: dimensions.width, height: dimensions.height)\n        }\n    }\n\n    /// Header values must fit the existing shared Int representation, without NSNumber truncation.\n    /// This is not a product resolution limit or a downsampling policy.\n    nonisolated static func dimensions(_ properties: [CFString: Any]) -> (width: Int32, height: Int32)? {\n        func edge(_ key: CFString) -> Int32? {\n            guard let number = properties[key] as? NSNumber,\n                  CFGetTypeID(number) != CFBooleanGetTypeID() else { return nil }\n            let value = number.doubleValue\n            guard value.isFinite, value >= 1, value <= Double(Int32.max), value.rounded(.towardZero) == value else { return nil }\n            return Int32(value)\n        }\n        guard let width = edge(kCGImagePropertyPixelWidth), let height = edge(kCGImagePropertyPixelHeight) else { return nil }\n        return (width, height)\n    }\n\n', '    nonisolated static func rawPreviewPixels(_ data: Data) throws -> Int64 {\n        try Task.checkCancellation()\n        guard NativeRawPreviewBridge.shared.isCompleteJpeg(data: data as NSData),\n              let source = CGImageSourceCreateWithData(data as CFData,\n                  [kCGImageSourceShouldCache: false] as CFDictionary),\n              let properties = CGImageSourceCopyPropertiesAtIndex(source, 0, nil) as? [CFString: Any],\n              let width = properties[kCGImagePropertyPixelWidth] as? NSNumber,\n              let height = properties[kCGImagePropertyPixelHeight] as? NSNumber,\n              (1...Int64(Int32.max)).contains(width.int64Value),\n              (1...Int64(Int32.max)).contains(height.int64Value) else { return -1 }\n        return LocalRawPreviewPolicy.shared.pixelCount(width: width.int32Value, height: height.int32Value)\n    }\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/Storage/PreviewImageDecoder.swift', []).append(("    /// RAW embedded-JPEG selection and TIFF's camera fallback are separate routes.\n    func originalBitmapPNG(_ data: Data) throws -> Data {\n        try withDataSource(data, maximumBytes: Int(Int32.max)) { source in\n            guard let properties = CGImageSourceCopyPropertiesAtIndex(source, 0, nil) as? [CFString: Any],\n                  let dimensions = Self.dimensions(properties) else { throw PreviewImageError.invalidSize }\n            let pixels = Int(dimensions.width).multipliedReportingOverflow(by: Int(dimensions.height))\n            guard !pixels.overflow, !pixels.partialValue.multipliedReportingOverflow(by: 4).overflow else {\n                throw PreviewImageError.invalidSize\n            }\n            try Task.checkCancellation()\n            guard let image = CGImageSourceCreateImageAtIndex(source, 0,\n                [kCGImageSourceShouldCacheImmediately: true] as CFDictionary) else { throw PreviewImageError.invalidImage }\n            // Encoding without source metadata strips orientation while preserving the decoded pixel grid.\n            return try encodePNG(image, maximumBytes: Int(Int32.max))\n        }\n    }\n\n", "    /// RAW embedded-JPEG selection and TIFF's camera fallback are separate routes.\n    func originalBitmapPNG(_ data: Data) throws -> Data {\n        try Task.checkCancellation()\n        guard !data.isEmpty,\n              let source = CGImageSourceCreateWithData(data as CFData,\n                  [kCGImageSourceShouldCache: false] as CFDictionary),\n              CGImageSourceGetCount(source) > 0,\n              let image = CGImageSourceCreateImageAtIndex(source, 0,\n                  [kCGImageSourceShouldCacheImmediately: true] as CFDictionary) else { throw PreviewImageError.invalidImage }\n        // Encoding without source metadata strips orientation while preserving the decoded pixel grid.\n        return try encodePNG(image, maximumBytes: Int(Int32.max))\n    }\n\n"))

CHANGES.setdefault('iosApp/ZTransfer/Storage/PreviewImageDecoder.swift', []).append(('    /// Diagnostic/grid/effect images keep their existing orientation-normalized path.\n    func fhdPreviewPNG(_ data: Data) throws -> Data {\n        try withDataSource(data, maximumBytes: 32 * 1024 * 1024) { source in\n            let decoded = try image(source, maximumPixelSize: 1920, honorOrientation: false)\n            return try encodePNG(decoded, maximumBytes: 20 * 1024 * 1024)\n        }\n    }\n\n    private func encodePNG(_ decoded: CGImage, maximumBytes: Int) throws -> Data {\n        try Task.checkCancellation()\n        return try autoreleasepool {\n            let output = NSMutableData()\n            guard let destination = CGImageDestinationCreateWithData(output, "public.png" as CFString, 1, nil) else {\n                throw PreviewImageError.invalidImage\n            }\n            CGImageDestinationAddImage(destination, decoded, nil)\n            try Task.checkCancellation()\n            guard CGImageDestinationFinalize(destination), output.length <= maximumBytes else { throw PreviewImageError.invalidImage }\n            try Task.checkCancellation()\n            return output as Data\n        }\n    }\n\n    /// ImageIO temporaries have a per-operation lifetime even on long-lived Swift concurrency threads.\n    /// Native output owners retain only the returned CGImage/Data, never an autoreleased source cache.\n    private func withDataSource<T>(_ data: Data, maximumBytes: Int, body: (CGImageSource) throws -> T) throws -> T {\n        try Task.checkCancellation()\n        guard !data.isEmpty, data.count <= maximumBytes else { throw PreviewImageError.invalidImage }\n        return try autoreleasepool {\n            guard let source = CGImageSourceCreateWithData(data as CFData,\n                [kCGImageSourceShouldCache: false] as CFDictionary), CGImageSourceGetCount(source) > 0 else {\n                throw PreviewImageError.invalidImage\n            }\n            try Task.checkCancellation()\n            let value = try body(source)\n            try Task.checkCancellation()\n            return value\n        }\n    }\n\n    func decode(_ data: Data, maximumPixelSize: Int = 2048) throws -> CGImage {\n        try Task.checkCancellation()\n        guard (1...4096).contains(maximumPixelSize) else { throw PreviewImageError.invalidSize }\n        return try withDataSource(data, maximumBytes: 32 * 1024 * 1024) {\n            try image($0, maximumPixelSize: maximumPixelSize)\n        }\n    }\n\n    func decodeFile(_ url: URL, maximumPixelSize: Int = 2048) throws -> CGImage {\n        try Task.checkCancellation()\n        guard (1...4096).contains(maximumPixelSize) else { throw PreviewImageError.invalidSize }\n        return try autoreleasepool {\n            guard url.isFileURL, (try? url.resourceValues(forKeys: [.isRegularFileKey]).isRegularFile) == true,\n                  let source = CGImageSourceCreateWithURL(url as CFURL,\n                      [kCGImageSourceShouldCache: false] as CFDictionary) else { throw PreviewImageError.invalidImage }\n            return try image(source, maximumPixelSize: maximumPixelSize)\n        }\n    }\n\n', '    /// Diagnostic/grid/effect images keep their existing orientation-normalized path.\n    func fhdPreviewPNG(_ data: Data) throws -> Data {\n        try Task.checkCancellation()\n        guard !data.isEmpty, data.count <= 32 * 1024 * 1024,\n              let source = CGImageSourceCreateWithData(data as CFData,\n                  [kCGImageSourceShouldCache: false] as CFDictionary) else { throw PreviewImageError.invalidImage }\n        let decoded = try image(source, maximumPixelSize: 1920, honorOrientation: false)\n        return try encodePNG(decoded, maximumBytes: 20 * 1024 * 1024)\n    }\n\n    private func encodePNG(_ decoded: CGImage, maximumBytes: Int) throws -> Data {\n        try Task.checkCancellation()\n        let output = NSMutableData()\n        guard let destination = CGImageDestinationCreateWithData(output, "public.png" as CFString, 1, nil) else {\n            throw PreviewImageError.invalidImage\n        }\n        CGImageDestinationAddImage(destination, decoded, nil)\n        guard CGImageDestinationFinalize(destination), output.length <= maximumBytes else { throw PreviewImageError.invalidImage }\n        try Task.checkCancellation()\n        return output as Data\n    }\n\n    func decode(_ data: Data, maximumPixelSize: Int = 2048) throws -> CGImage {\n        try Task.checkCancellation()\n        guard !data.isEmpty, data.count <= 32 * 1024 * 1024,\n              let source = CGImageSourceCreateWithData(data as CFData,\n                  [kCGImageSourceShouldCache: false] as CFDictionary) else { throw PreviewImageError.invalidImage }\n        return try image(source, maximumPixelSize: maximumPixelSize)\n    }\n\n    func decodeFile(_ url: URL, maximumPixelSize: Int = 2048) throws -> CGImage {\n        try Task.checkCancellation()\n        guard url.isFileURL, (try? url.resourceValues(forKeys: [.isRegularFileKey]).isRegularFile) == true,\n              let source = CGImageSourceCreateWithURL(url as CFURL,\n                  [kCGImageSourceShouldCache: false] as CFDictionary) else { throw PreviewImageError.invalidImage }\n        return try image(source, maximumPixelSize: maximumPixelSize)\n    }\n\n'))

CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(('/// A page adapter, not another camera/queue owner. The diagnostic/session owner forwards its ONE queue observer.\n@MainActor\nfinal class OriginalFilesPageBridge: NSObject, ObservableObject, Identifiable, NativeFilesPagePlatform, NativePreviewReadPlatform, NativeDirectorySettingsPlatform, NativeAutomaticTransferSettingsPlatform, UIDocumentPickerDelegate {\n    let id = UUID()\n    let queuePage: OriginalQueuePageBridge\n', '/// A page adapter, not another camera/queue owner. The diagnostic/session owner forwards its ONE queue observer.\n@MainActor\nfinal class OriginalFilesPageBridge: NSObject, ObservableObject, Identifiable, NativeFilesPagePlatform, NativePreviewReadPlatform, NativeDirectorySettingsPlatform, UIDocumentPickerDelegate {\n    let id = UUID()\n    let queuePage: OriginalQueuePageBridge\n'))

CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(('    private let preferences: BrowsePreferencesStore\n    private let transferPreferences: TransferPreferencesStore\n    private let automaticTransfer: CameraAutomaticTransferCoordinator?\n    private let automaticTransferTargetAvailable: Bool\n    private let directorySelection: ((URL, @escaping (String?) -> Void) -> Void)?\n    private var directoryPicker: (request: Int64, controller: UIDocumentPickerViewController)?\n', '    private let preferences: BrowsePreferencesStore\n    private let transferPreferences: TransferPreferencesStore\n    private let directorySelection: ((URL, @escaping (String?) -> Void) -> Void)?\n    private var directoryPicker: (request: Int64, controller: UIDocumentPickerViewController)?\n'))

CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(('         preferences: BrowsePreferencesStore? = nil, originals: OriginalFilesReading? = nil,\n         transferPreferences: TransferPreferencesStore? = nil, directoryDescription: String? = nil,\n         directoryMessage: String? = nil, selectDirectory: ((URL, @escaping (String?) -> Void) -> Void)? = nil,\n         automaticTransfer: CameraAutomaticTransferCoordinator? = nil, automaticTransferTargetAvailable: Bool = false) {\n        self.connectionID = connectionID; self.catalog = catalog; self.queue = queue; self.previews = previews\n        self.exifSource = exifSource; self.exifCache = exifCache\n', '         preferences: BrowsePreferencesStore? = nil, originals: OriginalFilesReading? = nil,\n         transferPreferences: TransferPreferencesStore? = nil, directoryDescription: String? = nil,\n         directoryMessage: String? = nil, selectDirectory: ((URL, @escaping (String?) -> Void) -> Void)? = nil) {\n        self.connectionID = connectionID; self.catalog = catalog; self.queue = queue; self.previews = previews\n        self.exifSource = exifSource; self.exifCache = exifCache\n'))

CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(('        self.preferences = preferences ?? BrowsePreferencesStore()\n        self.transferPreferences = transferPreferences ?? TransferPreferencesStore()\n        self.automaticTransfer = automaticTransfer\n        self.automaticTransferTargetAvailable = automaticTransferTargetAvailable\n        self.directorySelection = selectDirectory\n        queuePage = OriginalQueuePageBridge(connectionID: connectionID, queue: queue, previews: previews, stationMode: stationMode)\n        super.init()\n        precondition(model.attachPreviewReads(platform: self))\n        if automaticTransfer != nil { precondition(model.automaticTransfer.attach(platform: self)) }\n        if selectDirectory != nil {\n            precondition(model.directory.attach(platform: self, description: directoryDescription, message: directoryMessage))\n', '        self.preferences = preferences ?? BrowsePreferencesStore()\n        self.transferPreferences = transferPreferences ?? TransferPreferencesStore()\n        self.directorySelection = selectDirectory\n        queuePage = OriginalQueuePageBridge(connectionID: connectionID, queue: queue, previews: previews, stationMode: stationMode)\n        super.init()\n        precondition(model.attachPreviewReads(platform: self))\n        if selectDirectory != nil {\n            precondition(model.directory.attach(platform: self, description: directoryDescription, message: directoryMessage))\n'))

CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(('        }\n    }\n    func setConnected(_ value: Bool) {\n        if !closed { connected = value; queuePage.setConnected(value); model.automaticTransfer.reload() }\n    }\n    func readBrowsePreferences() -> NativeBrowsePreferences? {\n        guard !closed else { return nil }\n', '        }\n    }\n    func setConnected(_ value: Bool) { if !closed { connected = value; queuePage.setConnected(value) } }\n    func readBrowsePreferences() -> NativeBrowsePreferences? {\n        guard !closed else { return nil }\n'))

CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(('    func saveTransferPreferences(value: NativeTransferPreferences) -> Bool {\n        guard !closed else { return false }\n        let saved = transferPreferences.save(value)\n        automaticTransfer?.preferencesDidChange()\n        model.automaticTransfer.reload()\n        return saved\n    }\n    func readAutomaticTransfer() -> NativeAutomaticTransferPreferences {\n        let enabled = closed ? nil : automaticTransfer?.enabled\n        return NativeAutomaticTransferPreferences(enabled: enabled ?? false, valid: enabled != nil,\n            canEnable: automaticTransferTargetAvailable && connected)\n    }\n    func changeAutomaticTransfer(enabled: Bool) -> Bool {\n        guard !closed, !enabled || (automaticTransferTargetAvailable && connected) else { return false }\n        return automaticTransfer?.setEnabled(enabled) ?? false\n    }\n    func resetTransferPreferencesAfterConfirmation() -> Bool {\n        guard !closed else { return false }\n        return automaticTransfer?.resetAfterUserConfirmation() ?? false\n    }\n    func saveBrowsePreferences(value: NativeBrowsePreferences) -> Bool {\n', '    func saveTransferPreferences(value: NativeTransferPreferences) -> Bool {\n        guard !closed else { return false }\n        return transferPreferences.save(value)\n    }\n    func saveBrowsePreferences(value: NativeBrowsePreferences) -> Bool {\n'))

CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(('                guard let data else { completion.complete(image: nil); return }\n                try Task.checkCancellation()\n                guard !self.closed, self.previewUse?.session == sessionId else { completion.complete(image: nil); return }\n                let png = try await self.decoder.originalBitmapPNG(data)\n                try Task.checkCancellation()\n', '                guard let data else { completion.complete(image: nil); return }\n                try Task.checkCancellation()\n                let png = try await self.decoder.originalBitmapPNG(data)\n                try Task.checkCancellation()\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/preview/PreviewExifRationalReader.kt', []).append(('    private val attributes: Map<PreviewExifTag, String?>,\n    val partial: Boolean = false,\n    private val supplement: PreviewExifSupplement? = null,\n    private val littleEndian: Boolean = true,\n) {\n    /** The five rational tags clear synthesized decimals; optional extras replace observed tags only. */\n    fun applyTo(values: NativePreviewExifValues) {\n        if (complete || partial) rationalTags.values.forEach { values.set(it, attributes[it]) }\n        if (complete || partial) supplement?.applyTo(values, littleEndian)\n    }\n    fun value(tag: PreviewExifTag): String? = attributes[tag]\n}\n\n/** Bounded preview attributes, not a second image/MakerNote decoder. Existing numeric-only\n * entry points retain their five-tag contract; Native metadata opts into text/ISO/GPS fields.\n * Types and Float-vs-Double attribute representation follow AndroidX ExifInterface 1.3.7.\n * No JPEG entropy/image scan, MakerNote traversal or full-file allocation is needed.\n', '    private val attributes: Map<PreviewExifTag, String?>,\n    val partial: Boolean = false,\n) {\n    /** Even a missing/invalid raw tag replaces an ImageIO synthesized decimal. */\n    fun applyTo(values: NativePreviewExifValues) {\n        if (complete || partial) rationalTags.values.forEach { values.set(it, attributes[it]) }\n    }\n    fun value(tag: PreviewExifTag): String? = attributes[tag]\n}\n\n/** Small supplement for the five numeric preview tags, not a second image/EXIF decoder.\n * Types and Float-vs-Double attribute representation follow AndroidX ExifInterface 1.3.7.\n * No JPEG entropy/image scan, MakerNote traversal or full-file allocation is needed.\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/preview/PreviewExifRationalReader.kt', []).append(('    private class Invalid : Exception()\n    private class Unavailable : Exception()\n    private enum class DirectoryKind { TIFF, EXIF, GPS, OTHER }\n\n    fun read(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues = readInternal(source, size, false)\n', '    private class Invalid : Exception()\n    private class Unavailable : Exception()\n    private enum class DirectoryKind { TIFF, EXIF, OTHER }\n\n    fun read(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues = readInternal(source, size, false)\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/preview/PreviewExifRationalReader.kt', []).append(('    fun readHeader(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues = readInternal(source, size, true)\n\n    /** Native preview metadata uses the same directory walk and limits, adding non-rational fields. */\n    fun readMetadata(source: PreviewExifByteSource, size: Long, header: Boolean): PreviewExifRationalValues =\n        readInternal(source, size, header, captureMetadata = true)\n\n    private fun readInternal(source: PreviewExifByteSource, size: Long, retainPartial: Boolean, captureMetadata: Boolean = false): PreviewExifRationalValues {\n        var budget = 8 * 1024 * 1024\n        var requests = 0\n        val rawValues = mutableMapOf<PreviewExifTag, ByteArray?>()\n        val supplement = if (captureMetadata) PreviewExifSupplement() else null\n        fun bytes(offset: Long, count: Int, end: Long = size): ByteArray {\n            if (count < 0 || count > maximumReadBytes || offset < 0 || offset > end || count.toLong() > end - offset || end > size) throw Invalid()\n', '    fun readHeader(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues = readInternal(source, size, true)\n\n    private fun readInternal(source: PreviewExifByteSource, size: Long, retainPartial: Boolean): PreviewExifRationalValues {\n        var budget = 8 * 1024 * 1024\n        var requests = 0\n        val rawValues = mutableMapOf<PreviewExifTag, ByteArray?>()\n        fun bytes(offset: Long, count: Int, end: Long = size): ByteArray {\n            if (count < 0 || count > maximumReadBytes || offset < 0 || offset > end || count.toLong() > end - offset || end > size) throw Invalid()\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/preview/PreviewExifRationalReader.kt', []).append(('                } else "$numerator/$denominator"\n            }\n            return PreviewExifRationalValues(complete, values, partial, supplement, little)\n        }\n        try {\n', '                } else "$numerator/$denominator"\n            }\n            return PreviewExifRationalValues(complete, values, partial)\n        }\n        try {\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/preview/PreviewExifRationalReader.kt', []).append(('                            kind == DirectoryKind.TIFF && tag == 0x8769 -> DirectoryKind.EXIF\n                            kind == DirectoryKind.TIFF && tag == 0x014A -> DirectoryKind.TIFF\n                            kind == DirectoryKind.TIFF && tag == 0x8825 -> if (captureMetadata) DirectoryKind.GPS else DirectoryKind.OTHER\n                            kind == DirectoryKind.EXIF && tag == 0xA005 -> DirectoryKind.OTHER\n                            else -> null\n', '                            kind == DirectoryKind.TIFF && tag == 0x8769 -> DirectoryKind.EXIF\n                            kind == DirectoryKind.TIFF && tag == 0x014A -> DirectoryKind.TIFF\n                            kind == DirectoryKind.TIFF && tag == 0x8825 -> DirectoryKind.OTHER\n                            kind == DirectoryKind.EXIF && tag == 0xA005 -> DirectoryKind.OTHER\n                            else -> null\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/preview/PreviewExifRationalReader.kt', []).append(("                            }\n                            if (pointer > 0 && pointer < end - base) ifd(pointer, pointerKind, depth + 1)\n                        }\n                        // Reuse this exact visited-offset and APP1 boundary policy. Do not ask\n                        // ImageIO to select an unrelated embedded thumbnail's metadata.\n                        val metadataType = supplement?.expectedType(tag, kind.name)\n                        if (metadataType != null && (encodedType == metadataType || encodedType == 7)) {\n                            val width = when (metadataType) { 3 -> 2; 5 -> 8; else -> 1 }\n                            if (components <= Int.MAX_VALUE / width) {\n                                val length = (components * width).toInt()\n                                val body = if (length <= 4) entries.copyOfRange(8, 8 + length)\n                                    else bytes(dataOffset(entries, 8), length, end)\n                                supplement?.accept(tag, metadataType, body, if (kind == DirectoryKind.TIFF && !root) 1 else 0)\n                            }\n                        }\n                        val target = if (kind == DirectoryKind.EXIF) rationalTags[tag] else null\n", '                            }\n                            if (pointer > 0 && pointer < end - base) ifd(pointer, pointerKind, depth + 1)\n                        }\n                        val target = if (kind == DirectoryKind.EXIF) rationalTags[tag] else null\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/ui/NativeFilesPageModel.kt', []).append(('    init { require(queue.connectionId == connectionId) }\n    val directory = NativeDirectorySettingsModel()\n    val automaticTransfer = NativeAutomaticTransferSettingsModel()\n    private var platform: NativeFilesPagePlatform? = platform\n    private var closed = false\n', '    init { require(queue.connectionId == connectionId) }\n    val directory = NativeDirectorySettingsModel()\n    private var platform: NativeFilesPagePlatform? = platform\n    private var closed = false\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/ui/NativeFilesPageModel.kt', []).append(('\n    fun currentTransferPreferences(): NativeTransferPreferences = mutableTransfers.value\n    /** After an explicit settings reset; never clears catalog, selection or queue state. */\n    fun reloadTransferPreferences() {\n        if (closed) return\n        val value = platform?.readTransferPreferences()\n        mutableTransfers.value = value ?: NativeTransferPreferences.defaults()\n        mutableTransferPreferencesFailed.value = value == null\n        automaticTransfer.reload()\n    }\n    internal fun setOrganizeByDate(value: Boolean) = changeTransfers(mutableTransfers.value.copy(organizeByDate = value))\n    internal fun setDeferStart(value: Boolean) = changeTransfers(mutableTransfers.value.copy(deferStart = value))\n', '\n    fun currentTransferPreferences(): NativeTransferPreferences = mutableTransfers.value\n    internal fun setOrganizeByDate(value: Boolean) = changeTransfers(mutableTransfers.value.copy(organizeByDate = value))\n    internal fun setDeferStart(value: Boolean) = changeTransfers(mutableTransfers.value.copy(deferStart = value))\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/ui/NativeFilesPageModel.kt', []).append(('        closed = true\n        directory.close()\n        automaticTransfer.close()\n        previewReads?.close(); previewReads = null; previewPlatform = null\n        enqueues.toList().forEach { it.cancel() }; enqueues.clear()\n', '        closed = true\n        directory.close()\n        previewReads?.close(); previewReads = null; previewPlatform = null\n        enqueues.toList().forEach { it.cancel() }; enqueues.clear()\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/ui/NativePhotoSettingsOverlay.kt', []).append(('    val appearanceState by appearance.state.collectAsState()\n    val transfers by model.transferPreferences.collectAsState()\n    val automatic by model.automaticTransfer.state.collectAsState()\n    var confirmTransferReset by remember(model) { mutableStateOf(false) }\n    val recoveryText = nativeTransferRecoveryText(appearanceState.resolvedLanguage)\n    val directory by model.directory.state.collectAsState()\n    val openingAnchor = remember { anchor }\n', '    val appearanceState by appearance.state.collectAsState()\n    val transfers by model.transferPreferences.collectAsState()\n    val directory by model.directory.state.collectAsState()\n    val openingAnchor = remember { anchor }\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/ui/NativePhotoSettingsOverlay.kt', []).append(('                onTapToPreview = model::setTapToPreview)\n            Spacer(Modifier.height(14.dp))\n            // All three controls use the same preferences document and existing transfer queue.\n            SharedSettingsCard {\n                SharedTransferDirectoryHeader(directory.description, false, text, model.directory::choose)\n', '                onTapToPreview = model::setTapToPreview)\n            Spacer(Modifier.height(14.dp))\n            // Reuse the original header; automatic-event controls appear only once actually connected.\n            SharedSettingsCard {\n                SharedTransferDirectoryHeader(directory.description, false, text, model.directory::choose)\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/ui/NativePhotoSettingsOverlay.kt', []).append(('                        hapticsEnabled = appearanceState.hapticsEnabled, modifier = Modifier.weight(1f), text = text)\n                }\n                if (automatic.available) {\n                    SharedBooleanSettingsWheel(label = text.label(SettingsTextKey.auto_transfer_new_media),\n                        checked = automatic.enabled, onCheckedChange = model.automaticTransfer::setEnabled,\n                        enabled = !automatic.failed && (automatic.canEnable || automatic.enabled),\n                        hapticsEnabled = appearanceState.hapticsEnabled, text = text)\n                    if (!automatic.canEnable) Text(text.label(SettingsTextKey.dir_please_set),\n                        color = AppTheme.colors.accentOrange, style = MaterialTheme.typography.bodySmall)\n                    if (automatic.failed) TextButton(onClick = { confirmTransferReset = true }) {\n                        Text(recoveryText.reset, color = AppTheme.colors.accentOrange)\n                    }\n                }\n            }\n            Spacer(Modifier.height(14.dp))\n', '                        hapticsEnabled = appearanceState.hapticsEnabled, modifier = Modifier.weight(1f), text = text)\n                }\n            }\n            Spacer(Modifier.height(14.dp))\n'))

CHANGES.setdefault('shared/src/commonMain/kotlin/com/ztransfer/ui/NativePhotoSettingsOverlay.kt', []).append(('        }\n    }\n    if (confirmTransferReset) AlertDialog(onDismissRequest = { confirmTransferReset = false },\n        title = { Text(recoveryText.title) }, text = { Text(recoveryText.message) },\n        confirmButton = { TextButton(onClick = {\n            confirmTransferReset = false\n            if (model.automaticTransfer.resetAfterConfirmation()) model.reloadTransferPreferences()\n        }) { Text(recoveryText.reset) } },\n        dismissButton = { TextButton(onClick = { confirmTransferReset = false }) { Text(recoveryText.cancel) } })\n}\n', '        }\n    }\n}\n'))

CHANGES.setdefault('shared/src/iosMain/kotlin/com/ztransfer/preview/NativePreviewExifRationalBridge.kt', []).append(('    }\n    fun read(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues =\n        PreviewExifRationalReader.readMetadata(source, size, header = false)\n    fun readHeader(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues =\n        PreviewExifRationalReader.readMetadata(source, size, header = true)\n}\n', '    }\n    fun read(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues =\n        PreviewExifRationalReader.read(source, size)\n    fun readHeader(source: PreviewExifByteSource, size: Long): PreviewExifRationalValues =\n        PreviewExifRationalReader.readHeader(source, size)\n}\n'))

# END REVIEWED CHANGES

def previous_parallel_batch_source(path, value):
    for current, previous in AUDIT_CHANGES.get(path, ()):
        assert value.count(current) == 1, f"parallel audit hunk changed: {path}"
        value = value.replace(current, previous, 1)
    for current, previous in CHANGES.get(path, ()):
        assert value.count(current) == 1, f"parallel batch hunk changed: {path}"
        value = value.replace(current, previous, 1)
    return value

# Exact post-checkpoint audit fixes, reviewed against 8503823.
AUDIT_CHANGES = {}
AUDIT_CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('                pendingOrder.removeAll { $0 == handle }\n                scanCatchupHandles.remove(handle)\n                // Withdraw this raw handle immediately, but retain undelivered logical-media\n                // eligibility until authoritative reconciliation checks surviving backup aliases.\n                // An unknown removed handle may still invalidate an in-progress first scan.\n                if scanning || !handleBaseline.hasSnapshot || handle == 0 || handle == -1 ||\n', '                pendingOrder.removeAll { $0 == handle }\n                scanCatchupHandles.remove(handle)\n                scanCatchupMedia.removeValue(forKey: handle)\n                // An unknown removed handle may still invalidate an in-progress first scan.\n                if scanning || !handleBaseline.hasSnapshot || handle == 0 || handle == -1 ||\n'))
AUDIT_CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('                pendingObjects = pendingObjects.filter { handles.contains($0.key) }\n                scanCatchupHandles.formIntersection(handles)\n                for handle in handleOrder where handleBaseline.shouldResolve(handle: handle, visibleFiles: files) {\n                    if pendingObjects[handle] == nil {\n', '                pendingObjects = pendingObjects.filter { handles.contains($0.key) }\n                scanCatchupHandles.formIntersection(handles)\n                scanCatchupMedia = scanCatchupMedia.filter { handles.contains($0.key) }\n                for handle in handleOrder where handleBaseline.shouldResolve(handle: handle, visibleFiles: files) {\n                    if pendingObjects[handle] == nil {\n'))
AUDIT_CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(('                _ = await previews?.reconcile(updated)\n                if !closed, !Task.isCancelled, let onChange { await onChange(updated) }\n                // A deletion can interrupt full-scan addition delivery after its baseline was\n                // committed. Replay retained candidates against THIS stable publication; they\n                // are already known to the baseline, so the normal resolver cannot recover them.\n                // The existing delivery fence handles another change/close during this callback.\n                await publishScanAdditions(updated, previousFiles: base.files)\n            }\n        } catch CameraStreamError.operationInProgress { return 90 }\n', '                _ = await previews?.reconcile(updated)\n                if !closed, !Task.isCancelled, let onChange { await onChange(updated) }\n            }\n        } catch CameraStreamError.operationInProgress { return 90 }\n'))
AUDIT_CHANGES.setdefault('iosApp/ZTransfer/Network/CameraCatalog.swift', []).append(("        var compared = previousFiles\n        var additions: [CameraCatalogAddition] = []\n        var assignedMedia: [CameraFileInfo] = []\n        scanCatchupHandles.formIntersection(Set(result.objectInfos.keys))\n        let survivingCandidates = result.indexedObjectInfos.filter { scanCatchupHandles.contains($0.handle) }\n            .compactMap { NewCameraObjectPolicy.shared.publicationFile(info: $0) }\n        scanCatchupMedia = scanCatchupMedia.filter { saved in\n            survivingCandidates.contains { Self.sameLogicalIdentity(saved.value, $0) }\n        }\n        for info in result.indexedObjectInfos where scanCatchupHandles.contains(info.handle) {\n            guard let file = NewCameraObjectPolicy.shared.publicationFile(info: info) else { continue }\n            let isNew = NewCameraObjectPolicy.shared.isNew(files: compared, handle: info.handle, info: file)\n            compared = NewCameraObjectPolicy.shared.publish(files: compared, handle: info.handle, info: file)\n            let savedIdentity = scanCatchupMedia.values.contains { Self.sameLogicalIdentity($0, file) }\n            let alreadyAssigned = assignedMedia.contains { Self.sameLogicalIdentity($0, file) }\n            let media = !alreadyAssigned && (isNew || savedIdentity) && NewCameraObjectPolicy.shared.automaticMedia(file: file) ? file : nil\n            if let media { scanCatchupMedia[info.handle] = media; assignedMedia.append(media) }\n            additions.append(CameraCatalogAddition(snapshot: result, info: info, newMedia: media))\n        }\n        // A complete scan accounts for every raw handle, including non-media/folders. Only failed\n        // or raced scans retain candidates; repeating a successful scan must not enqueue them again.\n        for addition in additions {\n            guard !closed, !Task.isCancelled, !needsEventRescan,\n                  latest?.publicationRevision == result.publicationRevision else { return }\n            scanCatchupHandles.remove(addition.info.handle)\n            if let media = addition.newMedia {\n                // Consume a logical photo once even if another pending handle is its backup.\n                scanCatchupMedia = scanCatchupMedia.filter { !Self.sameLogicalIdentity($0.value, media) }\n            }\n            if let onAddition { await onAddition(addition) }\n        }\n    }\n\n    private static func sameLogicalIdentity(_ saved: CameraFileInfo, _ candidate: CameraFileInfo) -> Bool {\n        // Reuse the exact shared logicalIdentity comparison. The deliberately different probe\n        // handle disables isNew's raw-handle shortcut; no separate Swift identity format exists.\n        let differentHandle: Int32 = saved.handle == 0 ? 1 : 0\n        return !NewCameraObjectPolicy.shared.isNew(files: [saved], handle: differentHandle, info: candidate)\n    }\n\n", '        var compared = previousFiles\n        var additions: [CameraCatalogAddition] = []\n        for info in result.indexedObjectInfos where scanCatchupHandles.contains(info.handle) {\n            guard let file = NewCameraObjectPolicy.shared.publicationFile(info: info) else { continue }\n            let isNew = NewCameraObjectPolicy.shared.isNew(files: compared, handle: info.handle, info: file)\n            compared = NewCameraObjectPolicy.shared.publish(files: compared, handle: info.handle, info: file)\n            let saved = scanCatchupMedia[info.handle]\n            let sameSavedIdentity = saved?.fileName == file.fileName && saved?.size == file.size && saved?.captureDate == file.captureDate\n            let media = (isNew || sameSavedIdentity) && NewCameraObjectPolicy.shared.automaticMedia(file: file) ? file : nil\n            if let media { scanCatchupMedia[info.handle] = media }\n            additions.append(CameraCatalogAddition(snapshot: result, info: info, newMedia: media))\n        }\n        // A complete scan accounts for every raw handle, including non-media/folders. Only failed\n        // or raced scans retain candidates; repeating a successful scan must not enqueue them again.\n        scanCatchupHandles = scanCatchupHandles.intersection(Set(result.objectInfos.keys))\n        scanCatchupMedia = scanCatchupMedia.filter { scanCatchupHandles.contains($0.key) }\n        for addition in additions {\n            guard !closed, !Task.isCancelled, !needsEventRescan,\n                  latest?.publicationRevision == result.publicationRevision else { return }\n            scanCatchupHandles.remove(addition.info.handle)\n            scanCatchupMedia.removeValue(forKey: addition.info.handle)\n            if let onAddition { await onAddition(addition) }\n        }\n    }\n\n'))
AUDIT_CHANGES.setdefault('iosApp/ZTransfer/Diagnostics/CameraHandshakeProbe.swift', []).append(('        Task {\n            let snapshot = await queue.snapshot()\n            let initialCatalog = await catalog.snapshot()\n            // Validate AFTER reading the baseline: a newer event revision invalidates its reuse.\n            let state = await connection.snapshot()\n            guard filesPage === page, apConnection === connection, self.catalog === catalog else { return }\n            page.publishQueue(snapshot)\n            page.setConnected(state.phase == .ready)\n            page.loadInitialCatalog(initialCatalog, state: state)\n        }\n    }\n', '        Task {\n            let snapshot = await queue.snapshot()\n            let state = await connection.snapshot()\n            guard filesPage === page else { return }\n            page.publishQueue(snapshot)\n            page.setConnected(state.phase == .ready)\n            page.refresh()\n        }\n    }\n'))
AUDIT_CHANGES.setdefault('iosApp/ZTransfer/UI/OriginalFilesPage.swift', []).append(("        guard let year = day.year, (1...9999).contains(year), let month = day.month, let date = day.day else { return 0 }\n        return Int32(year * 10000 + month * 100 + date)\n    }\n\n    /// Initial presentation may reuse the session owner's completed baseline. A current connection\n    /// snapshot must be sampled AFTER the catalog value; stale/partial candidates take the normal scan.\n    /// This is deliberately separate from refresh(), which always performs a user-requested scan.\n    @discardableResult\n    func loadInitialCatalog(_ value: CameraCatalogSnapshot?, state: CameraConnectionSnapshot) -> Bool {\n        guard !closed, connected, refreshTask == nil, state.connectionID == connectionID, state.phase == .ready else { return false }\n        if let value, value.connectionID == connectionID, value.revision == state.eventRevision,\n           value.metadataComplete, !value.changedWhileScanning {\n            let sequence = model.beginScan()\n            if sequence > 0, acceptCatalog(value, sequence: sequence) {\n                // publishQueue may already have started this page's initial original-index read.\n                // Catalog reuse must not skip local originals or enqueue that same read twice.\n                if originalIndexTask == nil { refreshOriginals(rescan: originalRevision < 0) }\n                return true\n            }\n        }\n        refresh()\n        return false\n    }\n\n", '        guard let year = day.year, (1...9999).contains(year), let month = day.month, let date = day.day else { return 0 }\n        return Int32(year * 10000 + month * 100 + date)\n    }\n\n'))
